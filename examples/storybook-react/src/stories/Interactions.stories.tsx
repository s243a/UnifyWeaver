import type { Meta, StoryObj } from "@storybook/react";
import React, { useState } from "react";
import {
  Tooltip,
  PanZoomCanvas,
  DrillDownChart,
  SelectableItems,
  type DrillDownItem,
} from "../components/Interactions";
import { ThemeProvider } from "../components/ThemeToggle";

const meta: Meta = {
  title: "Interactions",
  tags: ["autodocs"],
  decorators: [
    (Story) => (
      <ThemeProvider>
        <div style={{ padding: "1rem" }}>
          <Story />
        </div>
      </ThemeProvider>
    ),
  ],
  parameters: {
    docs: {
      description: {
        component: `
Interaction components generated by UnifyWeaver for rich user experiences.

**Generated from:** \`generate_interactions/2\` in \`interaction_generator.pl\`

\`\`\`prolog
% Add tooltip interaction
?- declare_interaction(my_chart, tooltip, [
    trigger(hover),
    content(data_point),
    position(top),
    delay(100)
  ]).

% Add pan/zoom interaction
?- declare_interaction(my_chart, pan_zoom, [
    enable_pan(true),
    enable_zoom(true),
    zoom_extent([0.5, 3])
  ]).

% Add drill-down interaction
?- declare_interaction(my_chart, drill_down, [
    trigger(click),
    levels([region, country, city]),
    animation(slide_in)
  ]).
\`\`\`
        `,
      },
    },
  },
};

export default meta;

export const TooltipPositions: StoryObj = {
  render: () => (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        gap: "3rem",
        padding: "4rem",
      }}
    >
      <Tooltip content="I appear on top!" position="top">
        <button
          style={{
            padding: "0.75rem 1.5rem",
            background: "var(--color-primary)",
            color: "white",
            border: "none",
            borderRadius: "var(--border-radius-md)",
            cursor: "pointer",
          }}
        >
          Top
        </button>
      </Tooltip>

      <Tooltip content="I appear on the right!" position="right">
        <button
          style={{
            padding: "0.75rem 1.5rem",
            background: "var(--color-primary)",
            color: "white",
            border: "none",
            borderRadius: "var(--border-radius-md)",
            cursor: "pointer",
          }}
        >
          Right
        </button>
      </Tooltip>

      <Tooltip content="I appear on the bottom!" position="bottom">
        <button
          style={{
            padding: "0.75rem 1.5rem",
            background: "var(--color-primary)",
            color: "white",
            border: "none",
            borderRadius: "var(--border-radius-md)",
            cursor: "pointer",
          }}
        >
          Bottom
        </button>
      </Tooltip>

      <Tooltip content="I appear on the left!" position="left">
        <button
          style={{
            padding: "0.75rem 1.5rem",
            background: "var(--color-primary)",
            color: "white",
            border: "none",
            borderRadius: "var(--border-radius-md)",
            cursor: "pointer",
          }}
        >
          Left
        </button>
      </Tooltip>
    </div>
  ),
  parameters: {
    docs: {
      description: {
        story: "Tooltips can appear in any of the four cardinal positions.",
      },
    },
  },
};

export const TooltipWithRichContent: StoryObj = {
  render: () => (
    <div style={{ display: "flex", justifyContent: "center", padding: "3rem" }}>
      <Tooltip
        content={
          <div style={{ textAlign: "left" }}>
            <div style={{ fontWeight: 600, marginBottom: "0.25rem" }}>
              Revenue Details
            </div>
            <div>Q1: $124,500</div>
            <div>Q2: $148,200</div>
            <div style={{ color: "#22c55e" }}>↑ 19.1% growth</div>
          </div>
        }
        position="bottom"
      >
        <div
          style={{
            padding: "1.5rem 2rem",
            background: "var(--color-surface)",
            borderRadius: "var(--border-radius-lg)",
            border: "1px solid var(--color-border)",
            cursor: "help",
          }}
        >
          <div style={{ fontSize: "0.875rem", color: "var(--color-text-secondary)" }}>
            Total Revenue
          </div>
          <div
            style={{
              fontSize: "1.5rem",
              fontWeight: 700,
              color: "var(--color-text-primary)",
            }}
          >
            $272,700
          </div>
          <div style={{ fontSize: "0.75rem", color: "var(--color-text-secondary)" }}>
            Hover for details
          </div>
        </div>
      </Tooltip>
    </div>
  ),
  parameters: {
    docs: {
      description: {
        story: "Tooltips can contain rich React content, not just text.",
      },
    },
  },
};

export const PanAndZoom: StoryObj = {
  render: () => {
    const [transform, setTransform] = useState({ x: 0, y: 0, scale: 1 });

    return (
      <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
        <p style={{ color: "var(--color-text-secondary)", fontSize: "0.875rem" }}>
          Click and drag to pan. Use mouse wheel to zoom in/out.
        </p>
        <PanZoomCanvas
          minZoom={0.25}
          maxZoom={4}
          onTransformChange={setTransform}
        >
          <div
            style={{
              display: "grid",
              gridTemplateColumns: "repeat(3, 100px)",
              gap: "10px",
            }}
          >
            {Array.from({ length: 9 }, (_, i) => (
              <div
                key={i}
                style={{
                  width: "100px",
                  height: "100px",
                  background: `hsl(${i * 40}, 70%, 60%)`,
                  borderRadius: "8px",
                  display: "flex",
                  alignItems: "center",
                  justifyContent: "center",
                  color: "white",
                  fontWeight: 600,
                }}
              >
                {i + 1}
              </div>
            ))}
          </div>
        </PanZoomCanvas>
      </div>
    );
  },
  parameters: {
    docs: {
      description: {
        story: `
Pan and zoom interaction for exploring large content or visualizations.

\`\`\`prolog
% Configure pan/zoom for a chart
?- declare_interaction(scatter_plot, pan_zoom, [
    enable_pan(true),
    enable_zoom(true),
    zoom_extent([0.25, 4]),
    smooth_zoom(true)
  ]).
\`\`\`
        `,
      },
    },
  },
};

const sampleDrillDownData: DrillDownItem[] = [
  {
    id: "na",
    label: "North America",
    value: 450,
    children: [
      {
        id: "us",
        label: "United States",
        value: 320,
        children: [
          { id: "ca", label: "California", value: 120 },
          { id: "tx", label: "Texas", value: 85 },
          { id: "ny", label: "New York", value: 75 },
          { id: "other-us", label: "Other", value: 40 },
        ],
      },
      { id: "canada", label: "Canada", value: 90 },
      { id: "mexico", label: "Mexico", value: 40 },
    ],
  },
  {
    id: "eu",
    label: "Europe",
    value: 380,
    children: [
      { id: "uk", label: "United Kingdom", value: 120 },
      { id: "germany", label: "Germany", value: 110 },
      { id: "france", label: "France", value: 85 },
      { id: "other-eu", label: "Other", value: 65 },
    ],
  },
  {
    id: "apac",
    label: "Asia Pacific",
    value: 290,
    children: [
      { id: "japan", label: "Japan", value: 95 },
      { id: "australia", label: "Australia", value: 75 },
      { id: "korea", label: "South Korea", value: 60 },
      { id: "other-apac", label: "Other", value: 60 },
    ],
  },
  {
    id: "latam",
    label: "Latin America",
    value: 120,
  },
];

export const DrillDown: StoryObj = {
  render: () => {
    const [path, setPath] = useState<DrillDownItem[]>([]);

    return (
      <div style={{ display: "flex", flexDirection: "column", gap: "1rem" }}>
        <p style={{ color: "var(--color-text-secondary)", fontSize: "0.875rem" }}>
          Click on bars with arrows to drill down into subcategories.
          Use the breadcrumb to navigate back.
        </p>
        <DrillDownChart data={sampleDrillDownData} onDrillDown={setPath} />
        <div
          style={{
            padding: "0.75rem",
            background: "var(--color-surface)",
            borderRadius: "var(--border-radius-md)",
            fontSize: "0.75rem",
            color: "var(--color-text-secondary)",
          }}
        >
          Current path: {path.length === 0 ? "(root)" : path.map((p) => p.label).join(" → ")}
        </div>
      </div>
    );
  },
  parameters: {
    docs: {
      description: {
        story: `
Drill-down interaction for exploring hierarchical data.

\`\`\`prolog
% Define drill-down hierarchy
?- declare_interaction(sales_chart, drill_down, [
    levels([
      level(region, data_source(regions)),
      level(country, data_source(countries)),
      level(city, data_source(cities))
    ]),
    animation(slide_in_right),
    show_breadcrumb(true)
  ]).
\`\`\`
        `,
      },
    },
  },
};

export const MultiSelect: StoryObj = {
  render: () => {
    const items = Array.from({ length: 8 }, (_, i) => ({
      id: `item-${i}`,
      label: `Item ${i + 1}`,
    }));

    return (
      <div style={{ maxWidth: "400px" }}>
        <SelectableItems
          items={items}
          multiSelect={true}
          onSelectionChange={(ids) => console.log("Selected:", ids)}
        />
      </div>
    );
  },
  parameters: {
    docs: {
      description: {
        story: `
Multi-select interaction with keyboard support.

- **Ctrl/Cmd + Click**: Toggle individual items
- **Shift + Click**: Range select
- **Arrow keys + Space**: Keyboard navigation
        `,
      },
    },
  },
};

export const SingleSelect: StoryObj = {
  render: () => {
    const items = Array.from({ length: 5 }, (_, i) => ({
      id: `option-${i}`,
      label: `Option ${i + 1}`,
    }));

    return (
      <div style={{ maxWidth: "400px" }}>
        <SelectableItems
          items={items}
          multiSelect={false}
          onSelectionChange={(ids) => console.log("Selected:", ids)}
        />
      </div>
    );
  },
  parameters: {
    docs: {
      description: {
        story: "Single-select mode where only one item can be selected at a time.",
      },
    },
  },
};

export const CombinedInteractions: StoryObj = {
  render: () => (
    <div style={{ display: "flex", flexDirection: "column", gap: "2rem" }}>
      <section>
        <h3 style={{ margin: "0 0 1rem 0", color: "var(--color-text-primary)" }}>
          Chart with Tooltips
        </h3>
        <div
          style={{
            display: "flex",
            alignItems: "flex-end",
            gap: "0.5rem",
            height: "200px",
            padding: "1rem",
            background: "var(--color-surface)",
            borderRadius: "var(--border-radius-lg)",
            border: "1px solid var(--color-border)",
          }}
        >
          {[65, 82, 45, 93, 71, 58, 88].map((value, i) => (
            <Tooltip
              key={i}
              content={`Month ${i + 1}: ${value}%`}
              position="top"
            >
              <div
                style={{
                  flex: 1,
                  height: `${value}%`,
                  background: "var(--color-primary)",
                  borderRadius: "var(--border-radius-sm) var(--border-radius-sm) 0 0",
                  cursor: "pointer",
                  transition: "opacity var(--transition-fast)",
                }}
                onMouseOver={(e) => (e.currentTarget.style.opacity = "0.8")}
                onMouseOut={(e) => (e.currentTarget.style.opacity = "1")}
              />
            </Tooltip>
          ))}
        </div>
      </section>

      <section>
        <h3 style={{ margin: "0 0 1rem 0", color: "var(--color-text-primary)" }}>
          Pannable Diagram
        </h3>
        <PanZoomCanvas>
          <svg width="400" height="200" viewBox="0 0 400 200">
            <defs>
              <marker
                id="arrow"
                viewBox="0 0 10 10"
                refX="9"
                refY="5"
                markerWidth="6"
                markerHeight="6"
                orient="auto"
              >
                <path d="M 0 0 L 10 5 L 0 10 z" fill="var(--color-primary)" />
              </marker>
            </defs>
            {/* Nodes */}
            <circle cx="50" cy="100" r="30" fill="var(--color-primary)" />
            <circle cx="200" cy="50" r="30" fill="var(--color-primary)" />
            <circle cx="200" cy="150" r="30" fill="var(--color-primary)" />
            <circle cx="350" cy="100" r="30" fill="var(--color-primary)" />
            {/* Edges */}
            <line
              x1="80" y1="90" x2="165" y2="55"
              stroke="var(--color-primary)" strokeWidth="2"
              markerEnd="url(#arrow)"
            />
            <line
              x1="80" y1="110" x2="165" y2="145"
              stroke="var(--color-primary)" strokeWidth="2"
              markerEnd="url(#arrow)"
            />
            <line
              x1="230" y1="50" x2="315" y2="90"
              stroke="var(--color-primary)" strokeWidth="2"
              markerEnd="url(#arrow)"
            />
            <line
              x1="230" y1="150" x2="315" y2="110"
              stroke="var(--color-primary)" strokeWidth="2"
              markerEnd="url(#arrow)"
            />
            {/* Labels */}
            <text x="50" y="105" textAnchor="middle" fill="white" fontSize="12">A</text>
            <text x="200" y="55" textAnchor="middle" fill="white" fontSize="12">B</text>
            <text x="200" y="155" textAnchor="middle" fill="white" fontSize="12">C</text>
            <text x="350" y="105" textAnchor="middle" fill="white" fontSize="12">D</text>
          </svg>
        </PanZoomCanvas>
      </section>
    </div>
  ),
  parameters: {
    docs: {
      description: {
        story: "Example combining multiple interaction types in realistic scenarios.",
      },
    },
  },
};

// Generated by UnifyWeaver TypeScript Target
// Module: CliArgs

// --- UnifyWeaver compound-term runtime (G-A3-12) --- BEGIN
// A Prolog compound f(A1..An) is { $: "f", args: [...] }; an atom is a string,
// true/false are booleans (G-A3-13) and a list is an array -- so `x.$` tells a
// compound from all three without ever throwing. _uwEq is Prolog ==/2: structural.
function _uwEq(a, b) {
    if (a === b) return true;
    if (a == null || b == null) return false;
    if (Array.isArray(a) || Array.isArray(b)) {
        return Array.isArray(a) && Array.isArray(b) && a.length === b.length &&
               a.every((x, i) => _uwEq(x, b[i]));
    }
    if (typeof a === "object" && typeof b === "object") {
        return a.$ === b.$ && Array.isArray(a.args) && Array.isArray(b.args) &&
               a.args.length === b.args.length &&
               a.args.every((x, i) => _uwEq(x, b.args[i]));
    }
    return false;
}
// --- UnifyWeaver compound-term runtime (G-A3-12) --- END

// --- UnifyWeaver failure sentinel (G-A3-18) --- BEGIN
// A predicate that is SEMIDET and has output arguments answers with its value
// (or G-A3-9's tuple) or with this sentinel. A module-private Symbol cannot be
// produced by any Prolog term this target lowers, and cannot be forged by data
// crossing the module edge, so `x !== _uwFail` is an exact test.
const _uwFail = Symbol("uw.fail");
// --- UnifyWeaver failure sentinel (G-A3-18) --- END

// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: flags_put/4

export function flags_put(a1, a2, a3) {
  if (a1.length === 0) {
    return [{$: "-", args: [a2, a3]}, ...[]];
  }
  if (a1.length > 0 && a1[0] != null && a1[0].$ === "-" && a1[0].args.length === 2) {
    if (a1[0].args[0] === a2) {
      return [{$: "-", args: [a1[0].args[0], a3]}, ...a1.slice(1)];
    } else {
      const _s0 = flags_put(a1.slice(1), a2, a3);
      return [{$: "-", args: [a1[0].args[0], a1[0].args[1]]}, ..._s0];
    }
  }
  throw new Error("no matching clause for flags_put/4");
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: flags_set/4

export function flags_set(a1, a2, a3) {
  if (true) {
    if (a2 === "__proto__") {
      return a1;
    } else {
      const _s0 = flags_put(a1, a2, a3);
      return _s0;
    }
  }
  throw new Error("no matching clause for flags_set/4");
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 5) {
    const argv = process.argv.slice(2, 5).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(flags_set(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Native Clause Lowering
// Predicate: js_alpha/1

function js_alpha(arg1) {
    const v2 = arg1.charCodeAt(0);
      if ((((v2 >= 97 && v2 <= 122)) ? (true) : ((v2 >= 65 && v2 <= 90)))) {
        return true;
      }
    return false;
}

// CLI entry point
// Arguments are converted by shape (numeric-looking -> number, else string);
// declared parameter types would be more precise -- see G-A3-11.3.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => (s.trim() !== "" && !Number.isNaN(Number(s))) ? Number(s) : s);
    console.log(js_alpha(...argv));
}


// Generated by UnifyWeaver TypeScript Target - Native Clause Lowering
// Predicate: js_flag_char/1

function js_flag_char(arg1) {
    const v2 = arg1.charCodeAt(0);
      if ((((v2 >= 97 && v2 <= 122)) ? (true) : ((((v2 >= 65 && v2 <= 90)) ? (true) : ((((v2 >= 48 && v2 <= 57)) ? (true) : (v2 === 45))))))) {
        return true;
      }
    return false;
}

// CLI entry point
// Arguments are converted by shape (numeric-looking -> number, else string);
// declared parameter types would be more precise -- see G-A3-11.3.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => (s.trim() !== "" && !Number.isNaN(Number(s))) ? Number(s) : s);
    console.log(js_flag_char(...argv));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: legacy_flag_tail/1

export function legacy_flag_tail(a1) {
  if (a1.length === 0) {
    return true;
  }
  if (a1.length > 0) {
    if (js_flag_char(a1[0])) {
      return legacy_flag_tail(a1.slice(1));
    }
  }
  return false;
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: looks_like_legacy_flag/1

export function looks_like_legacy_flag(a1) {
  if (true) {
    const _s0 = Array.from(a1);
    if (_s0.length > 0 && _s0[0] === "-" && _s0.slice(1).length > 0 && _s0.slice(1)[0] === "-" && _s0.slice(1).slice(1).length > 0) {
      if (js_alpha(_s0.slice(1).slice(1)[0])) {
        if (legacy_flag_tail(_s0.slice(1).slice(1).slice(1))) {
          return true;
        }
      }
    }
  }
  return false;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(looks_like_legacy_flag(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: first_char_index/4

export function first_char_index(a1, a2, a3) {
  if (a1.length === 0) {
    return -1;
  }
  if (a1.length > 0) {
    if (a1[0] === a2) {
      return a3;
    } else {
      const _s0 = (a3 + 1);
      const _s1 = first_char_index(a1.slice(1), a2, _s0);
      return _s1;
    }
  }
  throw new Error("no matching clause for first_char_index/4");
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: first_equals_index/2

export function first_equals_index(a1) {
  if (true) {
    const _s0 = Array.from(a1);
    const _s1 = first_char_index(_s0, "=", 0);
    return _s1;
  }
  throw new Error("no matching clause for first_equals_index/2");
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(first_equals_index(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Native Clause Lowering
// Predicate: substring_from/3

function substring_from(arg1, arg2) {
    const v4 = arg1.length;
    const v5 = (v4 - arg2);
    const arg3 = arg1.slice(arg2, arg2 + v5);
      return arg3;
}

// CLI entry point
// Arguments are converted by shape (numeric-looking -> number, else string);
// declared parameter types would be more precise -- see G-A3-11.3.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => (s.trim() !== "" && !Number.isNaN(Number(s))) ? Number(s) : s);
    console.log(substring_from(...argv));
}


// Generated by UnifyWeaver TypeScript Target - Native Clause Lowering
// Predicate: substring_range/4

function substring_range(arg1, arg2, arg3) {
    const v5 = (arg3 - arg2);
    const arg4 = arg1.slice(arg2, arg2 + v5);
      return arg4;
}

// CLI entry point
// Arguments are converted by shape (numeric-looking -> number, else string);
// declared parameter types would be more precise -- see G-A3-11.3.
if (process.argv.length >= 5) {
    const argv = process.argv.slice(2, 5).map(
        (s) => (s.trim() !== "" && !Number.isNaN(Number(s))) ? Number(s) : s);
    console.log(substring_range(...argv));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: split_flag_token/3

export function split_flag_token(a1) {
  if (true) {
    const _s0 = first_equals_index(a1);
    if (_s0 >= 0) {
      const _s1 = substring_range(a1, 2, _s0);
      const _s2 = (_s0 + 1);
      const _s3 = substring_from(a1, _s2);
      return [_s1, {$: "some", args: [_s3]}];
    } else {
      const _s4 = substring_from(a1, 2);
      return [_s4, "none"];
    }
  }
  throw new Error("no matching clause for split_flag_token/3");
}

// CLI entry point (multi-output: the tuple is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(split_flag_token(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: starts_with/2

export function starts_with(a1, a2) {
  if (true) {
    const _s0 = a1.length;
    const _s1 = a2.length;
    if (_s0 >= _s1) {
      const _s2 = a1.slice(0, 0 + _s1);
      if (_s2 === a2) {
        return true;
      }
    }
  }
  return false;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(starts_with(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: lenient_loop/5

export function lenient_loop(a1, a2, a3) {
  if (a1.length === 0) {
    return [a2, a3];
  }
  if (a1.length > 0) {
    if (starts_with(a1[0], "--")) {
      const [_s0, _s1] = split_flag_token(a1[0]);
      if (_s1 != null && _s1.$ === "some" && _s1.args.length === 1) {
        const _s2 = flags_set(a3, _s0, _s1.args[0]);
        return lenient_loop(a1.slice(1), a2, _s2);
      } else {
        if (a1.slice(1).length > 0 && !(looks_like_legacy_flag(a1.slice(1)[0]))) {
          const _s3 = flags_set(a3, _s0, a1.slice(1)[0]);
          return lenient_loop(a1.slice(1).slice(1), a2, _s3);
        } else {
          const _s4 = flags_set(a3, _s0, true);
          return lenient_loop(a1.slice(1), a2, _s4);
        }
      }
    } else {
      return lenient_loop(a1.slice(1), [a1[0], ...a2], a3);
    }
  }
  throw new Error("no matching clause for lenient_loop/5");
}

// CLI entry point (multi-output: the tuple is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 5) {
    const argv = process.argv.slice(2, 5).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(lenient_loop(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: parse_lenient/3

export function parse_lenient(a1) {
  if (true) {
    const [_s0, _s1] = lenient_loop(a1, [], []);
    const _s2 = [..._s0].reverse();
    return [_s2, _s1];
  }
  throw new Error("no matching clause for parse_lenient/3");
}

// CLI entry point (multi-output: the tuple is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(parse_lenient(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: lenient_result/2

export function lenient_result(a1) {
  if (true) {
    const [_s0, _s1] = parse_lenient(a1);
    return {$: "ok", args: [_s0, _s1]};
  }
  throw new Error("no matching clause for lenient_result/2");
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(lenient_result(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: merge_flags_/3

export function merge_flags_(a1, a2) {
  if (a1.length === 0) {
    return a2;
  }
  if (a1.length > 0 && a1[0] != null && a1[0].$ === "-" && a1[0].args.length === 2) {
    const _s0 = flags_set(a2, a1[0].args[0], a1[0].args[1]);
    const _s1 = merge_flags_(a1.slice(1), _s0);
    return _s1;
  }
  throw new Error("no matching clause for merge_flags_/3");
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: merge_flags/3

export function merge_flags(a1, a2) {
  if (true) {
    const _s0 = merge_flags_(a2, a1);
    return _s0;
  }
  throw new Error("no matching clause for merge_flags/3");
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(merge_flags(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: count_required/3

export function count_required(a1, a2) {
  if (a1.length === 0) {
    return a2;
  }
  if (a1.length > 0) {
    let _s2;
    if (starts_with(a1[0], "[")) {
      _s2 = a2;
    } else {
      let _s1;
      if (starts_with(a1[0], "...")) {
        _s1 = a2;
      } else {
        const _s0 = (a2 + 1);
        _s1 = _s0;
      }
      _s2 = _s1;
    }
    const _s3 = count_required(a1.slice(1), _s2);
    return _s3;
  }
  throw new Error("no matching clause for count_required/3");
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: last_element/2

export function last_element(a1) {
  if (a1.length > 0) {
    if (_uwEq(a1.slice(1), [])) {
      return a1[0];
    } else {
      const _s0 = last_element(a1.slice(1));
      return _s0;
    }
  }
  return _uwFail;
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: nth0_default/4

export function nth0_default(a1, a2, a3) {
  if (true) {
    if (a2.length > 0) {
      if (a1 === 0) {
        return a2[0];
      } else {
        const _s0 = (a1 + -1);
        const _s1 = nth0_default(_s0, a2.slice(1), a3);
        return _s1;
      }
    } else {
      return a3;
    }
  }
  throw new Error("no matching clause for nth0_default/4");
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 5) {
    const argv = process.argv.slice(2, 5).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(nth0_default(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: drop_brackets/2

export function drop_brackets(a1) {
  if (a1.length === 0) {
    return [];
  }
  if (a1.length > 0) {
    const _s0 = drop_brackets(a1.slice(1));
    let _s1;
    if ((a1[0] === "[" || a1[0] === "]")) {
      _s1 = _s0;
    } else {
      _s1 = [a1[0], ..._s0];
    }
    return _s1;
  }
  throw new Error("no matching clause for drop_brackets/2");
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: strip_brackets/2

export function strip_brackets(a1) {
  if (true) {
    const _s0 = Array.from(a1);
    const _s1 = drop_brackets(_s0);
    const _s2 = _s1.join("");
    return _s2;
  }
  throw new Error("no matching clause for strip_brackets/2");
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(strip_brackets(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: check_arity/3

export function check_arity(a1, a2) {
  if (true) {
    const _s0 = a1.length;
    const _s1 = a2.length;
    let _t2;
    let _s3;
    if ((_t2 = last_element(a2)) !== _uwFail && starts_with(_t2, "...")) {
      _s3 = true;
    } else {
      _s3 = false;
    }
    const _s4 = count_required(a2, 0);
    if (_s0 < _s4) {
      const _s5 = nth0_default(_s0, a2, "argument");
      const _s6 = strip_brackets(_s5);
      const _s7 = ("missing argument: " + _s6);
      return {$: "err", args: [_s7]};
    } else {
      if ((_s3 === false && _s0 > _s1)) {
        const _s8 = nth0_default(_s1, a1, "");
        const _s9 = ("unexpected extra argument: " + _s8);
        return {$: "err", args: [_s9]};
      } else {
        return "ok";
      }
    }
  }
  throw new Error("no matching clause for check_arity/3");
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(check_arity(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: string_member/2

export function string_member(a1, a2) {
  if (a2.length > 0) {
    if (a1 === a2[0]) {
      return true;
    } else {
      return string_member(a1, a2.slice(1));
    }
  }
  return false;
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: js_object_prototype_key/1

export function js_object_prototype_key(a1) {
  if (true) {
    if (string_member(a1, ["constructor", ...["hasOwnProperty", ...["isPrototypeOf", ...["propertyIsEnumerable", ...["toLocaleString", ...["toString", ...["valueOf", ...["__defineGetter__", ...["__defineSetter__", ...["__lookupGetter__", ...["__lookupSetter__", ...["__proto__", ...[]]]]]]]]]]]]])) {
      return true;
    }
  }
  return false;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(js_object_prototype_key(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: pair_lookup/3

export function pair_lookup(a1, a2) {
  if (a1.length > 0 && a1[0] != null && a1[0].$ === "-" && a1[0].args.length === 2) {
    if (a1[0].args[0] === a2) {
      return a1[0].args[1];
    } else {
      const _s0 = pair_lookup(a1.slice(1), a2);
      return _s0;
    }
  }
  return _uwFail;
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: option_kind/3

export function option_kind(a1, a2) {
  if (true) {
    let _t0;
    if ((_t0 = pair_lookup(a1, a2)) !== _uwFail) {
      return _t0;
    } else {
      let _t1;
      if ((_t1 = pair_lookup([{$: "-", args: ["state", "string"]}, ...[{$: "-", args: ["name", "string"]}, ...[]]], a2)) !== _uwFail) {
        return _t1;
      } else {
        if (js_object_prototype_key(a2)) {
          return "other";
        }
      }
    }
  }
  return _uwFail;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(option_kind(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: long_flag_tail/1

export function long_flag_tail(a1) {
  if (a1.length === 0) {
    return true;
  }
  if (a1.length > 0) {
    if (a1[0] === "=") {
      return true;
    } else {
      if (js_flag_char(a1[0])) {
        return long_flag_tail(a1.slice(1));
      }
    }
  }
  return false;
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: is_long_flag/1

export function is_long_flag(a1) {
  if (true) {
    const _s0 = Array.from(a1);
    if (_s0.length > 0 && _s0[0] === "-" && _s0.slice(1).length > 0 && _s0.slice(1)[0] === "-" && _s0.slice(1).slice(1).length > 0) {
      if (js_alpha(_s0.slice(1).slice(1)[0])) {
        if (long_flag_tail(_s0.slice(1).slice(1).slice(1))) {
          return true;
        }
      }
    }
  }
  return false;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(is_long_flag(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: next_value/2

export function next_value(a1) {
  if (a1.length === 0) {
    return "none";
  }
  if (a1.length > 0) {
    if ((a1[0] !== "--" && !(is_long_flag(a1[0])))) {
      return {$: "some", args: [a1[0]]};
    } else {
      return "none";
    }
  }
  throw new Error("no matching clause for next_value/2");
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(next_value(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: strict_option/11

export function strict_option(a1, a2, a3, a4, a5, a6, a7, a8) {
  if (true) {
    if (a1 === "boolean") {
      let _s1;
      if (a3 != null && a3.$ === "some" && a3.args.length === 1) {
        let _s0;
        if (a3.args[0] === "false") {
          _s0 = false;
        } else {
          _s0 = true;
        }
        _s1 = _s0;
      } else {
        _s1 = true;
      }
      const _s2 = flags_set(a8, a2, _s1);
      const [_s3, _s4, _s5] = strict_loop(a4, a5, a6, a7, _s2);
      return [_s3, _s4, _s5];
    } else {
      if (a3 != null && a3.$ === "some" && a3.args.length === 1) {
        const _s6 = flags_set(a8, a2, a3.args[0]);
        const [_s7, _s8, _s9] = strict_loop(a4, a5, a6, a7, _s6);
        return [_s7, _s8, _s9];
      } else {
        const _s10 = next_value(a4);
        if (a1 === "string") {
          if (_s10 != null && _s10.$ === "some" && _s10.args.length === 1) {
            const _s11 = flags_set(a8, a2, _s10.args[0]);
            if (a4.length > 0) {
              const [_s12, _s13, _s14] = strict_loop(a4.slice(1), a5, a6, a7, _s11);
              return [_s12, _s13, _s14];
            }
          } else {
            const _s15 = ("--" + a2);
            const _s16 = (_s15 + " needs a value");
            return [a7, a8, {$: "err", args: [_s16]}];
          }
        } else {
          if (_s10 != null && _s10.$ === "some" && _s10.args.length === 1) {
            const _s17 = flags_set(a8, a2, _s10.args[0]);
            if (a4.length > 0) {
              const [_s18, _s19, _s20] = strict_loop(a4.slice(1), a5, a6, a7, _s17);
              return [_s18, _s19, _s20];
            }
          } else {
            const _s21 = flags_set(a8, a2, true);
            const [_s22, _s23, _s24] = strict_loop(a4, a5, a6, a7, _s21);
            return [_s22, _s23, _s24];
          }
        }
      }
    }
  }
  throw new Error("no matching clause for strict_option/11");
}

// CLI entry point (multi-output: the tuple is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 10) {
    const argv = process.argv.slice(2, 10).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(strict_option(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: strict_loop/8

export function strict_loop(a1, a2, a3, a4, a5) {
  if (a1.length === 0) {
    return [a4, a5, "ok"];
  }
  if (a1.length > 0) {
    if (a2 === true) {
      return strict_loop(a1.slice(1), true, a3, [a1[0], ...a4], a5);
    } else {
      if (a1[0] === "--") {
        return strict_loop(a1.slice(1), true, a3, a4, a5);
      } else {
        if (!(starts_with(a1[0], "--"))) {
          return strict_loop(a1.slice(1), a2, a3, [a1[0], ...a4], a5);
        } else {
          const [_s0, _s1] = split_flag_token(a1[0]);
          let _t2;
          if ((_t2 = option_kind(a3, _s0)) !== _uwFail) {
            const [_s3, _s4, _s5] = strict_option(_t2, _s0, _s1, a1.slice(1), a2, a3, a4, a5);
            return [_s3, _s4, _s5];
          } else {
            const _s6 = ("unknown option --" + _s0);
            return [a4, a5, {$: "err", args: [_s6]}];
          }
        }
      }
    }
  }
  throw new Error("no matching clause for strict_loop/8");
}

// CLI entry point (multi-output: the tuple is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 7) {
    const argv = process.argv.slice(2, 7).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(strict_loop(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: parse_strict/4

export function parse_strict(a1, a2, a3) {
  if (a2 != null && a2.$ === "schema" && a2.args.length === 2) {
    const [_s0, _s1, _s2] = strict_loop(a1, false, a2.args[0], [], []);
    if (_s2 != null && _s2.$ === "err" && _s2.args.length === 1) {
      return {$: "err", args: [_s2.args[0]]};
    } else {
      const _s3 = [..._s0].reverse();
      const _s4 = check_arity(_s3, a2.args[1]);
      if (_s4 != null && _s4.$ === "err" && _s4.args.length === 1) {
        return {$: "err", args: [_s4.args[0]]};
      } else {
        const _s5 = [...a3, ..._s3];
        return {$: "ok", args: [_s5, _s1]};
      }
    }
  }
  return _uwFail;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 5) {
    const argv = process.argv.slice(2, 5).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(parse_strict(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: is_global_key/1

export function is_global_key(a1) {
  if (true) {
    let _t0;
    if ((_t0 = pair_lookup([{$: "-", args: ["state", "string"]}, ...[{$: "-", args: ["name", "string"]}, ...[]]], a1)) !== _uwFail) {
      return true;
    } else {
      if (js_object_prototype_key(a1)) {
        return true;
      }
    }
  }
  return false;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(is_global_key(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: scan_leading_globals/4

export function scan_leading_globals(a1, a2) {
  if (a1.length === 0) {
    return [[], a2];
  }
  if (a1.length > 0) {
    if ((starts_with(a1[0], "--") && a1[0] !== "--")) {
      const [_s0, _s1] = split_flag_token(a1[0]);
      if (is_global_key(_s0)) {
        if (_s1 != null && _s1.$ === "some" && _s1.args.length === 1) {
          const _s2 = flags_set(a2, _s0, _s1.args[0]);
          return scan_leading_globals(a1.slice(1), _s2);
        } else {
          const _s3 = next_value(a1.slice(1));
          if (_s3 != null && _s3.$ === "some" && _s3.args.length === 1) {
            const _s4 = flags_set(a2, _s0, _s3.args[0]);
            if (a1.slice(1).length > 0) {
              return scan_leading_globals(a1.slice(1).slice(1), _s4);
            }
          } else {
            const _s5 = flags_set(a2, _s0, true);
            return scan_leading_globals(a1.slice(1), _s5);
          }
        }
      } else {
        return [[a1[0], ...a1.slice(1)], a2];
      }
    } else {
      return [[a1[0], ...a1.slice(1)], a2];
    }
  }
  throw new Error("no matching clause for scan_leading_globals/4");
}

// CLI entry point (multi-output: the tuple is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(scan_leading_globals(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: action_entry/3

export function action_entry(a1, a2) {
  if (true) {
    let _t0;
    if ((_t0 = pair_lookup(a1, a2)) !== _uwFail) {
      return _t0;
    } else {
      if (js_object_prototype_key(a2)) {
        return {$: "schema", args: [[], []]};
      }
    }
  }
  return _uwFail;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(action_entry(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: registry_entry/3

export function registry_entry(a1, a2) {
  if (true) {
    let _t0;
    if ((_t0 = pair_lookup(a1, a2)) !== _uwFail) {
      return _t0;
    } else {
      if (js_object_prototype_key(a2)) {
        return {$: "schema", args: [[], []]};
      }
    }
  }
  return _uwFail;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(registry_entry(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: schema_for/5

export function schema_for(a1, a2, a3) {
  if (true) {
    const _s0 = registry_entry(a3, a1);
    if (_s0 !== _uwFail) {
      if (_s0 != null && _s0.$ === "group" && _s0.args.length === 1) {
        if (a2 != null && a2.$ === "some" && a2.args.length === 1 && a2.args[0] !== "") {
          const _s1 = action_entry(_s0.args[0], a2.args[0]);
          if (_s1 !== _uwFail) {
            return [_s1, true];
          }
        }
      } else {
        return [_s0, false];
      }
    }
  }
  return _uwFail;
}

// CLI entry point (multi-output: the tuple is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 5) {
    const argv = process.argv.slice(2, 5).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(schema_for(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: parse_args_3/3

export function parse_args_3(a1, a2) {
  if (true) {
    const [_s0, _s1] = scan_leading_globals(a1, []);
    if (_s0.length > 0 && !(starts_with(_s0[0], "--"))) {
      let _s2;
      if (_s0.slice(1).length > 0) {
        _s2 = {$: "some", args: [_s0.slice(1)[0]]};
      } else {
        _s2 = "none";
      }
      let _t3;
      if ((_t3 = schema_for(_s0[0], _s2, a2)) !== _uwFail) {
        if (_t3[1] === true) {
          if (_s2 != null && _s2.$ === "some" && _s2.args.length === 1 && _s0.slice(1).length > 0) {
            const _s4 = parse_strict(_s0.slice(1).slice(1), _t3[0], [_s0[0], ...[_s2.args[0], ...[]]]);
            if (_s4 !== _uwFail) {
              if (_s4 != null && _s4.$ === "ok" && _s4.args.length === 2) {
                const _s5 = merge_flags(_s1, _s4.args[1]);
                return {$: "ok", args: [_s4.args[0], _s5]};
              } else {
                if (_s4 != null && _s4.$ === "err" && _s4.args.length === 1) {
                  return {$: "error", args: [_s4.args[0]]};
                }
              }
            }
          }
        } else {
          const _s6 = parse_strict(_s0.slice(1), _t3[0], [_s0[0], ...[]]);
          if (_s6 !== _uwFail) {
            if (_s6 != null && _s6.$ === "ok" && _s6.args.length === 2) {
              const _s7 = merge_flags(_s1, _s6.args[1]);
              return {$: "ok", args: [_s6.args[0], _s7]};
            } else {
              if (_s6 != null && _s6.$ === "err" && _s6.args.length === 1) {
                return {$: "error", args: [_s6.args[0]]};
              }
            }
          }
        }
      } else {
        const _s8 = lenient_result(a1);
        return _s8;
      }
    } else {
      const _s9 = lenient_result(a1);
      return _s9;
    }
  }
  return _uwFail;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 4) {
    const argv = process.argv.slice(2, 4).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(parse_args_3(...argv)));
}


// Generated by UnifyWeaver TypeScript Target - Structural Recursion (native clause lowering)
// Predicate: parse_args_2/2

export function parse_args_2(a1) {
  if (true) {
    const _s0 = parse_args_3(a1, [{$: "-", args: ["block", {$: "schema", args: [[{$: "-", args: ["include-key", "boolean"]}, ...[]], ["name", ...[]]]}]}, ...[{$: "-", args: ["unblock", {$: "schema", args: [[{$: "-", args: ["key", "string"]}, ...[]], ["[name]", ...[]]]}]}, ...[{$: "-", args: ["add", {$: "schema", args: [[{$: "-", args: ["profile", "string"]}, ...[{$: "-", args: ["until", "string"]}, ...[{$: "-", args: ["key", "string"]}, ...[{$: "-", args: ["key-file", "string"]}, ...[{$: "-", args: ["transport", "string"]}, ...[]]]]]], ["name", ...["[address]", ...[]]]]}]}, ...[{$: "-", args: ["daemon", {$: "schema", args: [[{$: "-", args: ["debug", "optional"]}, ...[{$: "-", args: ["port", "string"]}, ...[{$: "-", args: ["host", "string"]}, ...[{$: "-", args: ["hail-on", "string"]}, ...[{$: "-", args: ["hail-on-encrypted", "string"]}, ...[{$: "-", args: ["hail-on-tls", "string"]}, ...[{$: "-", args: ["tls-cert", "string"]}, ...[{$: "-", args: ["tls-key", "string"]}, ...[{$: "-", args: ["allow-origin", "string"]}, ...[{$: "-", args: ["chat", "boolean"]}, ...[{$: "-", args: ["route", "boolean"]}, ...[{$: "-", args: ["ui", "boolean"]}, ...[{$: "-", args: ["require-target-binding", "boolean"]}, ...[{$: "-", args: ["require-sealed", "boolean"]}, ...[]]]]]]]]]]]]]]], []]}]}, ...[{$: "-", args: ["commands", {$: "group", args: [[{$: "-", args: ["add", {$: "schema", args: [[], ["name", ...["...command", ...[]]]]}]}, ...[{$: "-", args: ["remove", {$: "schema", args: [[], ["name", ...[]]]}]}, ...[{$: "-", args: ["list", {$: "schema", args: [[], []]}]}, ...[]]]]]}]}, ...[{$: "-", args: ["route", {$: "group", args: [[{$: "-", args: ["discover", {$: "schema", args: [[{$: "-", args: ["dest", "string"]}, ...[{$: "-", args: ["dest-file", "string"]}, ...[{$: "-", args: ["control", "string"]}, ...[{$: "-", args: ["port", "string"]}, ...[]]]]], []]}]}, ...[{$: "-", args: ["status", {$: "schema", args: [[{$: "-", args: ["dest", "string"]}, ...[{$: "-", args: ["dest-file", "string"]}, ...[{$: "-", args: ["control", "string"]}, ...[{$: "-", args: ["port", "string"]}, ...[]]]]], []]}]}, ...[{$: "-", args: ["approve", {$: "schema", args: [[{$: "-", args: ["dest", "string"]}, ...[{$: "-", args: ["dest-file", "string"]}, ...[{$: "-", args: ["seal-key", "string"]}, ...[{$: "-", args: ["seal-key-file", "string"]}, ...[{$: "-", args: ["control", "string"]}, ...[{$: "-", args: ["port", "string"]}, ...[]]]]]]], []]}]}, ...[{$: "-", args: ["send", {$: "schema", args: [[{$: "-", args: ["dest", "string"]}, ...[{$: "-", args: ["dest-file", "string"]}, ...[{$: "-", args: ["public", "boolean"]}, ...[{$: "-", args: ["ttl", "string"]}, ...[{$: "-", args: ["budget", "string"]}, ...[{$: "-", args: ["control", "string"]}, ...[{$: "-", args: ["port", "string"]}, ...[]]]]]]]], ["...message", ...[]]]}]}, ...[]]]]]]}]}, ...[{$: "-", args: ["profiles", {$: "group", args: [[{$: "-", args: ["add", {$: "schema", args: [[{$: "-", args: ["allows", "string"]}, ...[{$: "-", args: ["description", "string"]}, ...[]]], ["name", ...[]]]}]}, ...[{$: "-", args: ["remove", {$: "schema", args: [[{$: "-", args: ["force", "boolean"]}, ...[{$: "-", args: ["reassign", "string"]}, ...[]]], ["name", ...[]]]}]}, ...[{$: "-", args: ["pin", {$: "schema", args: [[], ["name", ...[]]]}]}, ...[{$: "-", args: ["unpin", {$: "schema", args: [[], ["name", ...[]]]}]}, ...[{$: "-", args: ["list", {$: "schema", args: [[], []]}]}, ...[]]]]]]]}]}, ...[]]]]]]]]);
    if (_s0 !== _uwFail) {
      return _s0;
    }
  }
  return _uwFail;
}

// CLI entry point (the answer is printed as JSON)
// Each argument is JSON.parsed when it parses as JSON (so `[1,2]` is a list),
// and kept as a raw string otherwise.
if (process.argv.length >= 3) {
    const argv = process.argv.slice(2, 3).map(
        (s) => { try { return JSON.parse(s); } catch { return s; } });
    console.log(JSON.stringify(parse_args_2(...argv)));
}


package wam

import "fmt"

var _ = fmt.Sprintf

// Lowered predicates: direct Go methods on *WamState
// Generated by wam_go_lowered_emitter.pl
// (Atom intern table lives in atoms.go.)

// PredAlias_list2 — lowered from alias_list/2 (T4 all-clauses inline)
func (vm *WamState) PredAlias_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X9, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[108]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X9, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[108]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute alias_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["alias_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("alias_list/2", 2) { return true }; vm.warnUnresolved("call", "alias_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredAlias_lookup3 — lowered from alias_lookup/3
func (vm *WamState) PredAlias_lookup3() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_value X1, A3
    if !vm.Unify(vm.Regs[2], vm.Regs[100]) {
        return false
    }
    return true

}

// PredAlready_provided4 — lowered from already_provided/4
func (vm *WamState) PredAlready_provided4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable X6, A2
    vm.Regs[105] = vm.Regs[1]
    // get_variable Y4, A3
    vm.Regs[203] = vm.Regs[2]
    // get_variable Y5, A4
    vm.Regs[204] = vm.Regs[3]
    // put_structure -/2, A1
    if !vm.Step(&PutStructure{Functor: "-/2", Ai: 0}) {
        return false
    }
    // set_variable Y2
    if !vm.Step(&SetVariable{Xn: 201}) {
        return false
    }
    // set_variable Y3
    if !vm.Step(&SetVariable{Xn: 202}) {
        return false
    }
    // put_value X6, A2
    vm.Regs[1] = vm.getReg(105)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_value Y4, A4
    vm.Regs[3] = vm.getReg(203)
    // put_value Y5, A5
    vm.Regs[4] = vm.getReg(204)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute provides_sat/5 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["provides_sat/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provides_sat/5", 5) { return true }; vm.warnUnresolved("call", "provides_sat/5"); return false }()

}

// PredAlready_satisfied4 — lowered from already_satisfied/4 (T4 all-clauses inline)
func (vm *WamState) PredAlready_satisfied4() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X3, A1
        vm.Regs[102] = vm.Regs[0]
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // get_variable X5, A3
        vm.Regs[104] = vm.Regs[2]
        // get_variable Y2, A4
        vm.Regs[201] = vm.Regs[3]
        // put_value X4, A1
        vm.Regs[0] = vm.getReg(103)
        // put_value X5, A2
        vm.Regs[1] = vm.getReg(104)
        // put_variable Y1, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[2] = v
        }
        // call selected_ver/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["selected_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("selected_ver/3", 3) { return true }; vm.warnUnresolved("call", "selected_ver/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute satisfies/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["satisfies/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("satisfies/2", 2) { return true }; vm.warnUnresolved("call", "satisfies/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_variable X3, A3
        vm.Regs[102] = vm.Regs[2]
        // get_variable X4, A4
        vm.Regs[103] = vm.Regs[3]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X2, A2
        vm.Regs[1] = vm.getReg(101)
        // put_value X3, A3
        vm.Regs[2] = vm.getReg(102)
        // put_value X4, A4
        vm.Regs[3] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute already_provided/4 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["already_provided/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("already_provided/4", 4) { return true }; vm.warnUnresolved("call", "already_provided/4"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredAlt_reasons4 — lowered from alt_reasons/4
func (vm *WamState) PredAlt_reasons4() bool {
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_constant [], A2
    {
        _a := vm.deref(vm.Regs[1])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X2, A3
    vm.Regs[101] = vm.Regs[2]
    // get_constant [], A4
    {
        _a := vm.deref(vm.Regs[3])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    return true

}

// PredAudit_holds4 — lowered from audit_holds/4
func (vm *WamState) PredAudit_holds4() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_variable X2, A3
    vm.Regs[101] = vm.Regs[2]
    // get_value X2, A4
    if !vm.Unify(vm.Regs[3], vm.Regs[101]) {
        return false
    }
    return true

}

// PredBase_holds2 — lowered from base_holds/2
func (vm *WamState) PredBase_holds2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X4, A1
    vm.Regs[103] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // put_value X4, A1
    vm.Regs[0] = vm.getReg(103)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call base_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["base_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_list/2", 2) { return true }; vm.warnUnresolved("call", "base_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_constant [], A2
    vm.Regs[1] = wamAtom____0
    // put_variable Y2, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[2] = v
    }
    // call scan_base_holds/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["scan_base_holds/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("scan_base_holds/3", 3) { return true }; vm.warnUnresolved("call", "scan_base_holds/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value Y3, A2
    vm.Regs[1] = vm.getReg(202)
    // builtin_call sort/2 2
    if !vm.executeBuiltin("sort/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredBase_list2 — lowered from base_list/2 (T4 all-clauses inline)
func (vm *WamState) PredBase_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_value X4, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[103]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X4, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[103]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X4, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[103]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute base_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["base_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_list/2", 2) { return true }; vm.warnUnresolved("call", "base_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredBase_name2 — lowered from base_name/2
func (vm *WamState) PredBase_name2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_variable X2, A2
    vm.Regs[101] = vm.Regs[1]
    // put_value X1, A1
    vm.Regs[0] = vm.getReg(100)
    // put_value X2, A2
    vm.Regs[1] = vm.getReg(101)
    // put_variable X3, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 102), Idx: 102}
        vm.putReg(102, v)
        vm.Regs[2] = v
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute base_ver/3 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["base_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_ver/3", 3) { return true }; vm.warnUnresolved("call", "base_ver/3"); return false }()

}

// PredBase_reason3 — lowered from base_reason/3
func (vm *WamState) PredBase_reason3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X4, A1
    vm.Regs[103] = vm.Regs[0]
    // get_variable Y2, A2
    vm.Regs[201] = vm.Regs[1]
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // put_value X4, A1
    vm.Regs[0] = vm.getReg(103)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call base_holds/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["base_holds/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_holds/2", 2) { return true }; vm.warnUnresolved("call", "base_holds/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute hold_reason/3 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["hold_reason/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("hold_reason/3", 3) { return true }; vm.warnUnresolved("call", "hold_reason/3"); return false }()

}

// PredBase_ver3 — lowered from base_ver/3
func (vm *WamState) PredBase_ver3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable Y5, A2
    vm.Regs[204] = vm.Regs[1]
    // get_variable Y6, A3
    vm.Regs[205] = vm.Regs[2]
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_variable Y2, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[1] = v
    }
    // call base_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["base_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_list/2", 2) { return true }; vm.warnUnresolved("call", "base_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_variable Y3, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[1] = v
    }
    // call layers_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["layers_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("layers_list/2", 2) { return true }; vm.warnUnresolved("call", "layers_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value Y3, A2
    vm.Regs[1] = vm.getReg(202)
    // put_variable Y4, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
        vm.putReg(203, v)
        vm.Regs[2] = v
    }
    // builtin_call append/3 3
    if !vm.executeBuiltin("append/3", 3) {
        return false
    }
    // put_value Y4, A1
    vm.Regs[0] = vm.getReg(203)
    // put_value Y5, A2
    vm.Regs[1] = vm.getReg(204)
    // put_value Y6, A3
    vm.Regs[2] = vm.getReg(205)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute lookup_held/3 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["lookup_held/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("lookup_held/3", 3) { return true }; vm.warnUnresolved("call", "lookup_held/3"); return false }()

}

// PredBlocked_acc5 — lowered from blocked_acc/5
func (vm *WamState) PredBlocked_acc5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X3, A1
    vm.Regs[102] = vm.Regs[0]
    // get_structure req/2, A2
    if !vm.Step(&GetStructure{Functor: "req/2", Ai: 1}) {
        return false
    }
    // unify_variable Y2
    if !vm.Step(&UnifyVariable{Xn: 201}) {
        return false
    }
    // unify_variable X4
    if !vm.Step(&UnifyVariable{Xn: 103}) {
        return false
    }
    // get_variable Y1, A3
    vm.Regs[200] = vm.Regs[2]
    // get_variable X5, A4
    vm.Regs[104] = vm.Regs[3]
    // get_value X5, A5
    if !vm.Unify(vm.Regs[4], vm.Regs[104]) {
        return false
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // builtin_call atom/1 1
    if !vm.executeBuiltin("atom/1", 1) {
        return false
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // call seen_name/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["seen_name/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("seen_name/2", 2) { return true }; vm.warnUnresolved("call", "seen_name/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredBlocked_acc_list5 — lowered from blocked_acc_list/5 (T4 all-clauses inline)
func (vm *WamState) PredBlocked_acc_list5() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_variable X2, A3
        vm.Regs[101] = vm.Regs[2]
        // get_variable X3, A4
        vm.Regs[102] = vm.Regs[3]
        // get_value X3, A5
        if !vm.Unify(vm.Regs[4], vm.Regs[102]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y1, A1
        vm.Regs[200] = vm.Regs[0]
        // get_list A2
        if !vm.Step(&GetList{Ai: 1}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable Y2
        if !vm.Step(&UnifyVariable{Xn: 201}) {
            return false
        }
        // get_variable Y3, A3
        vm.Regs[202] = vm.Regs[2]
        // get_variable X7, A4
        vm.Regs[106] = vm.Regs[3]
        // get_variable Y5, A5
        vm.Regs[204] = vm.Regs[4]
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value X6, A2
        vm.Regs[1] = vm.getReg(105)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // put_value X7, A4
        vm.Regs[3] = vm.getReg(106)
        // put_variable Y4, A5
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
            vm.putReg(203, v)
            vm.Regs[4] = v
        }
        // call blocked_acc/5
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["blocked_acc/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("blocked_acc/5", 5) { return true }; vm.warnUnresolved("call", "blocked_acc/5"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // put_value Y4, A4
        vm.Regs[3] = vm.getReg(203)
        // put_value Y5, A5
        vm.Regs[4] = vm.getReg(204)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute blocked_acc_list/5 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["blocked_acc_list/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("blocked_acc_list/5", 5) { return true }; vm.warnUnresolved("call", "blocked_acc_list/5"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredBlocked_from4 — lowered from blocked_from/4
func (vm *WamState) PredBlocked_from4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_structure req/2, A2
    if !vm.Step(&GetStructure{Functor: "req/2", Ai: 1}) {
        return false
    }
    // unify_variable X6
    if !vm.Step(&UnifyVariable{Xn: 105}) {
        return false
    }
    // get_structure alternatives/1, X6
    if !vm.Step(&GetStructure{Functor: "alternatives/1", Ai: 105}) {
        return false
    }
    // unify_variable Y2
    if !vm.Step(&UnifyVariable{Xn: 201}) {
        return false
    }
    // unify_variable X7
    if !vm.Step(&UnifyVariable{Xn: 106}) {
        return false
    }
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // get_variable Y4, A4
    vm.Regs[203] = vm.Regs[3]
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_variable Y5, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
        vm.putReg(204, v)
        vm.Regs[3] = v
    }
    // call alt_reasons/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["alt_reasons/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("alt_reasons/4", 4) { return true }; vm.warnUnresolved("call", "alt_reasons/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y4, A1
    vm.Regs[0] = vm.getReg(203)
    // put_structure blocked/1, A2
    if !vm.Step(&PutStructure{Functor: "blocked/1", Ai: 1}) {
        return false
    }
    // set_variable X9
    if !vm.Step(&SetVariable{Xn: 108}) {
        return false
    }
    // put_structure alternatives/1, X9
    if !vm.Step(&PutStructure{Functor: "alternatives/1", Ai: 108}) {
        return false
    }
    // set_value Y5
    if !vm.Step(&SetValue{Xn: 204}) {
        return false
    }
    // builtin_call =/2 2
    if !vm.executeBuiltin("=/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredBuild_tree4 — lowered from build_tree/4
func (vm *WamState) PredBuild_tree4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // get_variable Y7, A3
    vm.Regs[206] = vm.Regs[2]
    // get_variable Y6, A4
    vm.Regs[205] = vm.Regs[3]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant 0, A2
        vm.Regs[1] = &Integer{Val: 0}
        // builtin_call =:=/2 2
        if !vm.executeBuiltin("=:=/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // put_value Y7, A1
        vm.Regs[0] = vm.getReg(206)
        // put_constant t, A2
        vm.Regs[1] = wamAtom_t_5
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        // put_value Y6, A1
        vm.Regs[0] = vm.getReg(205)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_variable Y2, A1
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[0] = v
        }
        // put_structure ///2, A2
        if !vm.Step(&PutStructure{Functor: "///2", Ai: 1}) {
            return false
        }
        // set_variable X13
        if !vm.Step(&SetVariable{Xn: 112}) {
            return false
        }
        // set_constant 2
        if !vm.Step(&SetConstant{C: &Integer{Val: 2}}) {
            return false
        }
        // put_structure -/2, X13
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 112}) {
            return false
        }
        // set_value Y1
        if !vm.Step(&SetValue{Xn: 200}) {
            return false
        }
        // set_constant 1
        if !vm.Step(&SetConstant{C: &Integer{Val: 1}}) {
            return false
        }
        // builtin_call is/2 2
        if !vm.executeBuiltin("is/2", 2) {
            return false
        }
        // put_variable Y4, A1
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
            vm.putReg(203, v)
            vm.Regs[0] = v
        }
        // put_structure -/2, A2
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 1}) {
            return false
        }
        // set_variable X15
        if !vm.Step(&SetVariable{Xn: 114}) {
            return false
        }
        // set_value Y2
        if !vm.Step(&SetValue{Xn: 201}) {
            return false
        }
        // put_structure -/2, X15
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 114}) {
            return false
        }
        // set_value Y1
        if !vm.Step(&SetValue{Xn: 200}) {
            return false
        }
        // set_constant 1
        if !vm.Step(&SetConstant{C: &Integer{Val: 1}}) {
            return false
        }
        // builtin_call is/2 2
        if !vm.executeBuiltin("is/2", 2) {
            return false
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_variable Y8, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 207), Idx: 207}
            vm.putReg(207, v)
            vm.Regs[2] = v
        }
        // put_structure [|]/2, A4
        if !vm.Step(&PutStructure{Functor: "[|]/2", Ai: 3}) {
            return false
        }
        // set_variable X17
        if !vm.Step(&SetVariable{Xn: 116}) {
            return false
        }
        // set_variable Y5
        if !vm.Step(&SetVariable{Xn: 204}) {
            return false
        }
        // put_structure -/2, X17
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 116}) {
            return false
        }
        // set_variable Y9
        if !vm.Step(&SetVariable{Xn: 208}) {
            return false
        }
        // set_variable Y10
        if !vm.Step(&SetVariable{Xn: 209}) {
            return false
        }
        // call build_tree/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["build_tree/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("build_tree/4", 4) { return true }; vm.warnUnresolved("call", "build_tree/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_value Y5, A2
        vm.Regs[1] = vm.getReg(204)
        // put_variable Y11, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 210), Idx: 210}
            vm.putReg(210, v)
            vm.Regs[2] = v
        }
        // put_value Y6, A4
        vm.Regs[3] = vm.getReg(205)
        // call build_tree/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["build_tree/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("build_tree/4", 4) { return true }; vm.warnUnresolved("call", "build_tree/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y7, A1
        vm.Regs[0] = vm.getReg(206)
        // put_structure t/4, A2
        if !vm.Step(&PutStructure{Functor: "t/4", Ai: 1}) {
            return false
        }
        // set_value Y8
        if !vm.Step(&SetValue{Xn: 207}) {
            return false
        }
        // set_value Y9
        if !vm.Step(&SetValue{Xn: 208}) {
            return false
        }
        // set_value Y10
        if !vm.Step(&SetValue{Xn: 209}) {
            return false
        }
        // set_value Y11
        if !vm.Step(&SetValue{Xn: 210}) {
            return false
        }
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredCandidates_high_first4 — lowered from candidates_high_first/4
func (vm *WamState) PredCandidates_high_first4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable Y2, A2
    vm.Regs[201] = vm.Regs[1]
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // get_variable Y5, A4
    vm.Regs[204] = vm.Regs[3]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // call excluded_name/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["excluded_name/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("excluded_name/2", 2) { return true }; vm.warnUnresolved("call", "excluded_name/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // builtin_call fail/0 0
        if !vm.executeBuiltin("fail/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        }
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_variable Y4, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
        vm.putReg(203, v)
        vm.Regs[3] = v
    }
    // call matching_versions_in/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["matching_versions_in/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("matching_versions_in/4", 4) { return true }; vm.warnUnresolved("call", "matching_versions_in/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y4, A1
    vm.Regs[0] = vm.getReg(203)
    // put_variable Y6, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 205), Idx: 205}
        vm.putReg(205, v)
        vm.Regs[1] = v
    }
    // call sort_versions_desc/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["sort_versions_desc/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("sort_versions_desc/2", 2) { return true }; vm.warnUnresolved("call", "sort_versions_desc/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y5, A1
    vm.Regs[0] = vm.getReg(204)
    // put_value Y6, A2
    vm.Regs[1] = vm.getReg(205)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredCanonicalize_name3 — lowered from canonicalize_name/3
func (vm *WamState) PredCanonicalize_name3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X4, A1
    vm.Regs[103] = vm.Regs[0]
    // get_variable Y2, A2
    vm.Regs[201] = vm.Regs[1]
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // put_value X4, A1
    vm.Regs[0] = vm.getReg(103)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call alias_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["alias_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("alias_list/2", 2) { return true }; vm.warnUnresolved("call", "alias_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute alias_lookup/3 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["alias_lookup/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("alias_lookup/3", 3) { return true }; vm.warnUnresolved("call", "alias_lookup/3"); return false }()

}

// PredClose_moving3 — lowered from close_moving/3
func (vm *WamState) PredClose_moving3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y4, A1
    vm.Regs[203] = vm.Regs[0]
    // get_variable Y6, A2
    vm.Regs[205] = vm.Regs[1]
    // get_variable Y8, A3
    vm.Regs[207] = vm.Regs[2]
    // put_value Y4, A1
    vm.Regs[0] = vm.getReg(203)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call base_holds/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["base_holds/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_holds/2", 2) { return true }; vm.warnUnresolved("call", "base_holds/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y4, A2
    vm.Regs[1] = vm.getReg(203)
    // put_value Y6, A3
    vm.Regs[2] = vm.getReg(205)
    // put_variable Y3, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[3] = v
    }
    // call first_broken/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["first_broken/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("first_broken/4", 4) { return true }; vm.warnUnresolved("call", "first_broken/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_constant none, A2
        vm.Regs[1] = wamAtom_none_6
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // put_value Y6, A1
        vm.Regs[0] = vm.getReg(205)
        // put_variable Y2, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[1] = v
        }
        // builtin_call sort/2 2
        if !vm.executeBuiltin("sort/2", 2) {
            return false
        }
        // put_value Y8, A1
        vm.Regs[0] = vm.getReg(207)
        // put_structure ok/1, A2
        if !vm.Step(&PutStructure{Functor: "ok/1", Ai: 1}) {
            return false
        }
        // set_value Y2
        if !vm.Step(&SetValue{Xn: 201}) {
            return false
        }
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_structure broken/3, A2
        if !vm.Step(&PutStructure{Functor: "broken/3", Ai: 1}) {
            return false
        }
        // set_variable Y5
        if !vm.Step(&SetVariable{Xn: 204}) {
            return false
        }
        // set_variable Y10
        if !vm.Step(&SetVariable{Xn: 209}) {
            return false
        }
        // set_variable Y9
        if !vm.Step(&SetVariable{Xn: 208}) {
            return false
        }
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
        {
            _trailMark := vm.TrailLen
            _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
            _condOk := func() bool {
            // put_value Y4, A1
            vm.Regs[0] = vm.getReg(203)
            // put_value Y5, A2
            vm.Regs[1] = vm.getReg(204)
            // put_value Y6, A3
            vm.Regs[2] = vm.getReg(205)
            // put_variable Y7, A4
            {
                v := &Unbound{Name: fmt.Sprintf("_R%d", 206), Idx: 206}
                vm.putReg(206, v)
                vm.Regs[3] = v
            }
            // call pick_repair/4
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["pick_repair/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("pick_repair/4", 4) { return true }; vm.warnUnresolved("call", "pick_repair/4"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
                return true
            }()
            if _condOk {
            // put_value Y4, A1
            vm.Regs[0] = vm.getReg(203)
            // put_structure [|]/2, A2
            if !vm.Step(&PutStructure{Functor: "[|]/2", Ai: 1}) {
                return false
            }
            // set_variable X13
            if !vm.Step(&SetVariable{Xn: 112}) {
                return false
            }
            // set_value Y6
            if !vm.Step(&SetValue{Xn: 205}) {
                return false
            }
            // put_structure -/2, X13
            if !vm.Step(&PutStructure{Functor: "-/2", Ai: 112}) {
                return false
            }
            // set_value Y5
            if !vm.Step(&SetValue{Xn: 204}) {
                return false
            }
            // set_value Y7
            if !vm.Step(&SetValue{Xn: 206}) {
                return false
            }
            // put_value Y8, A3
            vm.Regs[2] = vm.getReg(207)
            // call close_moving/3
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["close_moving/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("close_moving/3", 3) { return true }; vm.warnUnresolved("call", "close_moving/3"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
            } else {
                vm.Regs = _savedRegs
                vm.unwindTrailTo(_trailMark)
            // put_variable Y7, A4
            {
                v := &Unbound{Name: fmt.Sprintf("_R%d", 206), Idx: 206}
                vm.putReg(206, v)
                vm.Regs[3] = v
            }
            // put_value Y8, A1
            vm.Regs[0] = vm.getReg(207)
            // put_structure blocked/3, A2
            if !vm.Step(&PutStructure{Functor: "blocked/3", Ai: 1}) {
                return false
            }
            // set_value Y5
            if !vm.Step(&SetValue{Xn: 204}) {
                return false
            }
            // set_variable X13
            if !vm.Step(&SetVariable{Xn: 112}) {
                return false
            }
            // set_variable X14
            if !vm.Step(&SetVariable{Xn: 113}) {
                return false
            }
            // put_structure needs/1, X13
            if !vm.Step(&PutStructure{Functor: "needs/1", Ai: 112}) {
                return false
            }
            // set_value Y9
            if !vm.Step(&SetValue{Xn: 208}) {
                return false
            }
            // put_structure base_has/1, X14
            if !vm.Step(&PutStructure{Functor: "base_has/1", Ai: 113}) {
                return false
            }
            // set_value Y10
            if !vm.Step(&SetValue{Xn: 209}) {
                return false
            }
            // builtin_call =/2 2
            if !vm.executeBuiltin("=/2", 2) {
                return false
            }
            }
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredCmp_ver3 — lowered from cmp_ver/3 (T4 all-clauses inline)
func (vm *WamState) PredCmp_ver3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_constant <, A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom___7
            } else if !valueEquals(vm.deref(_a), wamAtom___7) {
                return false
            }
        }
        // get_variable X1, A2
        vm.Regs[100] = vm.Regs[1]
        // get_variable X2, A3
        vm.Regs[101] = vm.Regs[2]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X2, A2
        vm.Regs[1] = vm.getReg(101)
        // call version_lt/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["version_lt/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("version_lt/2", 2) { return true }; vm.warnUnresolved("call", "version_lt/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_constant >, A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom___8
            } else if !valueEquals(vm.deref(_a), wamAtom___8) {
                return false
            }
        }
        // get_variable X1, A2
        vm.Regs[100] = vm.Regs[1]
        // get_variable X2, A3
        vm.Regs[101] = vm.Regs[2]
        // put_value X2, A1
        vm.Regs[0] = vm.getReg(101)
        // put_value X1, A2
        vm.Regs[1] = vm.getReg(100)
        // call version_lt/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["version_lt/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("version_lt/2", 2) { return true }; vm.warnUnresolved("call", "version_lt/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_constant =, A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom___9
            } else if !valueEquals(vm.deref(_a), wamAtom___9) {
                return false
            }
        }
        // get_variable X1, A2
        vm.Regs[100] = vm.Regs[1]
        // get_variable X2, A3
        vm.Regs[101] = vm.Regs[2]
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredCollect_deps4 — lowered from collect_deps/4
func (vm *WamState) PredCollect_deps4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y3, A1
    vm.Regs[202] = vm.Regs[0]
    // get_variable Y5, A2
    vm.Regs[204] = vm.Regs[1]
    // get_variable Y6, A3
    vm.Regs[205] = vm.Regs[2]
    // get_variable Y7, A4
    vm.Regs[206] = vm.Regs[3]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_variable Y1, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[1] = v
        }
        // call dep_index/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["dep_index/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("dep_index/2", 2) { return true }; vm.warnUnresolved("call", "dep_index/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
        {
            _trailMark := vm.TrailLen
            _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
            _condOk := func() bool {
            // put_value Y1, A1
            vm.Regs[0] = vm.getReg(200)
            // put_structure -/2, A2
            if !vm.Step(&PutStructure{Functor: "-/2", Ai: 1}) {
                return false
            }
            // set_value Y5
            if !vm.Step(&SetValue{Xn: 204}) {
                return false
            }
            // set_value Y6
            if !vm.Step(&SetValue{Xn: 205}) {
                return false
            }
            // put_variable Y2, A3
            {
                v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
                vm.putReg(201, v)
                vm.Regs[2] = v
            }
            // call tree_lookup/3
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["tree_lookup/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("tree_lookup/3", 3) { return true }; vm.warnUnresolved("call", "tree_lookup/3"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
                return true
            }()
            if _condOk {
            // put_value Y7, A1
            vm.Regs[0] = vm.getReg(206)
            // put_value Y2, A2
            vm.Regs[1] = vm.getReg(201)
            // builtin_call =/2 2
            if !vm.executeBuiltin("=/2", 2) {
                return false
            }
            } else {
                vm.Regs = _savedRegs
                vm.unwindTrailTo(_trailMark)
            // put_variable Y2, A3
            {
                v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
                vm.putReg(201, v)
                vm.Regs[2] = v
            }
            // put_value Y7, A1
            vm.Regs[0] = vm.getReg(206)
            // put_constant [], A2
            vm.Regs[1] = wamAtom____0
            // builtin_call =/2 2
            if !vm.executeBuiltin("=/2", 2) {
                return false
            }
            }
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_variable Y1, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[1] = v
        }
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_variable Y4, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
            vm.putReg(203, v)
            vm.Regs[1] = v
        }
        // call depends_list/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["depends_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("depends_list/2", 2) { return true }; vm.warnUnresolved("call", "depends_list/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_value Y5, A2
        vm.Regs[1] = vm.getReg(204)
        // put_value Y6, A3
        vm.Regs[2] = vm.getReg(205)
        // put_value Y7, A4
        vm.Regs[3] = vm.getReg(206)
        // call matching_deps/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["matching_deps/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("matching_deps/4", 4) { return true }; vm.warnUnresolved("call", "matching_deps/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredConflicts_in4 — lowered from conflicts_in/4
func (vm *WamState) PredConflicts_in4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X5, A1
    vm.Regs[104] = vm.Regs[0]
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // get_variable Y2, A3
    vm.Regs[201] = vm.Regs[2]
    // get_variable Y3, A4
    vm.Regs[202] = vm.Regs[3]
    // put_value X5, A1
    vm.Regs[0] = vm.getReg(104)
    // put_variable Y4, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
        vm.putReg(203, v)
        vm.Regs[1] = v
    }
    // call conflicts_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["conflicts_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("conflicts_list/2", 2) { return true }; vm.warnUnresolved("call", "conflicts_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_structure conflicts/3, A1
    if !vm.Step(&PutStructure{Functor: "conflicts/3", Ai: 0}) {
        return false
    }
    // set_value Y1
    if !vm.Step(&SetValue{Xn: 200}) {
        return false
    }
    // set_value Y2
    if !vm.Step(&SetValue{Xn: 201}) {
        return false
    }
    // set_value Y3
    if !vm.Step(&SetValue{Xn: 202}) {
        return false
    }
    // put_value Y4, A2
    vm.Regs[1] = vm.getReg(203)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredConflicts_list2 — lowered from conflicts_list/2 (T4 all-clauses inline)
func (vm *WamState) PredConflicts_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_value X3, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[102]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X3, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[102]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X3, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[102]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute conflicts_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["conflicts_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("conflicts_list/2", 2) { return true }; vm.warnUnresolved("call", "conflicts_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredDep_breaks5 — lowered from dep_breaks/5
func (vm *WamState) PredDep_breaks5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_list A1
    if !vm.Step(&GetList{Ai: 0}) {
        return false
    }
    // unify_variable X11
    if !vm.Step(&UnifyVariable{Xn: 110}) {
        return false
    }
    // get_structure depends/4, X11
    if !vm.Step(&GetStructure{Functor: "depends/4", Ai: 110}) {
        return false
    }
    // unify_variable Y1
    if !vm.Step(&UnifyVariable{Xn: 200}) {
        return false
    }
    // unify_variable Y2
    if !vm.Step(&UnifyVariable{Xn: 201}) {
        return false
    }
    // unify_variable Y3
    if !vm.Step(&UnifyVariable{Xn: 202}) {
        return false
    }
    // unify_variable Y4
    if !vm.Step(&UnifyVariable{Xn: 203}) {
        return false
    }
    // unify_variable Y6
    if !vm.Step(&UnifyVariable{Xn: 205}) {
        return false
    }
    // get_variable Y7, A2
    vm.Regs[206] = vm.Regs[1]
    // get_variable Y8, A3
    vm.Regs[207] = vm.Regs[2]
    // get_variable Y9, A4
    vm.Regs[208] = vm.Regs[3]
    // get_variable Y10, A5
    vm.Regs[209] = vm.Regs[4]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y7, A2
        vm.Regs[1] = vm.getReg(206)
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y8, A2
        vm.Regs[1] = vm.getReg(207)
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
        // put_value Y9, A1
        vm.Regs[0] = vm.getReg(208)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_value Y4, A3
        vm.Regs[2] = vm.getReg(203)
        // put_variable Y5, A4
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
            vm.putReg(204, v)
            vm.Regs[3] = v
        }
        // call dep_breaks_need/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["dep_breaks_need/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("dep_breaks_need/4", 4) { return true }; vm.warnUnresolved("call", "dep_breaks_need/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // put_value Y10, A1
        vm.Regs[0] = vm.getReg(209)
        // put_value Y5, A2
        vm.Regs[1] = vm.getReg(204)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_variable Y5, A4
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
            vm.putReg(204, v)
            vm.Regs[3] = v
        }
        // put_value Y6, A1
        vm.Regs[0] = vm.getReg(205)
        // put_value Y7, A2
        vm.Regs[1] = vm.getReg(206)
        // put_value Y8, A3
        vm.Regs[2] = vm.getReg(207)
        // put_value Y9, A4
        vm.Regs[3] = vm.getReg(208)
        // put_value Y10, A5
        vm.Regs[4] = vm.getReg(209)
        // call dep_breaks/5
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["dep_breaks/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("dep_breaks/5", 5) { return true }; vm.warnUnresolved("call", "dep_breaks/5"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredDep_breaks_moving5 — lowered from dep_breaks_moving/5
func (vm *WamState) PredDep_breaks_moving5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X6, A1
    vm.Regs[105] = vm.Regs[0]
    // get_variable Y2, A2
    vm.Regs[201] = vm.Regs[1]
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // get_variable Y4, A4
    vm.Regs[203] = vm.Regs[3]
    // get_variable Y5, A5
    vm.Regs[204] = vm.Regs[4]
    // put_value X6, A1
    vm.Regs[0] = vm.getReg(105)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call depends_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["depends_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("depends_list/2", 2) { return true }; vm.warnUnresolved("call", "depends_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_value Y4, A4
    vm.Regs[3] = vm.getReg(203)
    // put_value Y5, A5
    vm.Regs[4] = vm.getReg(204)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute dep_breaks/5 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["dep_breaks/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("dep_breaks/5", 5) { return true }; vm.warnUnresolved("call", "dep_breaks/5"); return false }()

}

// PredDep_index2 — lowered from dep_index/2
func (vm *WamState) PredDep_index2() bool {
    // get_structure icat/3, A1
    if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
        return false
    }
    // unify_variable X1
    if !vm.Step(&UnifyVariable{Xn: 100}) {
        return false
    }
    // unify_variable X2
    if !vm.Step(&UnifyVariable{Xn: 101}) {
        return false
    }
    // unify_variable X3
    if !vm.Step(&UnifyVariable{Xn: 102}) {
        return false
    }
    // get_value X2, A2
    if !vm.Unify(vm.Regs[1], vm.Regs[101]) {
        return false
    }
    return true

}

// PredDep_mentions2 — lowered from dep_mentions/2 (T4 all-clauses inline)
func (vm *WamState) PredDep_mentions2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure alternatives/1, A1
        if !vm.Step(&GetStructure{Functor: "alternatives/1", Ai: 0}) {
            return false
        }
        // unify_variable Y3
        if !vm.Step(&UnifyVariable{Xn: 202}) {
            return false
        }
        // get_variable Y1, A2
        vm.Regs[200] = vm.Regs[1]
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // put_structure dep/2, A1
        if !vm.Step(&PutStructure{Functor: "dep/2", Ai: 0}) {
            return false
        }
        // set_value Y1
        if !vm.Step(&SetValue{Xn: 200}) {
            return false
        }
        // set_variable Y2
        if !vm.Step(&SetVariable{Xn: 201}) {
            return false
        }
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X2, A2
        vm.Regs[1] = vm.getReg(101)
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredDep_targets5 — lowered from dep_targets/5
func (vm *WamState) PredDep_targets5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X6, A1
    vm.Regs[105] = vm.Regs[0]
    // get_variable X7, A2
    vm.Regs[106] = vm.Regs[1]
    // get_variable X8, A3
    vm.Regs[107] = vm.Regs[2]
    // get_variable Y2, A4
    vm.Regs[201] = vm.Regs[3]
    // get_variable Y4, A5
    vm.Regs[203] = vm.Regs[4]
    // put_value X6, A1
    vm.Regs[0] = vm.getReg(105)
    // put_value X7, A2
    vm.Regs[1] = vm.getReg(106)
    // put_value X8, A3
    vm.Regs[2] = vm.getReg(107)
    // put_variable Y3, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[3] = v
    }
    // put_variable Y5, A5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
        vm.putReg(204, v)
        vm.Regs[4] = v
    }
    // call depends_in/5
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["depends_in/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("depends_in/5", 5) { return true }; vm.warnUnresolved("call", "depends_in/5"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_structure alternatives/1, A2
        if !vm.Step(&PutStructure{Functor: "alternatives/1", Ai: 1}) {
            return false
        }
        // set_variable Y1
        if !vm.Step(&SetVariable{Xn: 200}) {
            return false
        }
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // put_structure dep/2, A1
        if !vm.Step(&PutStructure{Functor: "dep/2", Ai: 0}) {
            return false
        }
        // set_value Y2
        if !vm.Step(&SetValue{Xn: 201}) {
            return false
        }
        // set_value Y4
        if !vm.Step(&SetValue{Xn: 203}) {
            return false
        }
        // put_value Y1, A2
        vm.Regs[1] = vm.getReg(200)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_value Y5, A2
        vm.Regs[1] = vm.getReg(204)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredDep_to_req3 — lowered from dep_to_req/3 (T4 all-clauses inline)
func (vm *WamState) PredDep_to_req3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure alternatives/1, A1
        if !vm.Step(&GetStructure{Functor: "alternatives/1", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_structure req/2, A3
        if !vm.Step(&GetStructure{Functor: "req/2", Ai: 2}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_structure alternatives/1, X3
        if !vm.Step(&GetStructure{Functor: "alternatives/1", Ai: 102}) {
            return false
        }
        // unify_value X1
        if !vm.Step(&UnifyValue{Xn: 100}) {
            return false
        }
        // unify_constant any
        if !vm.Step(&UnifyConstant{C: wamAtom_any_10}) {
            return false
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_structure req/2, A3
        if !vm.Step(&GetStructure{Functor: "req/2", Ai: 2}) {
            return false
        }
        // unify_value X1
        if !vm.Step(&UnifyValue{Xn: 100}) {
            return false
        }
        // unify_value X2
        if !vm.Step(&UnifyValue{Xn: 101}) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredDependents3 — lowered from dependents/3
func (vm *WamState) PredDependents3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable X6, A2
    vm.Regs[105] = vm.Regs[1]
    // get_variable Y5, A3
    vm.Regs[204] = vm.Regs[2]
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value X6, A2
    vm.Regs[1] = vm.getReg(105)
    // put_variable Y3, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[2] = v
    }
    // call canonicalize_name/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["canonicalize_name/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("canonicalize_name/3", 3) { return true }; vm.warnUnresolved("call", "canonicalize_name/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_variable Y2, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[1] = v
    }
    // call depends_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["depends_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("depends_list/2", 2) { return true }; vm.warnUnresolved("call", "depends_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value Y3, A2
    vm.Regs[1] = vm.getReg(202)
    // put_constant [], A3
    vm.Regs[2] = wamAtom____0
    // put_variable Y4, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
        vm.putReg(203, v)
        vm.Regs[3] = v
    }
    // call direct_on/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["direct_on/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("direct_on/4", 4) { return true }; vm.warnUnresolved("call", "direct_on/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y4, A1
    vm.Regs[0] = vm.getReg(203)
    // put_value Y5, A2
    vm.Regs[1] = vm.getReg(204)
    // builtin_call sort/2 2
    if !vm.executeBuiltin("sort/2", 2) {
        return false
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredDependents_installed3 — lowered from dependents_installed/3
func (vm *WamState) PredDependents_installed3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y2, A1
    vm.Regs[201] = vm.Regs[0]
    // get_variable X5, A2
    vm.Regs[104] = vm.Regs[1]
    // get_variable Y4, A3
    vm.Regs[203] = vm.Regs[2]
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value X5, A2
    vm.Regs[1] = vm.getReg(104)
    // put_variable Y1, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[2] = v
    }
    // call dependents/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["dependents/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("dependents/3", 3) { return true }; vm.warnUnresolved("call", "dependents/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_constant [], A3
    vm.Regs[2] = wamAtom____0
    // put_variable Y3, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[3] = v
    }
    // call keep_installed_or_base/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["keep_installed_or_base/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("keep_installed_or_base/4", 4) { return true }; vm.warnUnresolved("call", "keep_installed_or_base/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y3, A1
    vm.Regs[0] = vm.getReg(202)
    // put_value Y4, A2
    vm.Regs[1] = vm.getReg(203)
    // builtin_call sort/2 2
    if !vm.executeBuiltin("sort/2", 2) {
        return false
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredDepends_in5 — lowered from depends_in/5
func (vm *WamState) PredDepends_in5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X6, A1
    vm.Regs[105] = vm.Regs[0]
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // get_variable Y2, A3
    vm.Regs[201] = vm.Regs[2]
    // get_variable Y3, A4
    vm.Regs[202] = vm.Regs[3]
    // get_variable Y4, A5
    vm.Regs[203] = vm.Regs[4]
    // put_value X6, A1
    vm.Regs[0] = vm.getReg(105)
    // put_variable Y5, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
        vm.putReg(204, v)
        vm.Regs[1] = v
    }
    // call depends_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["depends_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("depends_list/2", 2) { return true }; vm.warnUnresolved("call", "depends_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_structure depends/4, A1
    if !vm.Step(&PutStructure{Functor: "depends/4", Ai: 0}) {
        return false
    }
    // set_value Y1
    if !vm.Step(&SetValue{Xn: 200}) {
        return false
    }
    // set_value Y2
    if !vm.Step(&SetValue{Xn: 201}) {
        return false
    }
    // set_value Y3
    if !vm.Step(&SetValue{Xn: 202}) {
        return false
    }
    // set_value Y4
    if !vm.Step(&SetValue{Xn: 203}) {
        return false
    }
    // put_value Y5, A2
    vm.Regs[1] = vm.getReg(204)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredDepends_list2 — lowered from depends_list/2 (T4 all-clauses inline)
func (vm *WamState) PredDepends_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_value X2, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[101]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X2, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[101]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X2, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[101]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute depends_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["depends_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("depends_list/2", 2) { return true }; vm.warnUnresolved("call", "depends_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredDirect_on4 — lowered from direct_on/4
func (vm *WamState) PredDirect_on4() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_variable X2, A3
    vm.Regs[101] = vm.Regs[2]
    // get_value X2, A4
    if !vm.Unify(vm.Regs[3], vm.Regs[101]) {
        return false
    }
    return true

}

// PredExclude_name3 — lowered from exclude_name/3 (T4 all-clauses inline)
func (vm *WamState) PredExclude_name3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_constant [], A3
        {
            _a := vm.deref(vm.Regs[2])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y1, A1
        vm.Regs[200] = vm.Regs[0]
        // get_list A2
        if !vm.Step(&GetList{Ai: 1}) {
            return false
        }
        // unify_value Y1
        if !vm.Step(&UnifyValue{Xn: 200}) {
            return false
        }
        // unify_variable Y2
        if !vm.Step(&UnifyVariable{Xn: 201}) {
            return false
        }
        // get_variable Y3, A3
        vm.Regs[202] = vm.Regs[2]
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute exclude_name/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["exclude_name/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("exclude_name/3", 3) { return true }; vm.warnUnresolved("call", "exclude_name/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_list A2
        if !vm.Step(&GetList{Ai: 1}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_list A3
        if !vm.Step(&GetList{Ai: 2}) {
            return false
        }
        // unify_value X2
        if !vm.Step(&UnifyValue{Xn: 101}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X3, A2
        vm.Regs[1] = vm.getReg(102)
        // put_value X4, A3
        vm.Regs[2] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute exclude_name/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["exclude_name/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("exclude_name/3", 3) { return true }; vm.warnUnresolved("call", "exclude_name/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredExcluded_list2 — lowered from excluded_list/2 (T4 all-clauses inline)
func (vm *WamState) PredExcluded_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X8, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[107]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X8, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[107]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute excluded_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["excluded_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("excluded_list/2", 2) { return true }; vm.warnUnresolved("call", "excluded_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredExcluded_name2 — lowered from excluded_name/2
func (vm *WamState) PredExcluded_name2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X3, A1
    vm.Regs[102] = vm.Regs[0]
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // put_value X3, A1
    vm.Regs[0] = vm.getReg(102)
    // put_variable Y2, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[1] = v
    }
    // call excluded_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["excluded_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("excluded_list/2", 2) { return true }; vm.warnUnresolved("call", "excluded_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredExplain_blocked3 — lowered from explain_blocked/3
func (vm *WamState) PredExplain_blocked3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable X4, A2
    vm.Regs[103] = vm.Regs[1]
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value X4, A2
    vm.Regs[1] = vm.getReg(103)
    // put_variable Y2, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[2] = v
    }
    // call request_to_req/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["request_to_req/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("request_to_req/3", 3) { return true }; vm.warnUnresolved("call", "request_to_req/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_constant [], A3
    vm.Regs[2] = wamAtom____0
    // put_value Y3, A4
    vm.Regs[3] = vm.getReg(202)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute blocked_from/4 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["blocked_from/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("blocked_from/4", 4) { return true }; vm.warnUnresolved("call", "blocked_from/4"); return false }()

}

// PredExplain_blocked_list3 — lowered from explain_blocked_list/3
func (vm *WamState) PredExplain_blocked_list3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable X5, A2
    vm.Regs[104] = vm.Regs[1]
    // get_variable Y4, A3
    vm.Regs[203] = vm.Regs[2]
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value X5, A2
    vm.Regs[1] = vm.getReg(104)
    // put_variable Y2, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[2] = v
    }
    // call request_to_req/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["request_to_req/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("request_to_req/3", 3) { return true }; vm.warnUnresolved("call", "request_to_req/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_constant [], A3
    vm.Regs[2] = wamAtom____0
    // put_constant [], A4
    vm.Regs[3] = wamAtom____0
    // put_variable Y3, A5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[4] = v
    }
    // call blocked_acc/5
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["blocked_acc/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("blocked_acc/5", 5) { return true }; vm.warnUnresolved("call", "blocked_acc/5"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y3, A1
    vm.Regs[0] = vm.getReg(202)
    // put_value Y4, A2
    vm.Regs[1] = vm.getReg(203)
    // builtin_call sort/2 2
    if !vm.executeBuiltin("sort/2", 2) {
        return false
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredFilter_satisfies3 — lowered from filter_satisfies/3
func (vm *WamState) PredFilter_satisfies3() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_constant [], A3
    {
        _a := vm.deref(vm.Regs[2])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    return true

}

// PredFirst_alt_already4 — lowered from first_alt_already/4 (T4 all-clauses inline)
func (vm *WamState) PredFirst_alt_already4() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X5, A1
        vm.Regs[104] = vm.Regs[0]
        // get_variable Y1, A2
        vm.Regs[200] = vm.Regs[1]
        // get_variable Y2, A3
        vm.Regs[201] = vm.Regs[2]
        // get_variable X6, A4
        vm.Regs[105] = vm.Regs[3]
        // put_structure dep/2, A1
        if !vm.Step(&PutStructure{Functor: "dep/2", Ai: 0}) {
            return false
        }
        // set_variable Y3
        if !vm.Step(&SetVariable{Xn: 202}) {
            return false
        }
        // set_variable Y4
        if !vm.Step(&SetVariable{Xn: 203}) {
            return false
        }
        // put_value X6, A2
        vm.Regs[1] = vm.getReg(105)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // put_value Y4, A4
        vm.Regs[3] = vm.getReg(203)
        // call already_satisfied/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["already_satisfied/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("already_satisfied/4", 4) { return true }; vm.warnUnresolved("call", "already_satisfied/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_constant layered, A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_layered_11
            } else if !valueEquals(vm.deref(_a), wamAtom_layered_11) {
                return false
            }
        }
        // get_variable Y1, A2
        vm.Regs[200] = vm.Regs[1]
        // get_variable X4, A3
        vm.Regs[103] = vm.Regs[2]
        // get_variable X5, A4
        vm.Regs[104] = vm.Regs[3]
        // put_structure dep/2, A1
        if !vm.Step(&PutStructure{Functor: "dep/2", Ai: 0}) {
            return false
        }
        // set_variable Y2
        if !vm.Step(&SetVariable{Xn: 201}) {
            return false
        }
        // set_variable Y3
        if !vm.Step(&SetVariable{Xn: 202}) {
            return false
        }
        // put_value X5, A2
        vm.Regs[1] = vm.getReg(104)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // call layer_satisfies/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["layer_satisfies/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("layer_satisfies/3", 3) { return true }; vm.warnUnresolved("call", "layer_satisfies/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredFirst_broken4 — lowered from first_broken/4
func (vm *WamState) PredFirst_broken4() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_variable X2, A3
    vm.Regs[101] = vm.Regs[2]
    // get_constant none, A4
    {
        _a := vm.deref(vm.Regs[3])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom_none_6
        } else if !valueEquals(vm.deref(_a), wamAtom_none_6) {
            return false
        }
    }
    return true

}

// PredFollow_dep_name5 — lowered from follow_dep_name/5
func (vm *WamState) PredFollow_dep_name5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X5, A1
    vm.Regs[104] = vm.Regs[0]
    // get_variable X6, A2
    vm.Regs[105] = vm.Regs[1]
    // get_variable X7, A3
    vm.Regs[106] = vm.Regs[2]
    // get_variable Y3, A4
    vm.Regs[202] = vm.Regs[3]
    // get_variable Y4, A5
    vm.Regs[203] = vm.Regs[4]
    // put_value X5, A1
    vm.Regs[0] = vm.getReg(104)
    // put_value X6, A2
    vm.Regs[1] = vm.getReg(105)
    // put_value X7, A3
    vm.Regs[2] = vm.getReg(106)
    // put_variable Y1, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[3] = v
    }
    // put_variable Y2, A5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[4] = v
    }
    // call depends_in/5
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["depends_in/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("depends_in/5", 5) { return true }; vm.warnUnresolved("call", "depends_in/5"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_value Y4, A4
    vm.Regs[3] = vm.getReg(203)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute follow_raw_dep/4 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["follow_raw_dep/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("follow_raw_dep/4", 4) { return true }; vm.warnUnresolved("call", "follow_raw_dep/4"); return false }()

}

// PredFollow_raw_dep4 — lowered from follow_raw_dep/4 (T4 all-clauses inline)
func (vm *WamState) PredFollow_raw_dep4() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure alternatives/1, A1
        if !vm.Step(&GetStructure{Functor: "alternatives/1", Ai: 0}) {
            return false
        }
        // unify_variable Y2
        if !vm.Step(&UnifyVariable{Xn: 201}) {
            return false
        }
        // get_variable X7, A2
        vm.Regs[106] = vm.Regs[1]
        // get_variable Y4, A3
        vm.Regs[203] = vm.Regs[2]
        // get_variable Y5, A4
        vm.Regs[204] = vm.Regs[3]
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // put_structure dep/2, A1
        if !vm.Step(&PutStructure{Functor: "dep/2", Ai: 0}) {
            return false
        }
        // set_variable Y6
        if !vm.Step(&SetVariable{Xn: 205}) {
            return false
        }
        // set_variable Y1
        if !vm.Step(&SetVariable{Xn: 200}) {
            return false
        }
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // put_structure -/2, A1
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 0}) {
            return false
        }
        // set_value Y6
        if !vm.Step(&SetValue{Xn: 205}) {
            return false
        }
        // set_variable Y3
        if !vm.Step(&SetVariable{Xn: 202}) {
            return false
        }
        // put_value Y4, A2
        vm.Regs[1] = vm.getReg(203)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // put_value Y5, A1
        vm.Regs[0] = vm.getReg(204)
        // put_value Y6, A2
        vm.Regs[1] = vm.getReg(205)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y1, A1
        vm.Regs[200] = vm.Regs[0]
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // get_variable Y3, A3
        vm.Regs[202] = vm.Regs[2]
        // get_value Y1, A4
        if !vm.Unify(vm.Regs[3], vm.Regs[200]) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // builtin_call atom/1 1
        if !vm.executeBuiltin("atom/1", 1) {
            return false
        }
        // put_structure -/2, A1
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 0}) {
            return false
        }
        // set_value Y1
        if !vm.Step(&SetValue{Xn: 200}) {
            return false
        }
        // set_variable Y2
        if !vm.Step(&SetVariable{Xn: 201}) {
            return false
        }
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredFreeze_audit2 — lowered from freeze_audit/2
func (vm *WamState) PredFreeze_audit2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y2, A1
    vm.Regs[201] = vm.Regs[0]
    // get_variable Y4, A2
    vm.Regs[203] = vm.Regs[1]
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call base_holds/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["base_holds/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_holds/2", 2) { return true }; vm.warnUnresolved("call", "base_holds/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_constant [], A3
    vm.Regs[2] = wamAtom____0
    // put_variable Y3, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[3] = v
    }
    // call audit_holds/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["audit_holds/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("audit_holds/4", 4) { return true }; vm.warnUnresolved("call", "audit_holds/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y3, A1
    vm.Regs[0] = vm.getReg(202)
    // put_value Y4, A2
    vm.Regs[1] = vm.getReg(203)
    // builtin_call sort/2 2
    if !vm.executeBuiltin("sort/2", 2) {
        return false
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredGroup_keyed2 — lowered from group_keyed/2 (T4 all-clauses inline)
func (vm *WamState) PredGroup_keyed2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_constant [], A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_list A1
        if !vm.Step(&GetList{Ai: 0}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_structure -/2, X3
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // get_structure -/2, X4
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // get_list A2
        if !vm.Step(&GetList{Ai: 1}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_structure -/2, X9
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 108}) {
            return false
        }
        // unify_value X5
        if !vm.Step(&UnifyValue{Xn: 104}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_structure [|]/2, X10
        if !vm.Step(&GetStructure{Functor: "[|]/2", Ai: 109}) {
            return false
        }
        // unify_value X7
        if !vm.Step(&UnifyValue{Xn: 106}) {
            return false
        }
        // unify_variable X11
        if !vm.Step(&UnifyVariable{Xn: 110}) {
            return false
        }
        // unify_variable Y2
        if !vm.Step(&UnifyVariable{Xn: 201}) {
            return false
        }
        // put_value X8, A1
        vm.Regs[0] = vm.getReg(107)
        // put_value X5, A2
        vm.Regs[1] = vm.getReg(104)
        // put_value X11, A3
        vm.Regs[2] = vm.getReg(110)
        // put_variable Y1, A4
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[3] = v
        }
        // call same_key/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["same_key/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("same_key/4", 4) { return true }; vm.warnUnresolved("call", "same_key/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute group_keyed/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["group_keyed/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("group_keyed/2", 2) { return true }; vm.warnUnresolved("call", "group_keyed/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredHold_reason3 — lowered from hold_reason/3
func (vm *WamState) PredHold_reason3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_list A1
    if !vm.Step(&GetList{Ai: 0}) {
        return false
    }
    // unify_variable X6
    if !vm.Step(&UnifyVariable{Xn: 105}) {
        return false
    }
    // get_structure hold/3, X6
    if !vm.Step(&GetStructure{Functor: "hold/3", Ai: 105}) {
        return false
    }
    // unify_variable Y1
    if !vm.Step(&UnifyVariable{Xn: 200}) {
        return false
    }
    // unify_variable X7
    if !vm.Step(&UnifyVariable{Xn: 106}) {
        return false
    }
    // unify_variable Y2
    if !vm.Step(&UnifyVariable{Xn: 201}) {
        return false
    }
    // unify_variable Y3
    if !vm.Step(&UnifyVariable{Xn: 202}) {
        return false
    }
    // get_variable Y4, A2
    vm.Regs[203] = vm.Regs[1]
    // get_variable Y5, A3
    vm.Regs[204] = vm.Regs[2]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y4, A2
        vm.Regs[1] = vm.getReg(203)
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // put_value Y5, A1
        vm.Regs[0] = vm.getReg(204)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_value Y4, A2
        vm.Regs[1] = vm.getReg(203)
        // put_value Y5, A3
        vm.Regs[2] = vm.getReg(204)
        // call hold_reason/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["hold_reason/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("hold_reason/3", 3) { return true }; vm.warnUnresolved("call", "hold_reason/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredIndex_catalog2 — lowered from index_catalog/2
func (vm *WamState) PredIndex_catalog2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y12, A1
    vm.Regs[211] = vm.Regs[0]
    // get_variable Y11, A2
    vm.Regs[210] = vm.Regs[1]
    // put_value Y12, A1
    vm.Regs[0] = vm.getReg(211)
    // call is_public_catalog/1
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["is_public_catalog/1"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("is_public_catalog/1", 1) { return true }; vm.warnUnresolved("call", "is_public_catalog/1"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y12, A1
    vm.Regs[0] = vm.getReg(211)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call depends_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["depends_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("depends_list/2", 2) { return true }; vm.warnUnresolved("call", "depends_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y12, A1
    vm.Regs[0] = vm.getReg(211)
    // put_variable Y5, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
        vm.putReg(204, v)
        vm.Regs[1] = v
    }
    // call packages/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["packages/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("packages/2", 2) { return true }; vm.warnUnresolved("call", "packages/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y5, A2
        vm.Regs[1] = vm.getReg(204)
        // call worth_indexing/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["worth_indexing/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("worth_indexing/2", 2) { return true }; vm.warnUnresolved("call", "worth_indexing/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant 0, A2
        vm.Regs[1] = &Integer{Val: 0}
        // put_variable Y2, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[2] = v
        }
        // call key_dep_rows/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["key_dep_rows/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("key_dep_rows/3", 3) { return true }; vm.warnUnresolved("call", "key_dep_rows/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_variable Y3, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
            vm.putReg(202, v)
            vm.Regs[1] = v
        }
        // builtin_call sort/2 2
        if !vm.executeBuiltin("sort/2", 2) {
            return false
        }
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_variable Y4, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
            vm.putReg(203, v)
            vm.Regs[1] = v
        }
        // call group_keyed/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["group_keyed/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("group_keyed/2", 2) { return true }; vm.warnUnresolved("call", "group_keyed/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_variable Y9, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 208), Idx: 208}
            vm.putReg(208, v)
            vm.Regs[1] = v
        }
        // call list_to_tree/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["list_to_tree/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("list_to_tree/2", 2) { return true }; vm.warnUnresolved("call", "list_to_tree/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y5, A1
        vm.Regs[0] = vm.getReg(204)
        // put_constant 0, A2
        vm.Regs[1] = &Integer{Val: 0}
        // put_variable Y6, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 205), Idx: 205}
            vm.putReg(205, v)
            vm.Regs[2] = v
        }
        // call key_pkg_rows/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["key_pkg_rows/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("key_pkg_rows/3", 3) { return true }; vm.warnUnresolved("call", "key_pkg_rows/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y6, A1
        vm.Regs[0] = vm.getReg(205)
        // put_variable Y7, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 206), Idx: 206}
            vm.putReg(206, v)
            vm.Regs[1] = v
        }
        // builtin_call sort/2 2
        if !vm.executeBuiltin("sort/2", 2) {
            return false
        }
        // put_value Y7, A1
        vm.Regs[0] = vm.getReg(206)
        // put_variable Y8, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 207), Idx: 207}
            vm.putReg(207, v)
            vm.Regs[1] = v
        }
        // call group_keyed/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["group_keyed/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("group_keyed/2", 2) { return true }; vm.warnUnresolved("call", "group_keyed/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y8, A1
        vm.Regs[0] = vm.getReg(207)
        // put_variable Y10, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 209), Idx: 209}
            vm.putReg(209, v)
            vm.Regs[1] = v
        }
        // call list_to_tree/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["list_to_tree/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("list_to_tree/2", 2) { return true }; vm.warnUnresolved("call", "list_to_tree/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y11, A1
        vm.Regs[0] = vm.getReg(210)
        // put_structure icat/3, A2
        if !vm.Step(&PutStructure{Functor: "icat/3", Ai: 1}) {
            return false
        }
        // set_value Y12
        if !vm.Step(&SetValue{Xn: 211}) {
            return false
        }
        // set_value Y9
        if !vm.Step(&SetValue{Xn: 208}) {
            return false
        }
        // set_value Y10
        if !vm.Step(&SetValue{Xn: 209}) {
            return false
        }
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y11, A1
        vm.Regs[0] = vm.getReg(210)
        // put_value Y12, A2
        vm.Regs[1] = vm.getReg(211)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredIndex_threshold1 — lowered from index_threshold/1
func (vm *WamState) PredIndex_threshold1() bool {
    // get_constant 64, A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = &Integer{Val: 64}
        } else if !valueEquals(vm.deref(_a), &Integer{Val: 64}) {
            return false
        }
    }
    return true

}

// PredInst_closure_names5 — lowered from inst_closure_names/5
func (vm *WamState) PredInst_closure_names5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_variable X2, A2
    vm.Regs[101] = vm.Regs[1]
    // get_variable X3, A3
    vm.Regs[102] = vm.Regs[2]
    // get_variable X4, A4
    vm.Regs[103] = vm.Regs[3]
    // get_variable X5, A5
    vm.Regs[104] = vm.Regs[4]
    // put_list A1
    if !vm.Step(&PutList{Ai: 0}) {
        return false
    }
    // set_variable X7
    if !vm.Step(&SetVariable{Xn: 106}) {
        return false
    }
    // set_constant []
    if !vm.Step(&SetConstant{C: wamAtom____0}) {
        return false
    }
    // put_structure -/2, X7
    if !vm.Step(&PutStructure{Functor: "-/2", Ai: 106}) {
        return false
    }
    // set_value X3
    if !vm.Step(&SetValue{Xn: 102}) {
        return false
    }
    // set_value X4
    if !vm.Step(&SetValue{Xn: 103}) {
        return false
    }
    // put_value X1, A2
    vm.Regs[1] = vm.getReg(100)
    // put_value X2, A3
    vm.Regs[2] = vm.getReg(101)
    // put_constant [], A4
    vm.Regs[3] = wamAtom____0
    // put_constant [], A5
    vm.Regs[4] = wamAtom____0
    // put_value X5, A6
    vm.Regs[5] = vm.getReg(104)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute inst_walk/6 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["inst_walk/6"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("inst_walk/6", 6) { return true }; vm.warnUnresolved("call", "inst_walk/6"); return false }()

}

// PredInst_walk6 — lowered from inst_walk/6
func (vm *WamState) PredInst_walk6() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_variable X2, A3
    vm.Regs[101] = vm.Regs[2]
    // get_variable X3, A4
    vm.Regs[102] = vm.Regs[3]
    // get_variable X4, A5
    vm.Regs[103] = vm.Regs[4]
    // get_value X4, A6
    if !vm.Unify(vm.Regs[5], vm.Regs[103]) {
        return false
    }
    return true

}

// PredInstalled_list2 — lowered from installed_list/2 (T4 all-clauses inline)
func (vm *WamState) PredInstalled_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_value X5, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[104]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X5, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[104]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X5, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[104]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute installed_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["installed_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("installed_list/2", 2) { return true }; vm.warnUnresolved("call", "installed_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredInstalled_or_base3 — lowered from installed_or_base/3 (T4 all-clauses inline)
func (vm *WamState) PredInstalled_or_base3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_variable X3, A3
        vm.Regs[102] = vm.Regs[2]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X2, A2
        vm.Regs[1] = vm.getReg(101)
        // put_value X3, A3
        vm.Regs[2] = vm.getReg(102)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute installed_ver/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["installed_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("installed_ver/3", 3) { return true }; vm.warnUnresolved("call", "installed_ver/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X3, A1
        vm.Regs[102] = vm.Regs[0]
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // get_variable Y1, A3
        vm.Regs[200] = vm.Regs[2]
        // put_value X3, A1
        vm.Regs[0] = vm.getReg(102)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // put_variable Y2, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[2] = v
        }
        // call base_ver/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["base_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_ver/3", 3) { return true }; vm.warnUnresolved("call", "base_ver/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredInstalled_ver3 — lowered from installed_ver/3
func (vm *WamState) PredInstalled_ver3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X4, A1
    vm.Regs[103] = vm.Regs[0]
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // get_variable Y2, A3
    vm.Regs[201] = vm.Regs[2]
    // put_value X4, A1
    vm.Regs[0] = vm.getReg(103)
    // put_variable Y3, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[1] = v
    }
    // call installed_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["installed_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("installed_list/2", 2) { return true }; vm.warnUnresolved("call", "installed_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_structure -/2, A1
    if !vm.Step(&PutStructure{Functor: "-/2", Ai: 0}) {
        return false
    }
    // set_value Y1
    if !vm.Step(&SetValue{Xn: 200}) {
        return false
    }
    // set_value Y2
    if !vm.Step(&SetValue{Xn: 201}) {
        return false
    }
    // put_value Y3, A2
    vm.Regs[1] = vm.getReg(202)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredIs_public_catalog1 — lowered from is_public_catalog/1 (T4 all-clauses inline)
func (vm *WamState) PredIs_public_catalog1() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredIs_v31 — lowered from is_v3/1
func (vm *WamState) PredIs_v31() bool {
    // get_structure v/3, A1
    if !vm.Step(&GetStructure{Functor: "v/3", Ai: 0}) {
        return false
    }
    // unify_variable X1
    if !vm.Step(&UnifyVariable{Xn: 100}) {
        return false
    }
    // unify_variable X2
    if !vm.Step(&UnifyVariable{Xn: 101}) {
        return false
    }
    // unify_variable X3
    if !vm.Step(&UnifyVariable{Xn: 102}) {
        return false
    }
    return true

}

// PredItem_ver3 — lowered from item_ver/3 (T4 all-clauses inline)
func (vm *WamState) PredItem_ver3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure -/2, A1
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // get_variable X3, A2
        vm.Regs[102] = vm.Regs[1]
        // get_value X2, A3
        if !vm.Unify(vm.Regs[2], vm.Regs[101]) {
            return false
        }
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X3, A2
        vm.Regs[1] = vm.getReg(102)
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure base/2, A1
        if !vm.Step(&GetStructure{Functor: "base/2", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // get_structure -/2, X1
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // get_variable X5, A2
        vm.Regs[104] = vm.Regs[1]
        // get_value X3, A3
        if !vm.Unify(vm.Regs[2], vm.Regs[102]) {
            return false
        }
        // put_value X2, A1
        vm.Regs[0] = vm.getReg(101)
        // put_value X5, A2
        vm.Regs[1] = vm.getReg(104)
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure layer/2, A1
        if !vm.Step(&GetStructure{Functor: "layer/2", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // get_variable X3, A2
        vm.Regs[102] = vm.Regs[1]
        // get_variable X4, A3
        vm.Regs[103] = vm.Regs[2]
        // put_value X2, A1
        vm.Regs[0] = vm.getReg(101)
        // put_value X3, A2
        vm.Regs[1] = vm.getReg(102)
        // put_value X4, A3
        vm.Regs[2] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute lookup_held/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["lookup_held/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("lookup_held/3", 3) { return true }; vm.warnUnresolved("call", "lookup_held/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredKeep_installed_or_base4 — lowered from keep_installed_or_base/4
func (vm *WamState) PredKeep_installed_or_base4() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_variable X2, A3
    vm.Regs[101] = vm.Regs[2]
    // get_value X2, A4
    if !vm.Unify(vm.Regs[3], vm.Regs[101]) {
        return false
    }
    return true

}

// PredKey_dep_rows3 — lowered from key_dep_rows/3 (T4 all-clauses inline)
func (vm *WamState) PredKey_dep_rows3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_constant [], A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_variable X1, A2
        vm.Regs[100] = vm.Regs[1]
        // get_constant [], A3
        {
            _a := vm.deref(vm.Regs[2])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_list A1
        if !vm.Step(&GetList{Ai: 0}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // get_structure depends/4, X5
        if !vm.Step(&GetStructure{Functor: "depends/4", Ai: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable Y2
        if !vm.Step(&UnifyVariable{Xn: 201}) {
            return false
        }
        // get_variable Y1, A2
        vm.Regs[200] = vm.Regs[1]
        // get_list A3
        if !vm.Step(&GetList{Ai: 2}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_structure -/2, X10
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 109}) {
            return false
        }
        // unify_variable X11
        if !vm.Step(&UnifyVariable{Xn: 110}) {
            return false
        }
        // get_structure -/2, X11
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 110}) {
            return false
        }
        // unify_variable X12
        if !vm.Step(&UnifyVariable{Xn: 111}) {
            return false
        }
        // get_structure -/2, X12
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 111}) {
            return false
        }
        // unify_value X6
        if !vm.Step(&UnifyValue{Xn: 105}) {
            return false
        }
        // unify_value X7
        if !vm.Step(&UnifyValue{Xn: 106}) {
            return false
        }
        // unify_value Y1
        if !vm.Step(&UnifyValue{Xn: 200}) {
            return false
        }
        // unify_variable X13
        if !vm.Step(&UnifyVariable{Xn: 112}) {
            return false
        }
        // unify_variable Y4
        if !vm.Step(&UnifyVariable{Xn: 203}) {
            return false
        }
        // put_value X8, A1
        vm.Regs[0] = vm.getReg(107)
        // put_value X9, A2
        vm.Regs[1] = vm.getReg(108)
        // put_value X13, A3
        vm.Regs[2] = vm.getReg(112)
        // call dep_to_req/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["dep_to_req/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("dep_to_req/3", 3) { return true }; vm.warnUnresolved("call", "dep_to_req/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_variable Y3, A1
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
            vm.putReg(202, v)
            vm.Regs[0] = v
        }
        // put_structure +/2, A2
        if !vm.Step(&PutStructure{Functor: "+/2", Ai: 1}) {
            return false
        }
        // set_value Y1
        if !vm.Step(&SetValue{Xn: 200}) {
            return false
        }
        // set_constant 1
        if !vm.Step(&SetConstant{C: &Integer{Val: 1}}) {
            return false
        }
        // builtin_call is/2 2
        if !vm.executeBuiltin("is/2", 2) {
            return false
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_value Y4, A3
        vm.Regs[2] = vm.getReg(203)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute key_dep_rows/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["key_dep_rows/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("key_dep_rows/3", 3) { return true }; vm.warnUnresolved("call", "key_dep_rows/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredKey_pkg_rows3 — lowered from key_pkg_rows/3 (T4 all-clauses inline)
func (vm *WamState) PredKey_pkg_rows3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_constant [], A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_variable X1, A2
        vm.Regs[100] = vm.Regs[1]
        // get_constant [], A3
        {
            _a := vm.deref(vm.Regs[2])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_list A1
        if !vm.Step(&GetList{Ai: 0}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // get_structure package/2, X4
        if !vm.Step(&GetStructure{Functor: "package/2", Ai: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable Y1
        if !vm.Step(&UnifyVariable{Xn: 200}) {
            return false
        }
        // get_variable X7, A2
        vm.Regs[106] = vm.Regs[1]
        // get_list A3
        if !vm.Step(&GetList{Ai: 2}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // get_structure -/2, X8
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_structure -/2, X9
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 108}) {
            return false
        }
        // unify_value X5
        if !vm.Step(&UnifyValue{Xn: 104}) {
            return false
        }
        // unify_value X7
        if !vm.Step(&UnifyValue{Xn: 106}) {
            return false
        }
        // unify_value X6
        if !vm.Step(&UnifyValue{Xn: 105}) {
            return false
        }
        // unify_variable Y3
        if !vm.Step(&UnifyVariable{Xn: 202}) {
            return false
        }
        // put_variable Y2, A1
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[0] = v
        }
        // put_structure +/2, A2
        if !vm.Step(&PutStructure{Functor: "+/2", Ai: 1}) {
            return false
        }
        // set_value X7
        if !vm.Step(&SetValue{Xn: 106}) {
            return false
        }
        // set_constant 1
        if !vm.Step(&SetConstant{C: &Integer{Val: 1}}) {
            return false
        }
        // builtin_call is/2 2
        if !vm.executeBuiltin("is/2", 2) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute key_pkg_rows/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["key_pkg_rows/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("key_pkg_rows/3", 3) { return true }; vm.warnUnresolved("call", "key_pkg_rows/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredLayer_closure3 — lowered from layer_closure/3
func (vm *WamState) PredLayer_closure3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable X4, A2
    vm.Regs[103] = vm.Regs[1]
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_list A2
    if !vm.Step(&PutList{Ai: 1}) {
        return false
    }
    // set_value X4
    if !vm.Step(&SetValue{Xn: 103}) {
        return false
    }
    // set_constant []
    if !vm.Step(&SetConstant{C: wamAtom____0}) {
        return false
    }
    // put_variable Y2, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[2] = v
    }
    // call resolve_layered/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["resolve_layered/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("resolve_layered/3", 3) { return true }; vm.warnUnresolved("call", "resolve_layered/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // call topo_sort_sel/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["topo_sort_sel/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("topo_sort_sel/3", 3) { return true }; vm.warnUnresolved("call", "topo_sort_sel/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredLayer_provider5 — lowered from layer_provider/5 (T4 all-clauses inline)
func (vm *WamState) PredLayer_provider5() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y3, A1
        vm.Regs[202] = vm.Regs[0]
        // get_variable Y6, A2
        vm.Regs[205] = vm.Regs[1]
        // get_variable Y7, A3
        vm.Regs[206] = vm.Regs[2]
        // get_variable Y4, A4
        vm.Regs[203] = vm.Regs[3]
        // get_variable Y5, A5
        vm.Regs[204] = vm.Regs[4]
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_variable Y2, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[1] = v
        }
        // call base_holds/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["base_holds/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_holds/2", 2) { return true }; vm.warnUnresolved("call", "base_holds/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_structure hold/3, A1
        if !vm.Step(&PutStructure{Functor: "hold/3", Ai: 0}) {
            return false
        }
        // set_value Y4
        if !vm.Step(&SetValue{Xn: 203}) {
            return false
        }
        // set_value Y5
        if !vm.Step(&SetValue{Xn: 204}) {
            return false
        }
        // set_variable Y1
        if !vm.Step(&SetVariable{Xn: 200}) {
            return false
        }
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_value Y4, A2
        vm.Regs[1] = vm.getReg(203)
        // put_value Y5, A3
        vm.Regs[2] = vm.getReg(204)
        // put_value Y6, A4
        vm.Regs[3] = vm.getReg(205)
        // put_value Y7, A5
        vm.Regs[4] = vm.getReg(206)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute provides_sat/5 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["provides_sat/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provides_sat/5", 5) { return true }; vm.warnUnresolved("call", "provides_sat/5"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y4, A1
        vm.Regs[203] = vm.Regs[0]
        // get_variable Y7, A2
        vm.Regs[206] = vm.Regs[1]
        // get_variable Y8, A3
        vm.Regs[207] = vm.Regs[2]
        // get_variable Y5, A4
        vm.Regs[204] = vm.Regs[3]
        // get_variable Y6, A5
        vm.Regs[205] = vm.Regs[4]
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_variable Y2, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[1] = v
        }
        // call layers_list/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["layers_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("layers_list/2", 2) { return true }; vm.warnUnresolved("call", "layers_list/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_structure layer/2, A1
        if !vm.Step(&PutStructure{Functor: "layer/2", Ai: 0}) {
            return false
        }
        // set_variable Y1
        if !vm.Step(&SetVariable{Xn: 200}) {
            return false
        }
        // set_variable Y3
        if !vm.Step(&SetVariable{Xn: 202}) {
            return false
        }
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_value Y5, A2
        vm.Regs[1] = vm.getReg(204)
        // put_value Y6, A3
        vm.Regs[2] = vm.getReg(205)
        // call lookup_held/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["lookup_held/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("lookup_held/3", 3) { return true }; vm.warnUnresolved("call", "lookup_held/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_value Y5, A2
        vm.Regs[1] = vm.getReg(204)
        // put_value Y6, A3
        vm.Regs[2] = vm.getReg(205)
        // put_value Y7, A4
        vm.Regs[3] = vm.getReg(206)
        // put_value Y8, A5
        vm.Regs[4] = vm.getReg(207)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute provides_sat/5 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["provides_sat/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provides_sat/5", 5) { return true }; vm.warnUnresolved("call", "provides_sat/5"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredLayered_walk_ver4 — lowered from layered_walk_ver/4
func (vm *WamState) PredLayered_walk_ver4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y2, A1
    vm.Regs[201] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // get_variable Y4, A3
    vm.Regs[203] = vm.Regs[2]
    // get_variable Y5, A4
    vm.Regs[204] = vm.Regs[3]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_variable Y1, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[2] = v
        }
        // call base_ver/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["base_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_ver/3", 3) { return true }; vm.warnUnresolved("call", "base_ver/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y4, A2
        vm.Regs[1] = vm.getReg(203)
        // call satisfies/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["satisfies/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("satisfies/2", 2) { return true }; vm.warnUnresolved("call", "satisfies/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y5, A1
        vm.Regs[0] = vm.getReg(204)
        // put_value Y1, A2
        vm.Regs[1] = vm.getReg(200)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_variable Y1, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[2] = v
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_value Y4, A3
        vm.Regs[2] = vm.getReg(203)
        // put_value Y5, A4
        vm.Regs[3] = vm.getReg(204)
        // call candidates_high_first/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["candidates_high_first/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("candidates_high_first/4", 4) { return true }; vm.warnUnresolved("call", "candidates_high_first/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredLayers_list2 — lowered from layers_list/2 (T4 all-clauses inline)
func (vm *WamState) PredLayers_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X7, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[106]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X7, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[106]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute layers_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["layers_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("layers_list/2", 2) { return true }; vm.warnUnresolved("call", "layers_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredList_to_tree2 — lowered from list_to_tree/2
func (vm *WamState) PredList_to_tree2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y2, A1
    vm.Regs[201] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // builtin_call length/2 2
    if !vm.executeBuiltin("length/2", 2) {
        return false
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_constant [], A4
    vm.Regs[3] = wamAtom____0
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute build_tree/4 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["build_tree/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("build_tree/4", 4) { return true }; vm.warnUnresolved("call", "build_tree/4"); return false }()

}

// PredLong_enough2 — lowered from long_enough/2
func (vm *WamState) PredLong_enough2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_list A1
    if !vm.Step(&GetList{Ai: 0}) {
        return false
    }
    // unify_variable X4
    if !vm.Step(&UnifyVariable{Xn: 103}) {
        return false
    }
    // unify_variable Y2
    if !vm.Step(&UnifyVariable{Xn: 201}) {
        return false
    }
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant 1, A2
        vm.Regs[1] = &Integer{Val: 1}
        // builtin_call =</2 2
        if !vm.executeBuiltin("=</2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_variable Y3, A1
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
            vm.putReg(202, v)
            vm.Regs[0] = v
        }
        // put_structure +/2, A2
        if !vm.Step(&PutStructure{Functor: "+/2", Ai: 1}) {
            return false
        }
        // set_value Y1
        if !vm.Step(&SetValue{Xn: 200}) {
            return false
        }
        // set_constant -1
        if !vm.Step(&SetConstant{C: &Integer{Val: -1}}) {
            return false
        }
        // builtin_call is/2 2
        if !vm.executeBuiltin("is/2", 2) {
            return false
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // call long_enough/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["long_enough/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("long_enough/2", 2) { return true }; vm.warnUnresolved("call", "long_enough/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredLookup_held3 — lowered from lookup_held/3
func (vm *WamState) PredLookup_held3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_list A1
    if !vm.Step(&GetList{Ai: 0}) {
        return false
    }
    // unify_variable Y1
    if !vm.Step(&UnifyVariable{Xn: 200}) {
        return false
    }
    // unify_variable Y3
    if !vm.Step(&UnifyVariable{Xn: 202}) {
        return false
    }
    // get_variable Y4, A2
    vm.Regs[203] = vm.Regs[1]
    // get_variable Y5, A3
    vm.Regs[204] = vm.Regs[2]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y4, A2
        vm.Regs[1] = vm.getReg(203)
        // put_variable Y2, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[2] = v
        }
        // call item_ver/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["item_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("item_ver/3", 3) { return true }; vm.warnUnresolved("call", "item_ver/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // put_value Y5, A1
        vm.Regs[0] = vm.getReg(204)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_variable Y2, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[2] = v
        }
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_value Y4, A2
        vm.Regs[1] = vm.getReg(203)
        // put_value Y5, A3
        vm.Regs[2] = vm.getReg(204)
        // call lookup_held/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["lookup_held/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("lookup_held/3", 3) { return true }; vm.warnUnresolved("call", "lookup_held/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredMap_requests3 — lowered from map_requests/3 (T4 all-clauses inline)
func (vm *WamState) PredMap_requests3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_constant [], A3
        {
            _a := vm.deref(vm.Regs[2])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y1, A1
        vm.Regs[200] = vm.Regs[0]
        // get_list A2
        if !vm.Step(&GetList{Ai: 1}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable Y2
        if !vm.Step(&UnifyVariable{Xn: 201}) {
            return false
        }
        // get_list A3
        if !vm.Step(&GetList{Ai: 2}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable Y3
        if !vm.Step(&UnifyVariable{Xn: 202}) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // put_value X5, A3
        vm.Regs[2] = vm.getReg(104)
        // call request_to_req/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["request_to_req/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("request_to_req/3", 3) { return true }; vm.warnUnresolved("call", "request_to_req/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute map_requests/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["map_requests/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("map_requests/3", 3) { return true }; vm.warnUnresolved("call", "map_requests/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredMatching_deps4 — lowered from matching_deps/4
func (vm *WamState) PredMatching_deps4() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_variable X2, A3
    vm.Regs[101] = vm.Regs[2]
    // get_constant [], A4
    {
        _a := vm.deref(vm.Regs[3])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    return true

}

// PredMatching_versions4 — lowered from matching_versions/4
func (vm *WamState) PredMatching_versions4() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_variable X2, A3
    vm.Regs[101] = vm.Regs[2]
    // get_constant [], A4
    {
        _a := vm.deref(vm.Regs[3])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    return true

}

// PredMatching_versions_in4 — lowered from matching_versions_in/4
func (vm *WamState) PredMatching_versions_in4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y3, A1
    vm.Regs[202] = vm.Regs[0]
    // get_variable Y5, A2
    vm.Regs[204] = vm.Regs[1]
    // get_variable Y6, A3
    vm.Regs[205] = vm.Regs[2]
    // get_variable Y7, A4
    vm.Regs[206] = vm.Regs[3]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_variable Y1, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[1] = v
        }
        // call pkg_index/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["pkg_index/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("pkg_index/2", 2) { return true }; vm.warnUnresolved("call", "pkg_index/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
        {
            _trailMark := vm.TrailLen
            _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
            _condOk := func() bool {
            // put_value Y1, A1
            vm.Regs[0] = vm.getReg(200)
            // put_value Y5, A2
            vm.Regs[1] = vm.getReg(204)
            // put_variable Y2, A3
            {
                v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
                vm.putReg(201, v)
                vm.Regs[2] = v
            }
            // call tree_lookup/3
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["tree_lookup/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("tree_lookup/3", 3) { return true }; vm.warnUnresolved("call", "tree_lookup/3"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
                return true
            }()
            if _condOk {
            // put_value Y2, A1
            vm.Regs[0] = vm.getReg(201)
            // put_value Y6, A2
            vm.Regs[1] = vm.getReg(205)
            // put_value Y7, A3
            vm.Regs[2] = vm.getReg(206)
            // call filter_satisfies/3
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["filter_satisfies/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("filter_satisfies/3", 3) { return true }; vm.warnUnresolved("call", "filter_satisfies/3"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
            } else {
                vm.Regs = _savedRegs
                vm.unwindTrailTo(_trailMark)
            // put_variable Y2, A3
            {
                v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
                vm.putReg(201, v)
                vm.Regs[2] = v
            }
            // put_value Y7, A1
            vm.Regs[0] = vm.getReg(206)
            // put_constant [], A2
            vm.Regs[1] = wamAtom____0
            // builtin_call =/2 2
            if !vm.executeBuiltin("=/2", 2) {
                return false
            }
            }
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_variable Y1, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[1] = v
        }
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_variable Y4, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
            vm.putReg(203, v)
            vm.Regs[1] = v
        }
        // call packages/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["packages/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("packages/2", 2) { return true }; vm.warnUnresolved("call", "packages/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_value Y5, A2
        vm.Regs[1] = vm.getReg(204)
        // put_value Y6, A3
        vm.Regs[2] = vm.getReg(205)
        // put_value Y7, A4
        vm.Regs[3] = vm.getReg(206)
        // call matching_versions/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["matching_versions/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("matching_versions/4", 4) { return true }; vm.warnUnresolved("call", "matching_versions/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredMember_selected3 — lowered from member_selected/3
func (vm *WamState) PredMember_selected3() bool {
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_variable X2, A2
    vm.Regs[101] = vm.Regs[1]
    // get_variable X3, A3
    vm.Regs[102] = vm.Regs[2]
    // put_structure -/2, A1
    if !vm.Step(&PutStructure{Functor: "-/2", Ai: 0}) {
        return false
    }
    // set_value X2
    if !vm.Step(&SetValue{Xn: 101}) {
        return false
    }
    // set_value X3
    if !vm.Step(&SetValue{Xn: 102}) {
        return false
    }
    // put_value X1, A2
    vm.Regs[1] = vm.getReg(100)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    return true

}

// PredNames_of2 — lowered from names_of/2 (T4 all-clauses inline)
func (vm *WamState) PredNames_of2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_constant [], A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_list A1
        if !vm.Step(&GetList{Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // get_structure -/2, X1
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // get_list A2
        if !vm.Step(&GetList{Ai: 1}) {
            return false
        }
        // unify_value X2
        if !vm.Step(&UnifyValue{Xn: 101}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // put_value X4, A1
        vm.Regs[0] = vm.getReg(103)
        // put_value X5, A2
        vm.Regs[1] = vm.getReg(104)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute names_of/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["names_of/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("names_of/2", 2) { return true }; vm.warnUnresolved("call", "names_of/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredNeeded_names4 — lowered from needed_names/4 (T4 all-clauses inline)
func (vm *WamState) PredNeeded_names4() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_constant [], A3
        {
            _a := vm.deref(vm.Regs[2])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_constant [], A4
        {
            _a := vm.deref(vm.Regs[3])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y2, A1
        vm.Regs[201] = vm.Regs[0]
        // get_variable Y3, A2
        vm.Regs[202] = vm.Regs[1]
        // get_variable X5, A3
        vm.Regs[104] = vm.Regs[2]
        // get_variable Y4, A4
        vm.Regs[203] = vm.Regs[3]
        // put_value X5, A1
        vm.Regs[0] = vm.getReg(104)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_variable Y1, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[2] = v
        }
        // call roots_to_pairs/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["roots_to_pairs/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("roots_to_pairs/3", 3) { return true }; vm.warnUnresolved("call", "roots_to_pairs/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // put_constant [], A4
        vm.Regs[3] = wamAtom____0
        // put_constant [], A5
        vm.Regs[4] = wamAtom____0
        // put_value Y4, A6
        vm.Regs[5] = vm.getReg(203)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute inst_walk/6 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["inst_walk/6"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("inst_walk/6", 6) { return true }; vm.warnUnresolved("call", "inst_walk/6"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredNo_acc_conflicts4 — lowered from no_acc_conflicts/4
func (vm *WamState) PredNo_acc_conflicts4() bool {
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_variable X2, A2
    vm.Regs[101] = vm.Regs[1]
    // get_variable X3, A3
    vm.Regs[102] = vm.Regs[2]
    // get_constant [], A4
    {
        _a := vm.deref(vm.Regs[3])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    return true

}

// PredOrder_lt2 — lowered from order_lt/2
func (vm *WamState) PredOrder_lt2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_constant [], A2
    {
        _a := vm.deref(vm.Regs[1])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // builtin_call fail/0 0
    if !vm.executeBuiltin("fail/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredOrder_val2 — lowered from order_val/2 (T4 all-clauses inline)
func (vm *WamState) PredOrder_val2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_constant 126, A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = &Integer{Val: 126}
            } else if !valueEquals(vm.deref(_a), &Integer{Val: 126}) {
                return false
            }
        }
        // get_constant -1, A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = &Integer{Val: -1}
            } else if !valueEquals(vm.deref(_a), &Integer{Val: -1}) {
                return false
            }
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y1, A1
        vm.Regs[200] = vm.Regs[0]
        // get_value Y1, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[200]) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant 65, A2
        vm.Regs[1] = &Integer{Val: 65}
        // builtin_call >=/2 2
        if !vm.executeBuiltin(">=/2", 2) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant 90, A2
        vm.Regs[1] = &Integer{Val: 90}
        // builtin_call =</2 2
        if !vm.executeBuiltin("=</2", 2) {
            return false
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y1, A1
        vm.Regs[200] = vm.Regs[0]
        // get_value Y1, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[200]) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant 97, A2
        vm.Regs[1] = &Integer{Val: 97}
        // builtin_call >=/2 2
        if !vm.executeBuiltin(">=/2", 2) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant 122, A2
        vm.Regs[1] = &Integer{Val: 122}
        // builtin_call =</2 2
        if !vm.executeBuiltin("=</2", 2) {
            return false
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // put_value X2, A1
        vm.Regs[0] = vm.getReg(101)
        // put_structure +/2, A2
        if !vm.Step(&PutStructure{Functor: "+/2", Ai: 1}) {
            return false
        }
        // set_value X1
        if !vm.Step(&SetValue{Xn: 100}) {
            return false
        }
        // set_constant 256
        if !vm.Step(&SetConstant{C: &Integer{Val: 256}}) {
            return false
        }
        // builtin_call is/2 2
        if !vm.executeBuiltin("is/2", 2) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredPackage_in3 — lowered from package_in/3
func (vm *WamState) PredPackage_in3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X4, A1
    vm.Regs[103] = vm.Regs[0]
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // get_variable Y2, A3
    vm.Regs[201] = vm.Regs[2]
    // put_value X4, A1
    vm.Regs[0] = vm.getReg(103)
    // put_variable Y3, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[1] = v
    }
    // call packages/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["packages/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("packages/2", 2) { return true }; vm.warnUnresolved("call", "packages/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_structure package/2, A1
    if !vm.Step(&PutStructure{Functor: "package/2", Ai: 0}) {
        return false
    }
    // set_value Y1
    if !vm.Step(&SetValue{Xn: 200}) {
        return false
    }
    // set_value Y2
    if !vm.Step(&SetValue{Xn: 201}) {
        return false
    }
    // put_value Y3, A2
    vm.Regs[1] = vm.getReg(202)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredPackage_in_name2 — lowered from package_in_name/2
func (vm *WamState) PredPackage_in_name2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X4, A1
    vm.Regs[103] = vm.Regs[0]
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // put_value X4, A1
    vm.Regs[0] = vm.getReg(103)
    // put_variable Y3, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[1] = v
    }
    // call packages/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["packages/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("packages/2", 2) { return true }; vm.warnUnresolved("call", "packages/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_structure package/2, A1
    if !vm.Step(&PutStructure{Functor: "package/2", Ai: 0}) {
        return false
    }
    // set_value Y1
    if !vm.Step(&SetValue{Xn: 200}) {
        return false
    }
    // set_variable Y2
    if !vm.Step(&SetVariable{Xn: 201}) {
        return false
    }
    // put_value Y3, A2
    vm.Regs[1] = vm.getReg(202)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredPackages2 — lowered from packages/2 (T4 all-clauses inline)
func (vm *WamState) PredPackages2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_value X1, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[100]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X1, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[100]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X1, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[100]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute packages/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["packages/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("packages/2", 2) { return true }; vm.warnUnresolved("call", "packages/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredPad_head2 — lowered from pad_head/2 (T4 all-clauses inline)
func (vm *WamState) PredPad_head2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_constant [], A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_list A2
        if !vm.Step(&GetList{Ai: 1}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // get_structure s/2, X1
        if !vm.Step(&GetStructure{Functor: "s/2", Ai: 100}) {
            return false
        }
        // unify_constant []
        if !vm.Step(&UnifyConstant{C: wamAtom____0}) {
            return false
        }
        // unify_constant 0
        if !vm.Step(&UnifyConstant{C: &Integer{Val: 0}}) {
            return false
        }
        // unify_constant []
        if !vm.Step(&UnifyConstant{C: wamAtom____0}) {
            return false
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_value X1, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[100]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredPick_repair4 — lowered from pick_repair/4
func (vm *WamState) PredPick_repair4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable Y2, A2
    vm.Regs[201] = vm.Regs[1]
    // get_variable Y4, A3
    vm.Regs[203] = vm.Regs[2]
    // get_variable Y3, A4
    vm.Regs[202] = vm.Regs[3]
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_constant any, A3
    vm.Regs[2] = wamAtom_any_10
    // put_value Y3, A4
    vm.Regs[3] = vm.getReg(202)
    // call candidates_high_first/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["candidates_high_first/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("candidates_high_first/4", 4) { return true }; vm.warnUnresolved("call", "candidates_high_first/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_value Y4, A4
    vm.Regs[3] = vm.getReg(203)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute repairs_moving/4 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["repairs_moving/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("repairs_moving/4", 4) { return true }; vm.warnUnresolved("call", "repairs_moving/4"); return false }()

}

// PredPkg_index2 — lowered from pkg_index/2
func (vm *WamState) PredPkg_index2() bool {
    // get_structure icat/3, A1
    if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
        return false
    }
    // unify_variable X1
    if !vm.Step(&UnifyVariable{Xn: 100}) {
        return false
    }
    // unify_variable X2
    if !vm.Step(&UnifyVariable{Xn: 101}) {
        return false
    }
    // unify_variable X3
    if !vm.Step(&UnifyVariable{Xn: 102}) {
        return false
    }
    // get_value X3, A2
    if !vm.Unify(vm.Regs[1], vm.Regs[102]) {
        return false
    }
    return true

}

// PredProvide_row5 — lowered from provide_row/5 (T4 all-clauses inline)
func (vm *WamState) PredProvide_row5() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure provides/3, A1
        if !vm.Step(&GetStructure{Functor: "provides/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_value X1, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[100]) {
            return false
        }
        // get_value X2, A3
        if !vm.Unify(vm.Regs[2], vm.Regs[101]) {
            return false
        }
        // get_value X3, A4
        if !vm.Unify(vm.Regs[3], vm.Regs[102]) {
            return false
        }
        // get_constant unversioned, A5
        {
            _a := vm.deref(vm.Regs[4])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_unversioned_15
            } else if !valueEquals(vm.deref(_a), wamAtom_unversioned_15) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure provides/4, A1
        if !vm.Step(&GetStructure{Functor: "provides/4", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // get_value X1, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[100]) {
            return false
        }
        // get_value X2, A3
        if !vm.Unify(vm.Regs[2], vm.Regs[101]) {
            return false
        }
        // get_value X3, A4
        if !vm.Unify(vm.Regs[3], vm.Regs[102]) {
            return false
        }
        // get_value X4, A5
        if !vm.Unify(vm.Regs[4], vm.Regs[103]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredProvide_satisfies2 — lowered from provide_satisfies/2 (T4 all-clauses inline)
func (vm *WamState) PredProvide_satisfies2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_constant unversioned, A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_unversioned_15
            } else if !valueEquals(vm.deref(_a), wamAtom_unversioned_15) {
                return false
            }
        }
        // get_constant any, A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_any_10
            } else if !valueEquals(vm.deref(_a), wamAtom_any_10) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y1, A1
        vm.Regs[200] = vm.Regs[0]
        // get_variable Y2, A2
        vm.Regs[201] = vm.Regs[1]
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant unversioned, A2
        vm.Regs[1] = wamAtom_unversioned_15
        // builtin_call \==/2 2
        if !vm.executeBuiltin("\\==/2", 2) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute satisfies/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["satisfies/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("satisfies/2", 2) { return true }; vm.warnUnresolved("call", "satisfies/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredProvider_candidate5 — lowered from provider_candidate/5
func (vm *WamState) PredProvider_candidate5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y6, A1
    vm.Regs[205] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // get_variable Y5, A3
    vm.Regs[204] = vm.Regs[2]
    // get_variable Y7, A4
    vm.Regs[206] = vm.Regs[3]
    // get_variable Y8, A5
    vm.Regs[207] = vm.Regs[4]
    // put_value Y6, A1
    vm.Regs[0] = vm.getReg(205)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call provides_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["provides_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provides_list/2", 2) { return true }; vm.warnUnresolved("call", "provides_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_variable Y2, A1
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[0] = v
    }
    // put_value Y1, A2
    vm.Regs[1] = vm.getReg(200)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value Y7, A2
    vm.Regs[1] = vm.getReg(206)
    // put_value Y8, A3
    vm.Regs[2] = vm.getReg(207)
    // put_value Y3, A4
    vm.Regs[3] = vm.getReg(202)
    // put_variable Y4, A5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
        vm.putReg(203, v)
        vm.Regs[4] = v
    }
    // call provide_row/5
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["provide_row/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provide_row/5", 5) { return true }; vm.warnUnresolved("call", "provide_row/5"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y6, A1
        vm.Regs[0] = vm.getReg(205)
        // put_value Y7, A2
        vm.Regs[1] = vm.getReg(206)
        // call excluded_name/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["excluded_name/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("excluded_name/2", 2) { return true }; vm.warnUnresolved("call", "excluded_name/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // builtin_call fail/0 0
        if !vm.executeBuiltin("fail/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        }
    }
    // put_value Y4, A1
    vm.Regs[0] = vm.getReg(203)
    // put_value Y5, A2
    vm.Regs[1] = vm.getReg(204)
    // call provide_satisfies/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["provide_satisfies/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provide_satisfies/2", 2) { return true }; vm.warnUnresolved("call", "provide_satisfies/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y6, A1
    vm.Regs[0] = vm.getReg(205)
    // put_value Y7, A2
    vm.Regs[1] = vm.getReg(206)
    // put_value Y8, A3
    vm.Regs[2] = vm.getReg(207)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute package_in/3 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["package_in/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("package_in/3", 3) { return true }; vm.warnUnresolved("call", "package_in/3"); return false }()

}

// PredProvides_for5 — lowered from provides_for/5
func (vm *WamState) PredProvides_for5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X7, A1
    vm.Regs[106] = vm.Regs[0]
    // get_variable Y5, A2
    vm.Regs[204] = vm.Regs[1]
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // get_variable Y4, A4
    vm.Regs[203] = vm.Regs[3]
    // get_variable Y6, A5
    vm.Regs[205] = vm.Regs[4]
    // put_value X7, A1
    vm.Regs[0] = vm.getReg(106)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call provides_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["provides_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provides_list/2", 2) { return true }; vm.warnUnresolved("call", "provides_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_variable Y2, A1
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[0] = v
    }
    // put_value Y1, A2
    vm.Regs[1] = vm.getReg(200)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value Y3, A2
    vm.Regs[1] = vm.getReg(202)
    // put_value Y4, A3
    vm.Regs[2] = vm.getReg(203)
    // put_value Y5, A4
    vm.Regs[3] = vm.getReg(204)
    // put_value Y6, A5
    vm.Regs[4] = vm.getReg(205)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute provide_row/5 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["provide_row/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provide_row/5", 5) { return true }; vm.warnUnresolved("call", "provide_row/5"); return false }()

}

// PredProvides_list2 — lowered from provides_list/2 (T4 all-clauses inline)
func (vm *WamState) PredProvides_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X10, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[109]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute provides_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["provides_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provides_list/2", 2) { return true }; vm.warnUnresolved("call", "provides_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredProvides_sat5 — lowered from provides_sat/5
func (vm *WamState) PredProvides_sat5() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X8, A1
    vm.Regs[107] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // get_variable Y4, A3
    vm.Regs[203] = vm.Regs[2]
    // get_variable Y5, A4
    vm.Regs[204] = vm.Regs[3]
    // get_variable Y7, A5
    vm.Regs[206] = vm.Regs[4]
    // put_value X8, A1
    vm.Regs[0] = vm.getReg(107)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call provides_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["provides_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provides_list/2", 2) { return true }; vm.warnUnresolved("call", "provides_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_variable Y2, A1
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[0] = v
    }
    // put_value Y1, A2
    vm.Regs[1] = vm.getReg(200)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value Y3, A2
    vm.Regs[1] = vm.getReg(202)
    // put_value Y4, A3
    vm.Regs[2] = vm.getReg(203)
    // put_value Y5, A4
    vm.Regs[3] = vm.getReg(204)
    // put_variable Y6, A5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 205), Idx: 205}
        vm.putReg(205, v)
        vm.Regs[4] = v
    }
    // call provide_row/5
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["provide_row/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provide_row/5", 5) { return true }; vm.warnUnresolved("call", "provide_row/5"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y6, A1
    vm.Regs[0] = vm.getReg(205)
    // put_value Y7, A2
    vm.Regs[1] = vm.getReg(206)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute provide_satisfies/2 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["provide_satisfies/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provide_satisfies/2", 2) { return true }; vm.warnUnresolved("call", "provide_satisfies/2"); return false }()

}

// PredRemoval_orphans3 — lowered from removal_orphans/3
func (vm *WamState) PredRemoval_orphans3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y10, A1
    vm.Regs[209] = vm.Regs[0]
    // get_variable X13, A2
    vm.Regs[112] = vm.Regs[1]
    // get_variable Y12, A3
    vm.Regs[211] = vm.Regs[2]
    // put_value Y10, A1
    vm.Regs[0] = vm.getReg(209)
    // put_value X13, A2
    vm.Regs[1] = vm.getReg(112)
    // put_variable Y7, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 206), Idx: 206}
        vm.putReg(206, v)
        vm.Regs[2] = v
    }
    // call canonicalize_name/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["canonicalize_name/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("canonicalize_name/3", 3) { return true }; vm.warnUnresolved("call", "canonicalize_name/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y10, A1
    vm.Regs[0] = vm.getReg(209)
    // put_variable Y6, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 205), Idx: 205}
        vm.putReg(205, v)
        vm.Regs[1] = v
    }
    // call installed_list/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["installed_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("installed_list/2", 2) { return true }; vm.warnUnresolved("call", "installed_list/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y10, A1
        vm.Regs[0] = vm.getReg(209)
        // put_value Y7, A2
        vm.Regs[1] = vm.getReg(206)
        // put_variable Y1, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[2] = v
        }
        // call installed_ver/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["installed_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("installed_ver/3", 3) { return true }; vm.warnUnresolved("call", "installed_ver/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_variable Y1, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[2] = v
        }
        // put_variable Y1, A1
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[0] = v
        }
        // put_constant none, A2
        vm.Regs[1] = wamAtom_none_6
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        }
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_constant none, A2
        vm.Regs[1] = wamAtom_none_6
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // put_value Y12, A1
        vm.Regs[0] = vm.getReg(211)
        // put_constant [], A2
        vm.Regs[1] = wamAtom____0
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y10, A1
        vm.Regs[0] = vm.getReg(209)
        // put_value Y6, A2
        vm.Regs[1] = vm.getReg(205)
        // put_value Y7, A3
        vm.Regs[2] = vm.getReg(206)
        // put_value Y1, A4
        vm.Regs[3] = vm.getReg(200)
        // put_variable Y8, A5
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 207), Idx: 207}
            vm.putReg(207, v)
            vm.Regs[4] = v
        }
        // call inst_closure_names/5
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["inst_closure_names/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("inst_closure_names/5", 5) { return true }; vm.warnUnresolved("call", "inst_closure_names/5"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y10, A1
        vm.Regs[0] = vm.getReg(209)
        // put_variable Y2, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[1] = v
        }
        // call requested_list/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["requested_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("requested_list/2", 2) { return true }; vm.warnUnresolved("call", "requested_list/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y7, A1
        vm.Regs[0] = vm.getReg(206)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_variable Y3, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
            vm.putReg(202, v)
            vm.Regs[2] = v
        }
        // call exclude_name/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["exclude_name/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("exclude_name/3", 3) { return true }; vm.warnUnresolved("call", "exclude_name/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y10, A1
        vm.Regs[0] = vm.getReg(209)
        // put_value Y6, A2
        vm.Regs[1] = vm.getReg(205)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // put_variable Y9, A4
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 208), Idx: 208}
            vm.putReg(208, v)
            vm.Regs[3] = v
        }
        // call needed_names/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["needed_names/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("needed_names/4", 4) { return true }; vm.warnUnresolved("call", "needed_names/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_variable Y11, Y11
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 210), Idx: 210}
            vm.putReg(210, v)
            vm.Regs[210] = v
        }
        // put_variable Y4, Y4
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
            vm.putReg(203, v)
            vm.Regs[203] = v
        }
        // put_variable Y5, Y5
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
            vm.putReg(204, v)
            vm.Regs[204] = v
        }
        // put_structure -/2, A1
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 0}) {
            return false
        }
        // set_value Y4
        if !vm.Step(&SetValue{Xn: 203}) {
            return false
        }
        // set_value Y5
        if !vm.Step(&SetValue{Xn: 204}) {
            return false
        }
        // put_value Y6, A2
        vm.Regs[1] = vm.getReg(205)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_value Y7, A2
        vm.Regs[1] = vm.getReg(206)
        // builtin_call \==/2 2
        if !vm.executeBuiltin("\\==/2", 2) {
            return false
        }
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_value Y8, A2
        vm.Regs[1] = vm.getReg(207)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
        {
            _trailMark := vm.TrailLen
            _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
            _condOk := func() bool {
            // put_value Y4, A1
            vm.Regs[0] = vm.getReg(203)
            // put_value Y9, A2
            vm.Regs[1] = vm.getReg(208)
            // builtin_call member/2 2
            if !vm.executeBuiltin("member/2", 2) {
                return false
            }
                return true
            }()
            if _condOk {
            // builtin_call fail/0 0
            if !vm.executeBuiltin("fail/0", 0) {
                return false
            }
            } else {
                vm.Regs = _savedRegs
                vm.unwindTrailTo(_trailMark)
            // builtin_call true/0 0
            if !vm.executeBuiltin("true/0", 0) {
                return false
            }
            }
        }
        // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
        {
            _trailMark := vm.TrailLen
            _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
            _condOk := func() bool {
            // put_value Y10, A1
            vm.Regs[0] = vm.getReg(209)
            // put_value Y4, A2
            vm.Regs[1] = vm.getReg(203)
            // call base_name/2
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["base_name/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_name/2", 2) { return true }; vm.warnUnresolved("call", "base_name/2"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
                return true
            }()
            if _condOk {
            // builtin_call fail/0 0
            if !vm.executeBuiltin("fail/0", 0) {
                return false
            }
            } else {
                vm.Regs = _savedRegs
                vm.unwindTrailTo(_trailMark)
            // builtin_call true/0 0
            if !vm.executeBuiltin("true/0", 0) {
                return false
            }
            }
        }
        // put_structure -/2, A1
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 0}) {
            return false
        }
        // set_value Y4
        if !vm.Step(&SetValue{Xn: 203}) {
            return false
        }
        // set_value Y5
        if !vm.Step(&SetValue{Xn: 204}) {
            return false
        }
        // put_value Y11, A1
        vm.Regs[0] = vm.getReg(210)
        // put_value Y12, A2
        vm.Regs[1] = vm.getReg(211)
        // builtin_call sort/2 2
        if !vm.executeBuiltin("sort/2", 2) {
            return false
        }
        }
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredRepairs_moving4 — lowered from repairs_moving/4
func (vm *WamState) PredRepairs_moving4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X3, A1
    vm.Regs[102] = vm.Regs[0]
    // get_variable X4, A2
    vm.Regs[103] = vm.Regs[1]
    // get_variable X5, A3
    vm.Regs[104] = vm.Regs[2]
    // get_variable Y2, A4
    vm.Regs[201] = vm.Regs[3]
    // put_value X3, A1
    vm.Regs[0] = vm.getReg(102)
    // put_value X4, A2
    vm.Regs[1] = vm.getReg(103)
    // put_value X5, A3
    vm.Regs[2] = vm.getReg(104)
    // put_variable Y1, A4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[3] = v
    }
    // call collect_deps/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["collect_deps/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("collect_deps/4", 4) { return true }; vm.warnUnresolved("call", "collect_deps/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute reqs_ok_moving/2 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["reqs_ok_moving/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("reqs_ok_moving/2", 2) { return true }; vm.warnUnresolved("call", "reqs_ok_moving/2"); return false }()

}

// PredReqs_ok_moving2 — lowered from reqs_ok_moving/2
func (vm *WamState) PredReqs_ok_moving2() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    return true

}

// PredRequest_to_req3 — lowered from request_to_req/3
func (vm *WamState) PredRequest_to_req3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y2, A1
    vm.Regs[201] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // get_structure req/2, A3
    if !vm.Step(&GetStructure{Functor: "req/2", Ai: 2}) {
        return false
    }
    // unify_variable Y4
    if !vm.Step(&UnifyVariable{Xn: 203}) {
        return false
    }
    // unify_variable Y5
    if !vm.Step(&UnifyVariable{Xn: 204}) {
        return false
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_structure req/2, A2
        if !vm.Step(&PutStructure{Functor: "req/2", Ai: 1}) {
            return false
        }
        // set_variable Y1
        if !vm.Step(&SetVariable{Xn: 200}) {
            return false
        }
        // set_value Y5
        if !vm.Step(&SetValue{Xn: 204}) {
            return false
        }
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y1, A2
        vm.Regs[1] = vm.getReg(200)
        // put_value Y4, A3
        vm.Regs[2] = vm.getReg(203)
        // call canonicalize_name/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["canonicalize_name/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("canonicalize_name/3", 3) { return true }; vm.warnUnresolved("call", "canonicalize_name/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_value Y4, A3
        vm.Regs[2] = vm.getReg(203)
        // call canonicalize_name/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["canonicalize_name/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("canonicalize_name/3", 3) { return true }; vm.warnUnresolved("call", "canonicalize_name/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y5, A1
        vm.Regs[0] = vm.getReg(204)
        // put_constant any, A2
        vm.Regs[1] = wamAtom_any_10
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredRequested_list2 — lowered from requested_list/2 (T4 all-clauses inline)
func (vm *WamState) PredRequested_list2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_structure catalog/6, A1
        if !vm.Step(&GetStructure{Functor: "catalog/6", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // get_value X6, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[105]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/9, A1
        if !vm.Step(&GetStructure{Functor: "catalog/9", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // get_value X6, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[105]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_structure catalog/10, A1
        if !vm.Step(&GetStructure{Functor: "catalog/10", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable X7
        if !vm.Step(&UnifyVariable{Xn: 106}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable X9
        if !vm.Step(&UnifyVariable{Xn: 108}) {
            return false
        }
        // unify_variable X10
        if !vm.Step(&UnifyVariable{Xn: 109}) {
            return false
        }
        // get_value X6, A2
        if !vm.Unify(vm.Regs[1], vm.Regs[105]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_structure icat/3, A1
        if !vm.Step(&GetStructure{Functor: "icat/3", Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // unify_variable X3
        if !vm.Step(&UnifyVariable{Xn: 102}) {
            return false
        }
        // get_variable X4, A2
        vm.Regs[103] = vm.Regs[1]
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X4, A2
        vm.Regs[1] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute requested_list/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["requested_list/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("requested_list/2", 2) { return true }; vm.warnUnresolved("call", "requested_list/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredResolve3 — lowered from resolve/3
func (vm *WamState) PredResolve3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X6, A1
    vm.Regs[105] = vm.Regs[0]
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // get_variable Y5, A3
    vm.Regs[204] = vm.Regs[2]
    // put_value X6, A1
    vm.Regs[0] = vm.getReg(105)
    // put_variable Y2, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[1] = v
    }
    // call index_catalog/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["index_catalog/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("index_catalog/2", 2) { return true }; vm.warnUnresolved("call", "index_catalog/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value Y1, A2
    vm.Regs[1] = vm.getReg(200)
    // put_variable Y3, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[2] = v
    }
    // call map_requests/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["map_requests/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("map_requests/3", 3) { return true }; vm.warnUnresolved("call", "map_requests/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_constant classic, A1
    vm.Regs[0] = wamAtom_classic_12
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_constant [], A4
    vm.Regs[3] = wamAtom____0
    // put_variable Y4, A5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
        vm.putReg(203, v)
        vm.Regs[4] = v
    }
    // call resolve_pending/5
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["resolve_pending/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("resolve_pending/5", 5) { return true }; vm.warnUnresolved("call", "resolve_pending/5"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // put_value Y4, A1
    vm.Regs[0] = vm.getReg(203)
    // put_value Y5, A2
    vm.Regs[1] = vm.getReg(204)
    // builtin_call sort/2 2
    if !vm.executeBuiltin("sort/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredResolve_alternatives6 — lowered from resolve_alternatives/6
func (vm *WamState) PredResolve_alternatives6() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y2, A1
    vm.Regs[201] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // get_variable Y1, A3
    vm.Regs[200] = vm.Regs[2]
    // get_variable Y6, A4
    vm.Regs[205] = vm.Regs[3]
    // get_variable Y7, A5
    vm.Regs[206] = vm.Regs[4]
    // get_variable Y8, A6
    vm.Regs[207] = vm.Regs[5]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_value Y7, A3
        vm.Regs[2] = vm.getReg(206)
        // put_value Y1, A4
        vm.Regs[3] = vm.getReg(200)
        // call first_alt_already/4
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["first_alt_already/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("first_alt_already/4", 4) { return true }; vm.warnUnresolved("call", "first_alt_already/4"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_value Y6, A3
        vm.Regs[2] = vm.getReg(205)
        // put_value Y7, A4
        vm.Regs[3] = vm.getReg(206)
        // put_value Y8, A5
        vm.Regs[4] = vm.getReg(207)
        // call resolve_pending/5
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["resolve_pending/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("resolve_pending/5", 5) { return true }; vm.warnUnresolved("call", "resolve_pending/5"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_structure dep/2, A1
        if !vm.Step(&PutStructure{Functor: "dep/2", Ai: 0}) {
            return false
        }
        // set_variable Y4
        if !vm.Step(&SetVariable{Xn: 203}) {
            return false
        }
        // set_variable Y5
        if !vm.Step(&SetVariable{Xn: 204}) {
            return false
        }
        // put_value Y1, A2
        vm.Regs[1] = vm.getReg(200)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_structure [|]/2, A3
        if !vm.Step(&PutStructure{Functor: "[|]/2", Ai: 2}) {
            return false
        }
        // set_variable X11
        if !vm.Step(&SetVariable{Xn: 110}) {
            return false
        }
        // set_value Y6
        if !vm.Step(&SetValue{Xn: 205}) {
            return false
        }
        // put_structure req/2, X11
        if !vm.Step(&PutStructure{Functor: "req/2", Ai: 110}) {
            return false
        }
        // set_value Y4
        if !vm.Step(&SetValue{Xn: 203}) {
            return false
        }
        // set_value Y5
        if !vm.Step(&SetValue{Xn: 204}) {
            return false
        }
        // put_value Y7, A4
        vm.Regs[3] = vm.getReg(206)
        // put_value Y8, A5
        vm.Regs[4] = vm.getReg(207)
        // call resolve_pending/5
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["resolve_pending/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("resolve_pending/5", 5) { return true }; vm.warnUnresolved("call", "resolve_pending/5"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredResolve_layered3 — lowered from resolve_layered/3
func (vm *WamState) PredResolve_layered3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X6, A1
    vm.Regs[105] = vm.Regs[0]
    // get_variable Y1, A2
    vm.Regs[200] = vm.Regs[1]
    // get_variable Y5, A3
    vm.Regs[204] = vm.Regs[2]
    // put_value X6, A1
    vm.Regs[0] = vm.getReg(105)
    // put_variable Y2, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[1] = v
    }
    // call index_catalog/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["index_catalog/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("index_catalog/2", 2) { return true }; vm.warnUnresolved("call", "index_catalog/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_value Y1, A2
    vm.Regs[1] = vm.getReg(200)
    // put_variable Y3, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[2] = v
    }
    // call map_requests/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["map_requests/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("map_requests/3", 3) { return true }; vm.warnUnresolved("call", "map_requests/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_constant layered, A1
    vm.Regs[0] = wamAtom_layered_11
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // put_constant [], A4
    vm.Regs[3] = wamAtom____0
    // put_variable Y4, A5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
        vm.putReg(203, v)
        vm.Regs[4] = v
    }
    // call resolve_pending/5
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["resolve_pending/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("resolve_pending/5", 5) { return true }; vm.warnUnresolved("call", "resolve_pending/5"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // put_value Y4, A1
    vm.Regs[0] = vm.getReg(203)
    // put_value Y5, A2
    vm.Regs[1] = vm.getReg(204)
    // builtin_call sort/2 2
    if !vm.executeBuiltin("sort/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredResolve_pending5 — lowered from resolve_pending/5
func (vm *WamState) PredResolve_pending5() bool {
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_variable X2, A2
    vm.Regs[101] = vm.Regs[1]
    // get_constant [], A3
    {
        _a := vm.deref(vm.Regs[2])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X3, A4
    vm.Regs[102] = vm.Regs[3]
    // get_value X3, A5
    if !vm.Unify(vm.Regs[4], vm.Regs[102]) {
        return false
    }
    return true

}

// PredRoots_to_pairs3 — lowered from roots_to_pairs/3 (T4 all-clauses inline)
func (vm *WamState) PredRoots_to_pairs3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_constant [], A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_variable X1, A2
        vm.Regs[100] = vm.Regs[1]
        // get_constant [], A3
        {
            _a := vm.deref(vm.Regs[2])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_list A1
        if !vm.Step(&GetList{Ai: 0}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // unify_variable Y1
        if !vm.Step(&UnifyVariable{Xn: 200}) {
            return false
        }
        // get_variable Y2, A2
        vm.Regs[201] = vm.Regs[1]
        // get_list A3
        if !vm.Step(&GetList{Ai: 2}) {
            return false
        }
        // unify_variable X5
        if !vm.Step(&UnifyVariable{Xn: 104}) {
            return false
        }
        // get_structure -/2, X5
        if !vm.Step(&GetStructure{Functor: "-/2", Ai: 104}) {
            return false
        }
        // unify_value X4
        if !vm.Step(&UnifyValue{Xn: 103}) {
            return false
        }
        // unify_variable X6
        if !vm.Step(&UnifyVariable{Xn: 105}) {
            return false
        }
        // unify_variable Y3
        if !vm.Step(&UnifyVariable{Xn: 202}) {
            return false
        }
        // put_structure -/2, A1
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 0}) {
            return false
        }
        // set_value X4
        if !vm.Step(&SetValue{Xn: 103}) {
            return false
        }
        // set_value X6
        if !vm.Step(&SetValue{Xn: 105}) {
            return false
        }
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // builtin_call member/2 2
        if !vm.executeBuiltin("member/2", 2) {
            return false
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute roots_to_pairs/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["roots_to_pairs/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("roots_to_pairs/3", 3) { return true }; vm.warnUnresolved("call", "roots_to_pairs/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_list A1
        if !vm.Step(&GetList{Ai: 0}) {
            return false
        }
        // unify_variable X1
        if !vm.Step(&UnifyVariable{Xn: 100}) {
            return false
        }
        // unify_variable X2
        if !vm.Step(&UnifyVariable{Xn: 101}) {
            return false
        }
        // get_variable X3, A2
        vm.Regs[102] = vm.Regs[1]
        // get_variable X4, A3
        vm.Regs[103] = vm.Regs[2]
        // put_value X2, A1
        vm.Regs[0] = vm.getReg(101)
        // put_value X3, A2
        vm.Regs[1] = vm.getReg(102)
        // put_value X4, A3
        vm.Regs[2] = vm.getReg(103)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute roots_to_pairs/3 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["roots_to_pairs/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("roots_to_pairs/3", 3) { return true }; vm.warnUnresolved("call", "roots_to_pairs/3"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredSafe_upgrade_reason5 — lowered from safe_upgrade_reason/5 (T4 all-clauses inline)
func (vm *WamState) PredSafe_upgrade_reason5() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_variable X3, A3
        vm.Regs[102] = vm.Regs[2]
        // get_constant modified, A4
        {
            _a := vm.deref(vm.Regs[3])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_modified_17
            } else if !valueEquals(vm.deref(_a), wamAtom_modified_17) {
                return false
            }
        }
        // get_structure unsafe/1, A5
        if !vm.Step(&GetStructure{Functor: "unsafe/1", Ai: 4}) {
            return false
        }
        // unify_constant modified
        if !vm.Step(&UnifyConstant{C: wamAtom_modified_17}) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_variable X3, A3
        vm.Regs[102] = vm.Regs[2]
        // get_constant footprint, A4
        {
            _a := vm.deref(vm.Regs[3])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_footprint_18
            } else if !valueEquals(vm.deref(_a), wamAtom_footprint_18) {
                return false
            }
        }
        // get_structure safe/1, A5
        if !vm.Step(&GetStructure{Functor: "safe/1", Ai: 4}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // get_structure cost/1, X4
        if !vm.Step(&GetStructure{Functor: "cost/1", Ai: 103}) {
            return false
        }
        // unify_constant footprint
        if !vm.Step(&UnifyConstant{C: wamAtom_footprint_18}) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_variable X3, A3
        vm.Regs[102] = vm.Regs[2]
        // get_constant blanket, A4
        {
            _a := vm.deref(vm.Regs[3])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_blanket_2
            } else if !valueEquals(vm.deref(_a), wamAtom_blanket_2) {
                return false
            }
        }
        // get_structure safe/1, A5
        if !vm.Step(&GetStructure{Functor: "safe/1", Ai: 4}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // get_structure cost/1, X4
        if !vm.Step(&GetStructure{Functor: "cost/1", Ai: 103}) {
            return false
        }
        // unify_constant blanket
        if !vm.Step(&UnifyConstant{C: wamAtom_blanket_2}) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_variable X3, A3
        vm.Regs[102] = vm.Regs[2]
        // get_constant layer_shadow, A4
        {
            _a := vm.deref(vm.Regs[3])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_layer_shadow_19
            } else if !valueEquals(vm.deref(_a), wamAtom_layer_shadow_19) {
                return false
            }
        }
        // get_structure safe/1, A5
        if !vm.Step(&GetStructure{Functor: "safe/1", Ai: 4}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // get_structure cost/1, X4
        if !vm.Step(&GetStructure{Functor: "cost/1", Ai: 103}) {
            return false
        }
        // unify_constant layer_shadow
        if !vm.Step(&UnifyConstant{C: wamAtom_layer_shadow_19}) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_variable X2, A2
        vm.Regs[101] = vm.Regs[1]
        // get_variable X3, A3
        vm.Regs[102] = vm.Regs[2]
        // get_constant abi_anchor, A4
        {
            _a := vm.deref(vm.Regs[3])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom_abi_anchor_3
            } else if !valueEquals(vm.deref(_a), wamAtom_abi_anchor_3) {
                return false
            }
        }
        // get_structure coordinated/1, A5
        if !vm.Step(&GetStructure{Functor: "coordinated/1", Ai: 4}) {
            return false
        }
        // unify_variable X4
        if !vm.Step(&UnifyVariable{Xn: 103}) {
            return false
        }
        // put_value X1, A1
        vm.Regs[0] = vm.getReg(100)
        // put_value X2, A2
        vm.Regs[1] = vm.getReg(101)
        // put_value X3, A3
        vm.Regs[2] = vm.getReg(102)
        // put_structure ok/1, A4
        if !vm.Step(&PutStructure{Functor: "ok/1", Ai: 3}) {
            return false
        }
        // set_value X4
        if !vm.Step(&SetValue{Xn: 103}) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute upgrade_set_result/4 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["upgrade_set_result/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("upgrade_set_result/4", 4) { return true }; vm.warnUnresolved("call", "upgrade_set_result/4"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredSame_key4 — lowered from same_key/4
func (vm *WamState) PredSame_key4() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_constant [], A3
    {
        _a := vm.deref(vm.Regs[2])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_constant [], A4
    {
        _a := vm.deref(vm.Regs[3])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    return true

}

// PredSatisfies2 — lowered from satisfies/2
func (vm *WamState) PredSatisfies2() bool {
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_constant any, A2
    {
        _a := vm.deref(vm.Regs[1])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom_any_10
        } else if !valueEquals(vm.deref(_a), wamAtom_any_10) {
            return false
        }
    }
    return true

}

// PredScan_base_holds3 — lowered from scan_base_holds/3
func (vm *WamState) PredScan_base_holds3() bool {
    // get_constant [], A1
    {
        _a := vm.deref(vm.Regs[0])
        if _, ok := _a.(*Unbound); ok {
            u := _a.(*Unbound)
            vm.trailBinding(u.Idx)
            vm.Regs[u.Idx] = wamAtom____0
        } else if !valueEquals(vm.deref(_a), wamAtom____0) {
            return false
        }
    }
    // get_variable X1, A2
    vm.Regs[100] = vm.Regs[1]
    // get_value X1, A3
    if !vm.Unify(vm.Regs[2], vm.Regs[100]) {
        return false
    }
    return true

}

// PredSeen_name2 — lowered from seen_name/2
func (vm *WamState) PredSeen_name2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_list A1
    if !vm.Step(&GetList{Ai: 0}) {
        return false
    }
    // unify_variable Y1
    if !vm.Step(&UnifyVariable{Xn: 200}) {
        return false
    }
    // unify_variable Y2
    if !vm.Step(&UnifyVariable{Xn: 201}) {
        return false
    }
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // builtin_call ==/2 2
        if !vm.executeBuiltin("==/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // call seen_name/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["seen_name/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("seen_name/2", 2) { return true }; vm.warnUnresolved("call", "seen_name/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredSegs_lt2 — lowered from segs_lt/2 (T4 all-clauses inline)
func (vm *WamState) PredSegs_lt2() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_constant [], A1
        {
            _a := vm.deref(vm.Regs[0])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // builtin_call fail/0 0
        if !vm.executeBuiltin("fail/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X4, A1
        vm.Regs[103] = vm.Regs[0]
        // get_variable Y1, A2
        vm.Regs[200] = vm.Regs[1]
        // put_value X4, A1
        vm.Regs[0] = vm.getReg(103)
        // put_variable Y2, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[1] = v
        }
        // call pad_head/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["pad_head/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("pad_head/2", 2) { return true }; vm.warnUnresolved("call", "pad_head/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_variable Y3, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
            vm.putReg(202, v)
            vm.Regs[1] = v
        }
        // call pad_head/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["pad_head/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("pad_head/2", 2) { return true }; vm.warnUnresolved("call", "pad_head/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute segs_lt_1/2 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["segs_lt_1/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("segs_lt_1/2", 2) { return true }; vm.warnUnresolved("call", "segs_lt_1/2"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredSegs_lt_12 — lowered from segs_lt_1/2
func (vm *WamState) PredSegs_lt_12() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_list A1
    if !vm.Step(&GetList{Ai: 0}) {
        return false
    }
    // unify_variable X7
    if !vm.Step(&UnifyVariable{Xn: 106}) {
        return false
    }
    // get_structure s/2, X7
    if !vm.Step(&GetStructure{Functor: "s/2", Ai: 106}) {
        return false
    }
    // unify_variable Y1
    if !vm.Step(&UnifyVariable{Xn: 200}) {
        return false
    }
    // unify_variable Y3
    if !vm.Step(&UnifyVariable{Xn: 202}) {
        return false
    }
    // unify_variable Y5
    if !vm.Step(&UnifyVariable{Xn: 204}) {
        return false
    }
    // get_list A2
    if !vm.Step(&GetList{Ai: 1}) {
        return false
    }
    // unify_variable X8
    if !vm.Step(&UnifyVariable{Xn: 107}) {
        return false
    }
    // get_structure s/2, X8
    if !vm.Step(&GetStructure{Functor: "s/2", Ai: 107}) {
        return false
    }
    // unify_variable Y2
    if !vm.Step(&UnifyVariable{Xn: 201}) {
        return false
    }
    // unify_variable Y4
    if !vm.Step(&UnifyVariable{Xn: 203}) {
        return false
    }
    // unify_variable Y6
    if !vm.Step(&UnifyVariable{Xn: 205}) {
        return false
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // call order_lt/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["order_lt/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("order_lt/2", 2) { return true }; vm.warnUnresolved("call", "order_lt/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
        {
            _trailMark := vm.TrailLen
            _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
            _condOk := func() bool {
            // put_value Y1, A1
            vm.Regs[0] = vm.getReg(200)
            // put_value Y2, A2
            vm.Regs[1] = vm.getReg(201)
            // builtin_call ==/2 2
            if !vm.executeBuiltin("==/2", 2) {
                return false
            }
            // put_value Y3, A1
            vm.Regs[0] = vm.getReg(202)
            // put_value Y4, A2
            vm.Regs[1] = vm.getReg(203)
            // builtin_call </2 2
            if !vm.executeBuiltin("</2", 2) {
                return false
            }
                return true
            }()
            if _condOk {
            // builtin_call true/0 0
            if !vm.executeBuiltin("true/0", 0) {
                return false
            }
            } else {
                vm.Regs = _savedRegs
                vm.unwindTrailTo(_trailMark)
            // put_value Y1, A1
            vm.Regs[0] = vm.getReg(200)
            // put_value Y2, A2
            vm.Regs[1] = vm.getReg(201)
            // builtin_call ==/2 2
            if !vm.executeBuiltin("==/2", 2) {
                return false
            }
            // put_value Y3, A1
            vm.Regs[0] = vm.getReg(202)
            // put_value Y4, A2
            vm.Regs[1] = vm.getReg(203)
            // builtin_call =:=/2 2
            if !vm.executeBuiltin("=:=/2", 2) {
                return false
            }
            // put_value Y5, A1
            vm.Regs[0] = vm.getReg(204)
            // put_value Y6, A2
            vm.Regs[1] = vm.getReg(205)
            // call segs_lt/2
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["segs_lt/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("segs_lt/2", 2) { return true }; vm.warnUnresolved("call", "segs_lt/2"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
            }
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredSelected_ver3 — lowered from selected_ver/3
func (vm *WamState) PredSelected_ver3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_list A1
    if !vm.Step(&GetList{Ai: 0}) {
        return false
    }
    // unify_variable Y1
    if !vm.Step(&UnifyVariable{Xn: 200}) {
        return false
    }
    // unify_variable Y2
    if !vm.Step(&UnifyVariable{Xn: 201}) {
        return false
    }
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // get_variable Y4, A3
    vm.Regs[203] = vm.Regs[2]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_structure -/2, A2
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 1}) {
            return false
        }
        // set_value Y3
        if !vm.Step(&SetValue{Xn: 202}) {
            return false
        }
        // set_value Y4
        if !vm.Step(&SetValue{Xn: 203}) {
            return false
        }
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_value Y4, A3
        vm.Regs[2] = vm.getReg(203)
        // call selected_ver/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["selected_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("selected_ver/3", 3) { return true }; vm.warnUnresolved("call", "selected_ver/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredSort_versions_desc2 — lowered from sort_versions_desc/2
func (vm *WamState) PredSort_versions_desc2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_constant is_v3, A1
        vm.Regs[0] = wamAtom_is_v3_21
        // put_value Y1, A2
        vm.Regs[1] = vm.getReg(200)
        // builtin_call maplist/2 2
        if !vm.executeBuiltin("maplist/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_variable Y2, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[1] = v
        }
        // builtin_call sort/2 2
        if !vm.executeBuiltin("sort/2", 2) {
            return false
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // builtin_call reverse/2 2
        if !vm.executeBuiltin("reverse/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_constant cmp_ver, A1
        vm.Regs[0] = wamAtom_cmp_ver_22
        // put_value Y1, A2
        vm.Regs[1] = vm.getReg(200)
        // put_variable Y2, A3
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
            vm.putReg(201, v)
            vm.Regs[2] = v
        }
        // call predsort/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["predsort/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("predsort/3", 3) { return true }; vm.warnUnresolved("call", "predsort/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // builtin_call reverse/2 2
        if !vm.executeBuiltin("reverse/2", 2) {
            return false
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredTight_base_revdep2 — lowered from tight_base_revdep/2
func (vm *WamState) PredTight_base_revdep2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y2, A1
    vm.Regs[201] = vm.Regs[0]
    // get_variable Y3, A2
    vm.Regs[202] = vm.Regs[1]
    // put_value Y2, A1
    vm.Regs[0] = vm.getReg(201)
    // put_variable Y1, A2
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
        vm.putReg(200, v)
        vm.Regs[1] = v
    }
    // call base_holds/2
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["base_holds/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_holds/2", 2) { return true }; vm.warnUnresolved("call", "base_holds/2"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y3, A3
    vm.Regs[2] = vm.getReg(202)
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    // execute tight_rev_in/3 (tail call)
    return func() bool { if pc, ok := vm.Ctx.Labels["tight_rev_in/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("tight_rev_in/3", 3) { return true }; vm.warnUnresolved("call", "tight_rev_in/3"); return false }()

}

// PredTight_constraint1 — lowered from tight_constraint/1
func (vm *WamState) PredTight_constraint1() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // put_constant any, A2
    vm.Regs[1] = wamAtom_any_10
    // builtin_call \==/2 2
    if !vm.executeBuiltin("\\==/2", 2) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredTopo_all7 — lowered from topo_all/7 (T4 all-clauses inline)
func (vm *WamState) PredTopo_all7() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_variable X2, A3
        vm.Regs[101] = vm.Regs[2]
        // get_variable X3, A4
        vm.Regs[102] = vm.Regs[3]
        // get_value X3, A5
        if !vm.Unify(vm.Regs[4], vm.Regs[102]) {
            return false
        }
        // get_variable X4, A6
        vm.Regs[103] = vm.Regs[5]
        // get_value X4, A7
        if !vm.Unify(vm.Regs[6], vm.Regs[103]) {
            return false
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y1, A1
        vm.Regs[200] = vm.Regs[0]
        // get_list A2
        if !vm.Step(&GetList{Ai: 1}) {
            return false
        }
        // unify_variable X8
        if !vm.Step(&UnifyVariable{Xn: 107}) {
            return false
        }
        // unify_variable Y2
        if !vm.Step(&UnifyVariable{Xn: 201}) {
            return false
        }
        // get_variable Y3, A3
        vm.Regs[202] = vm.Regs[2]
        // get_variable X9, A4
        vm.Regs[108] = vm.Regs[3]
        // get_variable Y5, A5
        vm.Regs[204] = vm.Regs[4]
        // get_variable X10, A6
        vm.Regs[109] = vm.Regs[5]
        // get_variable Y7, A7
        vm.Regs[206] = vm.Regs[6]
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value X8, A2
        vm.Regs[1] = vm.getReg(107)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // put_value X9, A4
        vm.Regs[3] = vm.getReg(108)
        // put_variable Y4, A5
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
            vm.putReg(203, v)
            vm.Regs[4] = v
        }
        // put_value X10, A6
        vm.Regs[5] = vm.getReg(109)
        // put_variable Y6, A7
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 205), Idx: 205}
            vm.putReg(205, v)
            vm.Regs[6] = v
        }
        // call topo_one/7
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["topo_one/7"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("topo_one/7", 7) { return true }; vm.warnUnresolved("call", "topo_one/7"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // put_value Y4, A4
        vm.Regs[3] = vm.getReg(203)
        // put_value Y5, A5
        vm.Regs[4] = vm.getReg(204)
        // put_value Y6, A6
        vm.Regs[5] = vm.getReg(205)
        // put_value Y7, A7
        vm.Regs[6] = vm.getReg(206)
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        // execute topo_all/7 (tail call)
        return func() bool { if pc, ok := vm.Ctx.Labels["topo_all/7"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("topo_all/7", 7) { return true }; vm.warnUnresolved("call", "topo_all/7"); return false }()
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredTopo_one7 — lowered from topo_one/7
func (vm *WamState) PredTopo_one7() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_variable X2, A2
    vm.Regs[101] = vm.Regs[1]
    // get_variable X3, A3
    vm.Regs[102] = vm.Regs[2]
    // get_variable X4, A4
    vm.Regs[103] = vm.Regs[3]
    // get_value X4, A5
    if !vm.Unify(vm.Regs[4], vm.Regs[103]) {
        return false
    }
    // get_variable X5, A6
    vm.Regs[104] = vm.Regs[5]
    // get_value X5, A7
    if !vm.Unify(vm.Regs[6], vm.Regs[104]) {
        return false
    }
    // put_value X2, A1
    vm.Regs[0] = vm.getReg(101)
    // put_value X4, A2
    vm.Regs[1] = vm.getReg(103)
    // builtin_call member/2 2
    if !vm.executeBuiltin("member/2", 2) {
        return false
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredTopo_sort_sel3 — lowered from topo_sort_sel/3 (T4 all-clauses inline)
func (vm *WamState) PredTopo_sort_sel3() bool {
    _t4 := vm.LoClauseSnapshot()
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable X1, A1
        vm.Regs[100] = vm.Regs[0]
        // get_constant [], A2
        {
            _a := vm.deref(vm.Regs[1])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // get_constant [], A3
        {
            _a := vm.deref(vm.Regs[2])
            if _, ok := _a.(*Unbound); ok {
                u := _a.(*Unbound)
                vm.trailBinding(u.Idx)
                vm.Regs[u.Idx] = wamAtom____0
            } else if !valueEquals(vm.deref(_a), wamAtom____0) {
                return false
            }
        }
        // builtin_call !/0 0
        if !vm.executeBuiltin("!/0", 0) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    if func() bool {
        // allocate
        vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
        // get_variable Y2, A1
        vm.Regs[201] = vm.Regs[0]
        // get_variable Y4, A2
        vm.Regs[203] = vm.Regs[1]
        // get_variable Y7, A3
        vm.Regs[206] = vm.Regs[2]
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_variable Y1, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 200), Idx: 200}
            vm.putReg(200, v)
            vm.Regs[1] = v
        }
        // builtin_call sort/2 2
        if !vm.executeBuiltin("sort/2", 2) {
            return false
        }
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_variable Y3, A2
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
            vm.putReg(202, v)
            vm.Regs[1] = v
        }
        // call names_of/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["names_of/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("names_of/2", 2) { return true }; vm.warnUnresolved("call", "names_of/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // put_value Y4, A3
        vm.Regs[2] = vm.getReg(203)
        // put_constant [], A4
        vm.Regs[3] = wamAtom____0
        // put_variable Y5, A5
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
            vm.putReg(204, v)
            vm.Regs[4] = v
        }
        // put_constant [], A6
        vm.Regs[5] = wamAtom____0
        // put_variable Y6, A7
        {
            v := &Unbound{Name: fmt.Sprintf("_R%d", 205), Idx: 205}
            vm.putReg(205, v)
            vm.Regs[6] = v
        }
        // call topo_all/7
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["topo_all/7"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("topo_all/7", 7) { return true }; vm.warnUnresolved("call", "topo_all/7"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        // put_value Y6, A1
        vm.Regs[0] = vm.getReg(205)
        // put_value Y7, A2
        vm.Regs[1] = vm.getReg(206)
        // builtin_call reverse/2 2
        if !vm.executeBuiltin("reverse/2", 2) {
            return false
        }
        // deallocate
        if env := vm.popEnvFrame(); env != nil {
            vm.CP = env.CP
        }
        return true
    }() { return true }
    vm.LoRestoreClause(_t4)
    return false

}

// PredTree_lookup3 — lowered from tree_lookup/3
func (vm *WamState) PredTree_lookup3() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_structure t/4, A1
    if !vm.Step(&GetStructure{Functor: "t/4", Ai: 0}) {
        return false
    }
    // unify_variable Y3
    if !vm.Step(&UnifyVariable{Xn: 202}) {
        return false
    }
    // unify_variable X7
    if !vm.Step(&UnifyVariable{Xn: 106}) {
        return false
    }
    // unify_variable Y1
    if !vm.Step(&UnifyVariable{Xn: 200}) {
        return false
    }
    // unify_variable Y6
    if !vm.Step(&UnifyVariable{Xn: 205}) {
        return false
    }
    // get_variable Y4, A2
    vm.Regs[203] = vm.Regs[1]
    // get_variable Y5, A3
    vm.Regs[204] = vm.Regs[2]
    // put_variable Y2, A1
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[0] = v
    }
    // put_value Y4, A2
    vm.Regs[1] = vm.getReg(203)
    // put_value X7, A3
    vm.Regs[2] = vm.getReg(106)
    // builtin_call compare/3 3
    if !vm.executeBuiltin("compare/3", 3) {
        return false
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_constant =, A2
        vm.Regs[1] = wamAtom___9
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
            return true
        }()
        if _condOk {
        // put_value Y5, A1
        vm.Regs[0] = vm.getReg(204)
        // put_value Y1, A2
        vm.Regs[1] = vm.getReg(200)
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
        {
            _trailMark := vm.TrailLen
            _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
            _condOk := func() bool {
            // put_value Y2, A1
            vm.Regs[0] = vm.getReg(201)
            // put_constant <, A2
            vm.Regs[1] = wamAtom___7
            // builtin_call =/2 2
            if !vm.executeBuiltin("=/2", 2) {
                return false
            }
                return true
            }()
            if _condOk {
            // put_value Y3, A1
            vm.Regs[0] = vm.getReg(202)
            // put_value Y4, A2
            vm.Regs[1] = vm.getReg(203)
            // put_value Y5, A3
            vm.Regs[2] = vm.getReg(204)
            // call tree_lookup/3
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["tree_lookup/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("tree_lookup/3", 3) { return true }; vm.warnUnresolved("call", "tree_lookup/3"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
            } else {
                vm.Regs = _savedRegs
                vm.unwindTrailTo(_trailMark)
            // put_value Y6, A1
            vm.Regs[0] = vm.getReg(205)
            // put_value Y4, A2
            vm.Regs[1] = vm.getReg(203)
            // put_value Y5, A3
            vm.Regs[2] = vm.getReg(204)
            // call tree_lookup/3
            {
                savedCP := vm.CP
                if !func() bool { if pc, ok := vm.Ctx.Labels["tree_lookup/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("tree_lookup/3", 3) { return true }; vm.warnUnresolved("call", "tree_lookup/3"); return false }() {
                    return false
                }
                vm.CP = savedCP
            }
            }
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredUpgrade_set4 — lowered from upgrade_set/4
func (vm *WamState) PredUpgrade_set4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable X1, A1
    vm.Regs[100] = vm.Regs[0]
    // get_variable X2, A2
    vm.Regs[101] = vm.Regs[1]
    // get_variable X3, A3
    vm.Regs[102] = vm.Regs[2]
    // get_variable X4, A4
    vm.Regs[103] = vm.Regs[3]
    // put_value X1, A1
    vm.Regs[0] = vm.getReg(100)
    // put_value X2, A2
    vm.Regs[1] = vm.getReg(101)
    // put_value X3, A3
    vm.Regs[2] = vm.getReg(102)
    // put_structure ok/1, A4
    if !vm.Step(&PutStructure{Functor: "ok/1", Ai: 3}) {
        return false
    }
    // set_value X4
    if !vm.Step(&SetValue{Xn: 103}) {
        return false
    }
    // call upgrade_set_result/4
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["upgrade_set_result/4"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("upgrade_set_result/4", 4) { return true }; vm.warnUnresolved("call", "upgrade_set_result/4"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredUpgrade_set_result4 — lowered from upgrade_set_result/4
func (vm *WamState) PredUpgrade_set_result4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable X5, A2
    vm.Regs[104] = vm.Regs[1]
    // get_variable Y3, A3
    vm.Regs[202] = vm.Regs[2]
    // get_variable Y4, A4
    vm.Regs[203] = vm.Regs[3]
    // put_value Y1, A1
    vm.Regs[0] = vm.getReg(200)
    // put_value X5, A2
    vm.Regs[1] = vm.getReg(104)
    // put_variable Y2, A3
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 201), Idx: 201}
        vm.putReg(201, v)
        vm.Regs[2] = v
    }
    // call canonicalize_name/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["canonicalize_name/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("canonicalize_name/3", 3) { return true }; vm.warnUnresolved("call", "canonicalize_name/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // put_value Y3, A3
        vm.Regs[2] = vm.getReg(202)
        // call package_in/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["package_in/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("package_in/3", 3) { return true }; vm.warnUnresolved("call", "package_in/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_list A2
        if !vm.Step(&PutList{Ai: 1}) {
            return false
        }
        // set_variable X7
        if !vm.Step(&SetVariable{Xn: 106}) {
            return false
        }
        // set_constant []
        if !vm.Step(&SetConstant{C: wamAtom____0}) {
            return false
        }
        // put_structure -/2, X7
        if !vm.Step(&PutStructure{Functor: "-/2", Ai: 106}) {
            return false
        }
        // set_value Y2
        if !vm.Step(&SetValue{Xn: 201}) {
            return false
        }
        // set_value Y3
        if !vm.Step(&SetValue{Xn: 202}) {
            return false
        }
        // put_value Y4, A3
        vm.Regs[2] = vm.getReg(203)
        // call close_moving/3
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["close_moving/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("close_moving/3", 3) { return true }; vm.warnUnresolved("call", "close_moving/3"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y4, A1
        vm.Regs[0] = vm.getReg(203)
        // put_constant no_candidate, A2
        vm.Regs[1] = wamAtom_no_candidate_16
        // builtin_call =/2 2
        if !vm.executeBuiltin("=/2", 2) {
            return false
        }
        }
    }
    // builtin_call !/0 0
    if !vm.executeBuiltin("!/0", 0) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredVirtual_provider_ceilings4 — lowered from virtual_provider_ceilings/4
func (vm *WamState) PredVirtual_provider_ceilings4() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y3, A1
    vm.Regs[202] = vm.Regs[0]
    // get_variable Y2, A2
    vm.Regs[201] = vm.Regs[1]
    // get_variable Y7, A3
    vm.Regs[206] = vm.Regs[2]
    // get_variable Y1, A4
    vm.Regs[200] = vm.Regs[3]
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y3, A1
        vm.Regs[0] = vm.getReg(202)
        // put_value Y2, A2
        vm.Regs[1] = vm.getReg(201)
        // call package_in_name/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["package_in_name/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("package_in_name/2", 2) { return true }; vm.warnUnresolved("call", "package_in_name/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // builtin_call fail/0 0
        if !vm.executeBuiltin("fail/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        }
    }
    // put_variable Y4, Y4
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 203), Idx: 203}
        vm.putReg(203, v)
        vm.Regs[203] = v
    }
    // put_variable Y7, Y7
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 206), Idx: 206}
        vm.putReg(206, v)
        vm.Regs[206] = v
    }
    // put_variable Y5, Y5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 204), Idx: 204}
        vm.putReg(204, v)
        vm.Regs[204] = v
    }
    // put_value Y3, A1
    vm.Regs[0] = vm.getReg(202)
    // put_value Y2, A2
    vm.Regs[1] = vm.getReg(201)
    // put_value Y4, A3
    vm.Regs[2] = vm.getReg(203)
    // put_value Y5, A4
    vm.Regs[3] = vm.getReg(204)
    // put_variable Y6, A5
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 205), Idx: 205}
        vm.putReg(205, v)
        vm.Regs[4] = v
    }
    // call provides_for/5
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["provides_for/5"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provides_for/5", 5) { return true }; vm.warnUnresolved("call", "provides_for/5"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // put_value Y3, A1
    vm.Regs[0] = vm.getReg(202)
    // put_value Y4, A2
    vm.Regs[1] = vm.getReg(203)
    // put_value Y5, A3
    vm.Regs[2] = vm.getReg(204)
    // call base_ver/3
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["base_ver/3"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("base_ver/3", 3) { return true }; vm.warnUnresolved("call", "base_ver/3"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y6, A1
        vm.Regs[0] = vm.getReg(205)
        // put_value Y7, A2
        vm.Regs[1] = vm.getReg(206)
        // call provide_satisfies/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["provide_satisfies/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("provide_satisfies/2", 2) { return true }; vm.warnUnresolved("call", "provide_satisfies/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // builtin_call fail/0 0
        if !vm.executeBuiltin("fail/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        }
    }
    // put_structure blocked/3, A1
    if !vm.Step(&PutStructure{Functor: "blocked/3", Ai: 0}) {
        return false
    }
    // set_value Y4
    if !vm.Step(&SetValue{Xn: 203}) {
        return false
    }
    // set_variable _XT0
    if !vm.Step(&SetVariable{Xn: 0}) {
        return false
    }
    // set_variable _XT1
    if !vm.Step(&SetVariable{Xn: 0}) {
        return false
    }
    // put_structure needs/1, _XT0
    if !vm.Step(&PutStructure{Functor: "needs/1", Ai: 0}) {
        return false
    }
    // set_value Y7
    if !vm.Step(&SetValue{Xn: 206}) {
        return false
    }
    // put_structure base_has/1, _XT1
    if !vm.Step(&PutStructure{Functor: "base_has/1", Ai: 0}) {
        return false
    }
    // set_value Y5
    if !vm.Step(&SetValue{Xn: 204}) {
        return false
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

// PredWorth_indexing2 — lowered from worth_indexing/2
func (vm *WamState) PredWorth_indexing2() bool {
    // allocate
    vm.Stack = append(vm.Stack, &EnvFrame{CP: vm.CP, B0: len(vm.ChoicePoints)})
    // get_variable Y1, A1
    vm.Regs[200] = vm.Regs[0]
    // get_variable Y2, A2
    vm.Regs[201] = vm.Regs[1]
    // put_variable Y3, A1
    {
        v := &Unbound{Name: fmt.Sprintf("_R%d", 202), Idx: 202}
        vm.putReg(202, v)
        vm.Regs[0] = v
    }
    // call index_threshold/1
    {
        savedCP := vm.CP
        if !func() bool { if pc, ok := vm.Ctx.Labels["index_threshold/1"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("index_threshold/1", 1) { return true }; vm.warnUnresolved("call", "index_threshold/1"); return false }() {
            return false
        }
        vm.CP = savedCP
    }
    // if-then-else (lowered from try_me_else/cut_ite/jump/trust_me)
    {
        _trailMark := vm.TrailLen
        _savedRegs := vm.Regs   // Regs is a fixed array; assignment copies it
        _condOk := func() bool {
        // put_value Y1, A1
        vm.Regs[0] = vm.getReg(200)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // call long_enough/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["long_enough/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("long_enough/2", 2) { return true }; vm.warnUnresolved("call", "long_enough/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
            return true
        }()
        if _condOk {
        // builtin_call true/0 0
        if !vm.executeBuiltin("true/0", 0) {
            return false
        }
        } else {
            vm.Regs = _savedRegs
            vm.unwindTrailTo(_trailMark)
        // put_value Y2, A1
        vm.Regs[0] = vm.getReg(201)
        // put_value Y3, A2
        vm.Regs[1] = vm.getReg(202)
        // call long_enough/2
        {
            savedCP := vm.CP
            if !func() bool { if pc, ok := vm.Ctx.Labels["long_enough/2"]; ok { vm.PC = pc; return vm.Run() }; if vm.executeBuiltin("long_enough/2", 2) { return true }; vm.warnUnresolved("call", "long_enough/2"); return false }() {
                return false
            }
            vm.CP = savedCP
        }
        }
    }
    // deallocate
    if env := vm.popEnvFrame(); env != nil {
        vm.CP = env.CP
    }
    return true

}

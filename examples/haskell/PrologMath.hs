{-# LANGUAGE BangPatterns #-}
-- Generated by UnifyWeaver Haskell Target
module PrologMath
    ( sumTo, factorial, fib
    ) where

import Prelude hiding (sum)

-- | sumTo (tail recursion)
sumTo :: Int -> Int -> Int
sumTo 0 !acc = acc
sumTo n !acc = sumTo (n - 1) (acc + n)

-- | factorial (factorial)
factorial :: Int -> Int
factorial 0 = 1
factorial 1 = 1
factorial n = n * factorial (n - 1)

-- | fib (linear recursion)
fib :: Int -> Int
fib 0 = 0
fib 1 = 1
fib n = fib (n - 1) + fib (n - 2)

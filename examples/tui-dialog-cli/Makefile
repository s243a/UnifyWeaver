.PHONY: run clean install

# Run the TUI application
run:
	@bash dialog_cli.sh

# Make scripts executable
install:
	@chmod +x dialog_cli.sh
	@chmod +x run.sh
	@echo "Scripts are now executable"

# Clean generated files (if any)
clean:
	@echo "Nothing to clean"

// Generated by UnifyWeaver TypeScript Target
// Pattern: tail_recursion

export const sum = (n: number, acc: number = 0): number => {
  if (n <= 0) return acc;
  return sum(n - 1, acc + n);
};

// Strict version for guaranteed TCO
export const sumStrict = (n: number, acc: number = 0): number => {
  let current = n;
  let result = acc;
  while (current > 0) {
    result += current;
    current--;
  }
  return result;
};

// Test
console.log('[TEST] sum(10) =', sum(10));
console.log('[TEST] sum(100) =', sum(100));
console.log('[TEST] sumStrict(100) =', sumStrict(100));
console.log('[PASS] All tests completed');

<!-- SPDX-License-Identifier: MIT OR Apache-2.0 -->
<!-- Copyright (c) 2026 John William Creighton (s243a) -->

# JS-family targets — parity punchlist

**Living document.** Tracks the gap between the JavaScript-family targets
(TypeScript, annotated JS, vanilla JS, ClojureScript, and the `wam_javascript`
hybrid) and the mature reference targets (python/rust/go for pattern; rust/haskell/
lua/cpp for WAM). Built to be worked down item by item and to double as a status board.

**Last updated:** 2026-08-30 (some OPEN rows are pending confirmation from two
in-flight parity analyses — see §6).

## Legend

| Field | Values |
|---|---|
| **Status** | ✅ done · 🔄 in-progress · ⬜ open · ❓ suspected (needs analysis to confirm) |
| **Owner** | `grok` (JS WAM runtime/emitter lane) · `opus` (disjoint pattern work) · `main` (INT-0 wiring/merges) · `—` (unassigned) |
| **Size** | S (hours) · M (day) · L (multi-day / research) |
| **Collision** | which hot files it edits — `runtime.js.mustache`/emitter are actively edited by Grok's indexing work, so ⚠️ marks WAM items that must serialize behind it |

---

## 1. Completed this session ✅ (done ledger)

| ID | Target | Item | Merged |
|---|---|---|---|
| D1 | typescript | Bindings 154 → 179 (Map/Set, Number formatting) | PR #4177 |
| D2 | typescript | Fixed 3 codegen bugs (empty fact array, leaked `generate_match_expr`, `transitive_closure` fallthrough) | #4177 |
| D3 | annotated_js | New pattern target (inherits TS, TS→JSDoc rewrite; `tsc --checkJs` clean) | #4177 |
| D4 | vanilla_js | New pattern target (inherits TS, centralized type-strip; node-verified) | #4177 |
| D5 | clojurescript | `runtime(scittle\|nbb\|bb)` variants; 67-binding `clojurescript_bindings.pl` | #4177 |
| D6 | clojurescript / clojure base | Numeric recursion codegen fix (base-level; also improves JVM Clojure) | #4177 |
| D7 | (infra) | PAR-1 pattern-target parity harness | #4177 |
| D8 | wam_javascript | Interpreter tier + six WAM conventions + 48/48 conformance arm | #4177 |
| D9 | wam_javascript | Builtins: findall/functor/arg/=../copy_term/\+/call/aggregate_all | #4177 |
| D10 | wam_javascript | Full ISO bagof/3 + setof/3 (witness grouping, `^`, empty-fail, std order) | PR #4178 |
| D11 | (infra) | Registry wiring + BINDING_MATRIX rows for the new targets | #4177/#4178 |
| D12 | wam_javascript | First-argument indexing (`switch_on_constant/structure/term`; ground dispatch leaves no CP, var-first-arg falls back to try/retry/trust) | Grok `grok/wamjs-indexing` |
| D13 | vanilla_js | Registered all 7 advanced-pattern hooks (delegate to TS + type-strip); `target(vanilla_js)` now dispatches (was: no clause → fail). G-P10. | (this branch) |
| D14 | typescript (→AJS/VJS) | Real recursion (G-P1): tree/multicall/direct/tail hooks now derive base cases + offsets + aggregation from the actual clause (was: hardcoded fib for any predicate). Structural recursion (G-P2): native list lowering for member/append/reverse/length, node-verified vs SWI. | PR #4182 |
| D15 | wam_javascript | Switch-indexing cross-target matrix row added (G-W5): full coverage of all 5 tracked columns (const_ft/const_a2/const_a2_ft/term_a2/struct_a2), residuals documented. | (this branch) |
| D16 | typescript (→AJS/VJS) | Component emission (G-P4): `compile_module` now calls `compile_component` (was collected-but-dropped); revived orphaned `custom_chart` (+ fixed its unevaluated-`format` bug). Component-free modules unchanged. | (this branch) |
| D17 | clojurescript | Wired the `clojurescript` binding key (G-P11): `resolve_binding([clojurescript, clojure], …)` fallback-with-override; loaded both catalogues; call-head rewrite after interop (no double-transform); bb skips it. `parse_double→js/parseFloat` proof, nbb-verified, JVM regression green. Bindings 67→68. | (this branch) |
| D18 | typescript (→AJS/VJS) | Aggregate compilation (G-P3): `aggregate_all` (count/sum/max/min/bag/set) + `findall` as goals (was 0 refs → 50); node-verified vs SWI. Follow-up: bagof/setof at pattern level + non-extensional inner goals. | PR #4184 |
| D19 | clojure + clojurescript | Component pattern wired into the clojure base (G-P5): loads `component_registry` + revived orphaned `custom_clojure`, emits declared components (both JVM Clojure and CLJS via interop rewrite). CLJS `compile_module/3` added (G-P6). nbb-verified (module + component); JVM regression green. | PR #4185 |
| D20 | wam_javascript | **Tier-2 lowered emitter (G-W1, Grok):** new `wam_javascript_lowered_emitter.pl` + `functions`/`mixed` emit modes. Lowers single-clause det bodies, T4 inline, T5 first-arg dispatch, T6 hash dispatch (≥8 keys), ITE/negation/once; falls back to interpreter for aggregates/cuts/unsupported (never wrong code). Interpreter mode unchanged → conformance still 48/48. JS is no longer one of the 4 WAM targets without a lowered emitter. | PR #4186 |
| D21 | typescript (→AJS/VJS) | Streaming/generator emit mode (G-P8): `mode(generator)`/`mode(pipeline)` (+ clojure-style aliases) via Node built-in `readline` (no deps); incremental stdin→stdout. Batch mode unchanged; falls back to batch for non-qualifying shapes. node-verified across TS/AJS/VJS vs SWI. | PR #4188 |
| D22 | annotated_js, vanilla_js | Rewriter hardening (G-P14): vanilla strips union types (incl. `\| null`); annotated keeps `import * as`/named-alias imports intact + strips inline arrow-param annotations; union return → `@returns` JSDoc. node + `tsc --checkJs` verified; base TS suite unaffected. | PR #4189 |
| D23 | wam_javascript | ISO/library builtin breadth (G-W3, Grok): sort family (sort/2,4, msort, keysort, compare, predsort), list lib (append/reverse/nth0/nth1/last/sum_list/max_list/min_list/list_to_set/select/include/exclude), atom/string (atom_concat/length/chars/codes, char_code, sub_atom, up/downcase, atom_string/split_string/string_concat), format/2,3 (~w~a~d~p~q~n~s~t~~), tab, writeln, assoc (list-based). node-verified vs SWI; conformance 48/48. Follow-up: term_variables/numbervars/=@=, ~f/~r, real string tag, AVL assoc. | PR #4190 |
| D24 | typescript + clojure (→AJS/VJS/CLJS) | Guard-goal codegen (G-P7): added negation (`\+`/`not`) and type-check preds (integer/float/number/atom/is_list/compound/var/nonvar/ground) to `ts_guard_condition/3` + `clojure_guard_condition/3` (+ `member/2` under negation). Was: classified as guard upstream then render-failed. node+nbb verified vs SWI; 5 suites green (TS 57/AJS 17/VJS 22/CLJS 26/JVM 12). Regex `match` = follow-up. | PR #4192 |
| D25 | wam_javascript | Richer runtime term parser (G-W2, Grok): Pratt reader for ints/floats/atoms (bare+quoted)/vars/lists (`[H\|T]`)/compounds, ISO default op table (`1+2`→`+(1,2)`); CLI pads missing args with unbound vars; `read_term_from_atom`/`atom_to_term`/`term_to_atom` builtins. Registered `native(parse_term)` in `wam_runtime_parser_capability.pl` (INT-0). node-vs-SWI CLI (list→S=6, compound→X=a, float 3.14>3.0); conformance 48/48. Follow-up: user `op/3`, postfix ops. | PR #4193 |
| D26 | typescript (→AJS/VJS) | Data-source consumer (G-P9, Option A): TS now consumes JSON + CSV sources via `_typescript` templates (`json_source.pl`/`csv_source.pl`) + new `typescript_source_compiler.pl` wrapper + one routing clause on `is_dynamic_source/1`. Self-contained Node (fs, no npm). node output matches jq/awk; existing bash/PowerShell-pure paths untouched. Follow-up: non-comma CSV delimiters, columns/schema projection. | PR #4194 |
| D27 | wam_javascript | External fact sources (G-W4, Grok): `javascript_wam_fact_sources([source(P/2, file(Path))])` (alias `js_fact_sources/1`) emits `CallFactStream` instead of inline WAM; Node runtime reads TSV/CSV/JSONL via `fs` (no npm), first-arg indexed when A1 bound; cells parsed via `parse_term`. Inline-facts unchanged. node-vs-SWI on shared triples; conformance 48/48. LMDB/CSR out of scope (matches Lua peer). | PR #4196 |
| D28 | typescript + clojure (→AJS/VJS/CLJS) | Regex `match/2,3` guard codegen (G-P7 completion): `match(Subject,Pattern)` → TS `new RegExp(pat).test(x)`, Clojure `(re-find (re-pattern pat) x)`; `match/3` regex-type arg advisory (uses host engine). String-input typing for match-on-arg1; `\+ match` composes. node/nbb-vs-SWI (pcre oracle). Optional INT-0 follow-up (in patch): a 2-line `is_guard_goal` addition to make bare positive `match` a batch guard. | PR #4198 |
| D29 | wam_javascript | Term-meta builtins (G-W3 follow-up, Grok): `term_variables/2` (distinct unbound, first-occurrence order), `numbervars/3` (`'$VAR'(N)` from Start, End=Start+count), `=@=/2`/`\=@=/2` (variant equality). Reuses `copy_term`'s deref/seen-struct walk. node-vs-SWI matched; conformance 48/48. Residual: `write` prints `'$VAR'(N)` literally (no A/B letter-render), no `numbervars/4` options/attvars. | PR #4199 |
| D30 | typescript + clojure (→AJS/VJS/CLJS) | Uniqueness/order constraints (G-P-dedup): `compile_facts` now consults `constraint_analyzer:get_constraints/2` — `unique(true)` emits order-preserving/sorted dedup (TS `Set`+`JSON.stringify`; Clojure `distinct`/`sort`), `unique(false)` raw; mirrors rust/go. Covers the facts/query-helper shape (inherited by AJS/VJS/CLJS); recursion/aggregate/streaming shapes deferred (no single materialized site). node/nbb-vs-SWI `setof`; 5 suites green. | PR #4201 |
| D31 | typescript (→AJS/VJS) | Data-source polish (G-P9 follow-up): non-comma CSV delimiters (tab/pipe/… via `String.split` string-literal, `js_escape_delimiter/2` — no regex pitfalls; bash/PS `delimiter` unchanged) + JSON `columns()` projection with reorder (dotted/`jsonpath` paths). node-vs-`awk` verified. Deferred (blocked-upstream, documented): CSV subset/reorder projection + JSON `schema` (need `sources.pl`/`compile_source` changes that'd alter bash/PS output). | PR #4202 |
| D32 | wam_javascript | User-defined operators `op/3` (G-W2 follow-up, Grok): live Pratt operator table = ISO defaults + user declarations. Infix (xfx/xfy/yfx), prefix (fx/fy), postfix (xf/yf); priority 0 removes; atom or list of names; ISO coexistence rules. Compile-time `:- op/3` threaded via `javascript_wam_ops/1` (alias `js_op_decls/1`) → `install_declared_ops` at startup; runtime `op/3` builtin. node-vs-SWI (custom infix/prefix/postfix + emit-time decls); conformance 48/48. Residual: no `current_op/3`; process-global (like SWI). | (this branch) |
| D33 | typescript (→AJS/VJS) | CSV subset/reorder projection (G-P9 residual, unblocks D31): new additive `project_columns([Name,…])` option — distinct from arity-defining `columns` — resolves names→0-based indices against the file's detected header at compile time (`resolve_projection/5`) and selects/reorders exactly those fields in the `_typescript` templates (`[fields[i],fields[j]].join(":")`; single-col arity-1 too). Pass-through defaults keep no-projection TS **byte-identical**; bash/PS never reference the vars → unchanged. Unknown name / arity mismatch fails with a clear error. node-vs-expected verified; 15 TS-source tests green (4 new). | (this branch) |
| D34 | wam_javascript | Real string type tag (G-W3 residual, Grok): distinct `V.String` term tag in the runtime term model — `unify`/`==`/`copy_term`/`compare`/`sort`/`write` all handle it; a string unifies only with an equal string, not an atom of the same text. String-producing builtins now box strings (`atom_string/2`, `string_concat/3`, `string_chars/2`, `string_to_atom/2` A1, `number_string/2`, `split_string/4`); `atom_concat`/`atom_chars` stay atoms; `string/1` true only for the tag, `atom/1` false. Standard order matches **SWI 9.0.4** (`Var<Number<String<Atom<Compound`; `"foo" @< foo`). node-vs-SWI probes + `probe_string_tag` green; conformance still 48/48; GitHub CI 16/16. Residuals: compiled `"foo"` literals collapse to atom (shared WAM constant tokens — needs `wam_target.pl`); `writeq`/`~q` nested-string quoting; fact-source cells stay atoms; no `string_length/2`. | (this branch) |
| D35 | wam_javascript | String polish (D34 residuals, Grok): `string_length/2` (string/atom/number → char length) + `writeq/1` and a recursive quoted writer reused by `~q` — strings render `"…"`, atoms single-quoted only when needed. Quoting subset: unquoted `[]`/`!`/`;`/`{}`/`^[a-z][A-Za-z0-9_]*$`/ISO graphic atoms (except lone `.`), quoted otherwise; escapes `\\`/`\'`. `write/1` unchanged. node-vs-SWI + `probe_string_polish` green; builtins/lowered/fact-sources green; conformance 48/48. Residuals: compiled `"foo"` literals still collapse (out of scope, `wam_target.pl`); fact-source cells stay atoms; no `\n`/`\t` escapes in `writeq`; brace terms `{a}` not special-cased. | (this branch) |
| D36 | typescript (→AJS/VJS) | JSON `schema` mode for the Node consumer (G-P9 "Option-B" residual, Opus): the TS/Node JSON consumer now honors a declared `schema(...)` — reads `dynamic_source_metadata/2`'s `schema_fields` (per-field `name`/`column_type`/`path`) and emits a **typed record object** per JSON record (`JSON.stringify` of `{field:value,…}` in declared order, coerced string→String / int→trunc Number / float→Number / boolean→bool / json→passthrough / null→null), the parity the C# target already had. New `_typescript_schema` templates + `json_schema_projection_js/2`; wrapper routes via non-empty `schema_fields`, else falls through to flat-columns unchanged. Only non-additive edit = one render-var line; bash/PS byte-identical; flat-columns TS untouched (2 control tests). New isolated suite `test_typescript_json_schema` 6/6 green (2 node-exec); `test_typescript_source`/`json_source_validation`/`csv_source` green. | (this branch) |
| D37 | wam_javascript (+ shared WAM) | Compiled string literals → JS string tag (D34/D35 residual, Grok; **2 shared files**): `wam_target:quote_wam_constant/2` now spells a Prolog `string` value as a double-quoted `"…"` token (atoms/numbers unchanged); `wam_text_parser:wam_classify_constant_token/2` strips the quotes and **still returns `atom(Name)`** (no new `string(_)` Class — every other runtime interns the atom exactly as before) plus a new exported `wam_constant_token_is_string/1` signal. JS-only consumers (`wam_javascript_target:constant_to_js_term/2`, lowered emitter T5/T6) build a `V.String` when the signal is true, so a compiled `lit("hi")`/`X = "hi"` yields a real string. `probe_string_literal` green; **full-fleet conformance ALL_PASS** (compiled+ran python/rust/c/cpp/go through the changed shared code, no regression) + JS arm 48/48. Residual: fact-source JSON/TSV cells still intern as atoms. | (this branch) |
| D38 | wam_javascript | Runtime profiling (GP-PROF, Grok): opt-in instrumentation, off by default (`Runtime._prof=null`; unprofiled loop byte-identical to pre-GP-PROF). `UW_PROFILE=1` → compact stderr table, `UW_PROFILE=json` → stable-schema JSON on stderr, `Runtime.profile(...)` programmatic. Per-predicate (exclusive): calls/instr/ns/cps/max_cp_depth/lowered; global: instructions/unify_calls/trail_pushes/heap_cells/backtracks/trail_undos/wall_ns. Lowered tier = call counts only (marked `*`). stdout byte-identical on AND off (report → stderr). Overhead: off ≈ noise (±1%), on ≈ 6–10%. 3 probes + lowered call-count check green; suites + JS-arm conformance green. | (this branch) |
| D39 | (fleet docs) | **WAM fleet deficiency-class documentation** (Fable lead): new `docs/WAM_FLEET_GAPS.md` (target × class matrix, verified/suspected with file:line evidence, `examples/cli_args/` as the fleet benchmark recipe) + 16 per-target `WAM_*_STATUS.md` updated + `WAM_ELIXIR_STATUS.md` created + `WAM_BACKEND_CONVENTIONS.md` §7 (Execute-of-builtin must take Proceed's return path) & §8 (never assume Allocate framing protects Y — X-window aliases into Y space). Key verified findings: the A2 "Y-clobber" is precisely X-window overflow aliasing (X101≡Y1) — named-register runtimes immune, numeric flat/frame runtimes (haskell/python/go/lua/scala/r/elixir) exposed; `sub_string/5` missing everywhere but cpp/JS; **wat's Execute-of-unknown jumps to PC 0** (control-flow corruption, worst cell); **haskell's pattern fact-fallback has the exact G-A3-8 execute-the-predicate hazard**; wam_lua/wam_clojure have no conformance arms at all. | (this branch) |
| D40 | clojurescript (+clojure base) | **ClojureScript is the FOURTH build carrying the entire argparser** (`examples/cli_args/cljs/`, Opus): all 43 predicates → one CLJS namespace, nbb-loads clean, **corpus 17/17 contract points / 25/25 argv-lines (error messages exact)**, **differential 5067 lines, 0 divergences, 0 mismatches, 0 crashes**. The G-A3 machinery ported to Clojure idiom: vector tuples, map compounds (`{:$ tag :args […]}`), fresh-`Object` sentinel with `identical?`, structural `=` (no `_uwEq` needed — verified `(= true "true")` false), `(declare …)` for forward refs; the expression language collapsed four TS statement mechanisms into the item fold. Both call-graph fixpoints reproduce the TS lane's answers EXACTLY (same nine sentinel predicates — pinned). Emission discipline: 45/46 byte-identical, 1 change (`declare`, required). Recursion = direct calls with MEASURED headroom (~77×/~100× beyond corpus bounds; `recur` noted as the limit for unbounded input). Suites: cljs 8/8 baseline + new 66-test shapes suite + full TS bar green. | (this branch) |
| D41 | wam_javascript | Performance round 1 (GP-PERF, Grok): argparser builds in **mixed mode** — T4+ITE lowering with depth-aware nested-`\+` fold, direct `Runtime.op_*` emit, cheap `==/2` ITE, ground-fact `copy_term` intern (`program.ground_memo`, trail-safe: no unbound cells → never trailed). **1.48× on the 5067-line differential** (their box; 1.58× on ours), interpreter instructions 388,834 → 20,667 on the 200-line profile; corpus 17/17 + differential 0/0 + all suites + conformance unchanged — the gates held as semantics regression tests. Stretch ≥5× NOT met, root cause identified honestly: the lowered tier's own full-register `copy_table` per clause-fail and `snapshot_machine` for unifying ITE conditions are the new cost; also Execute-of-a-non-self-user-predicate can't lower yet (`cp=0` steals the query continuation — kept interpreted rather than wrong). Next lever (GP-PERF-2): correct Execute-of-user via nested `run_isolated` without `cp=0`, then A-reg-only snapshots for helpers. | (this branch) |
| D42 | wam_javascript | Performance round 2 (GP-PERF-2, Grok): the whole argparser now lowers — **zero interpreter fallbacks** (all 43 preds incl. `parse_args/2` wrappers, `drop_brackets/2`, `substring_*`; 200-line profile `instr=0`). Correct **Execute-of-user** path: lowered callee = JS `return` (CP untouched); interpreted callee via `execute_user_isolated` (save CP, isolate with `cp=0` so the inner Proceed halts, restore) — continuation-integrity probes permanent. **Liveness-gated Y-save** (blanket save was a measured 4.98s regression → save iff a Y is live after the Call); nil/cons + lite-ITE snapshot cuts; direct JS Call/Execute. **Cumulative 2.12× vs A2** (4.639→2.187s their box; 8.90→3.49s ≈ 2.55× ours); round target ≥3× vs R1 not met (1.50×) — remaining cost is the lowered tier's own snapshots (unbound-A1 T4 register copy, `snapshot_lite` for unifying ITE, Y `allocate` on recursive Call). Semantics gates all unchanged: corpus 17/17, differential 0/0, suites + JS-arm conformance green. Named next levers: read-only-ITE snapshot skip, emit-time `default_registry` intern. | (this branch) |
| D43 | wam_javascript | **Indexed persistent fact stores — dual backend (GP-LMDB, Grok; closes the roadmap)**: `source(P/N, indexed(Prefix))` = dependency-free seek-based store (UWFI/UWIX format: length-prefixed records + sorted-key table binary-searched via `fs.readSync`; **bound-A1 lookup reads 506 of 992,872 bytes in 29 seeks** — proof reproduced locally, and the probe requires bytes_read>200 so a prefix mis-parse can't fake it); `source(P/N, lmdb(Dir))` = real LMDB via the `lmdb` npm package, lazily loaded, **the fleet's first optional-dependency tier** (opt-in per declaration; absence = one loud actionable error, never a silent cross-backend swap; no repo package.json entry — the gated arm actually RAN in Grok's env via /tmp-prefixed install). One `CallFactStream` path; D27 `file(...)` byte-identical; identical semantics probed across B and A vs SWI; typed index keys preserve D34 atom/string/number distinctions. Builders: `scripts/js_wam/uw_fact_index.js` / `uw_fact_lmdb.js`. All suites + conformance + corpus 17/17 + differential 0/0 green. Residual (stated, not started): secondary indexes, write paths, CSR. This store is the designated catalog backend for uw-resolve P2. | (this branch) |
| D44 | wam_javascript + (new program) | **uw-resolve P0/P1 (Grok): the frozen-base package resolver — the SECOND demonstration program, and the first that BACKTRACKS.** `examples/pkg_resolver/`: catalog-as-term model (no assert/retract), four queries (`resolve`, `resolve_layered` + `explain_blocked` with exact `blocked(Dep, needs(C), base_has(V))` ceilings, topo-ordered `layer_closure`, `removal_orphans`), 22-scenario contract corpus, wamjs build 23/23 vs SWI, **2200-case seeded differential 0 divergences** (~3 ms/case mixed). Backtracking found 3 real wamjs bugs the det argparser couldn't: ground-fact memo mis-pinned projections (`get_value`/`get_variable` wrongly intern-once); T4 lowering stole search CPs from naked `member/2`; and **`!` wiped ALL choice points instead of cutting to the Call barrier** — a core cut-semantics bug invisible to 48-query conformance, fixed with `cut_barrier` + 3 permanent probes. All prior gates re-verified green (JS suites, conformance, cli_args 17/17 + 5067/0/0). Residuals: epoch/tilde versions, Provides/virtual, GP-LMDB catalog (P2), pkg CLI, write paths. | (this branch) |
| D45 | wam_javascript (+ conventions) | **Cut/choice-point semantics audit + hardening (Opus): 12 divergences fixed, barrier model written down.** Headliners: `Execute` (LCO) never rebased `B0` — a `!` in ANY tail-called predicate pruned its CALLER's alternatives (D1); plain disjunction lowered as if-then-else, silently deleting the retry branch (D7); aggregate inner-`!` popped the aggregate CP and failed the whole findall (D3); three hand-rolled CP literals omitted barrier state so backtracking DELETED the barrier regime (D9); lowered ITE never cut its condition's CPs (D8); plus call/1 control-construct metacalls, frame-balance and cp fixes, unresumable-CP drops, and a principled generalized refuse-to-lower taint walk (nondet builtins = exactly what pushes CPs; multi-clause callees; can-yield-many; commit-less disjunction). New `tests/test_wam_javascript_cut_semantics.pl`: **35 SWI-oracled probes × 4 emit modes, all pass** + 5 emitter-decision tests. Normative barrier-model section in the status doc; **conventions §9 adopted** (cut is a barrier, never a stack wipe; both call AND execute set B0; probe-corpus requirement — 48-query conformance provably doesn't exercise this). ~+5% resolver-differential cost (4 predicates correctly refuse to lower) taken deliberately. All gates re-verified locally: cut suite, 3 JS suites, conformance, resolver 22/22+23/23+2200/0, cli_args 17/17+5067/0/0 (frozen build byte-identical). | (this branch) |
| D46 | (uw-resolve) | **P0.5 — graduated freeze semantics + mining adoptions (Grok).** `catalog/9` (back-compat: `catalog/6` accepted, bare holds = `blanket`; all 22 P0 scenarios unchanged-and-green as a gate). `base(Pkg-Ver, Reason)` with `safe_upgrade` verdicts (`safe(cost(R))`/`coordinated(Set)`/`unsafe(modified)`/`no_candidate`), `upgrade_set` = minimal reverse-dep closure over base holds whose constraints break at NewVer (tolerant deps stay OUT — minimality pinned), `freeze_audit` with `over_frozen/1` + `suggest(abi_anchor)` (the TrixiePup diagnosis as a query). Named layers (`layer(Name, Members)`) participate in layered resolve/closure/explanations. Mining adoptions: `excluded` filters candidate generation ONLY (never removal — the Pkg conflation pinned by test), `dependents`/`dependents_installed`, `alias` at the request edge. 16 new scenarios; SWI 38/38; wamjs corpus 39/39; differential **2400 cases / 10 queries / 0 divergences** — re-verified HERE on the D45-hardened runtime (wamjs leg rebuilt at merge; the barrier machinery's first fresh 2400-case backtracking shakeout). No shared files touched; no runtime bugs surfaced. Residuals: P2 GP-LMDB catalog, Provides/virtual, epoch/tilde, pkg CLI, write paths, per-file layer modeling. | (this branch) |
| D47 | (uw-resolve, CLI) | **`pkg` — the Pkg-style CLI, dogfooding BOTH demonstration programs at once (Opus).** `examples/pkg_resolver/cli/`: the command grammar IS a `cli_args` registry term (`pkg_schema.pl`, one `schema(...)` per command with shared `--catalog`/`--json` options), parsed by the TRANSPILED argparser (`parseArgs(argv, registry)` — the `/3` registry path, byte-identical `cliArgs.mjs`), dispatched to the P0.5 wamjs resolver shim: **zero parse logic and zero resolve logic hand-written in JS**, with ONE documented exception (`pkg deps` is a raw catalog projection — filter+sort, no alias rewriting). 10 commands (resolve, install-plan/layer, why-blocked, deps, what-needs [--installed], orphans, why-frozen, audit, safe-upgrade), exit codes 0/1/2 (success / query-false-or-blocked / usage — usage strings all compiled-argparser-produced). `derive.pl` regenerates registry JSON, both example catalogs (teaching `catalog/6` + frozen-base `catalog/9`), and an SWI-oracled expected corpus into `generated/`. **86/86 node tests** (registry fidelity, per-command contract vs SWI expectations, exit codes, `--json` shapes, usage-error wording); live spot-checks reproduce the transcript (audit: 5 holds / 1 over-frozen / 1 suggested reason; why-blocked firefox-esr: 2 ceilings, exit 1). Regression bar untouched-and-green (resolver 38/38 + 39/39 + differential 0, cli_args 17/17 + 5067/0/0, JS suites + conformance). Residual: P2 store-backed `--catalog` (Grok round in flight owns everything outside `cli/`); `search`/repo-ingestion commands deferred behind it. | (this branch) |
| D48 | (uw-resolve, stores) | **P2 — store-backed resolution: the D43 stores become the resolver's catalog backend (Grok; closes the P2 residual).** JSONL catalog dump (`kind`-discriminated rows, `catalog` field = store key prefix so many catalogs share one index) compiled by `store/rich_to_p2.mjs` + `uw_fact_index.js` into four `indexed(Prefix)` stores — `pkg`/`dep`/`conflict` keyed `CatId\|Name`, plus a build-time precomputed `revdep` store keyed `CatId\|DepName` so `dependents`/`upgrade_set` are seeks, not scans (compounds packed into scalar a2 cells: `Ver#Dep#Constraint`). `resolver_store.pl` adapter exposes the same 10 queries with per-name lookups served by fact-source predicates; `resolver.pl`'s term API untouched (every prior gate unchanged). SWI oracle reads the SAME JSONL the indexer consumes (no UWFI reader in SWI — identical rows both engines). Environment split documented: shared huge catalog in stores, machine-local `env(CatId, Base, Installed, Requested, Layers, Excluded, Aliases)` stays a term — the one-distro-index/many-machines shape. **Scale proof re-verified at merge**: 5,000 pkgs / 15,000 edges (seed `0xc0ffee01`), store 1,645,394 bytes; bound `resolve_layered` reads **8,242 bytes = 0.50%** of the store (650 reads); SWI store-total 0.537s beats term-total 0.701s with load included, results equal. Gates all re-run HERE: term corpus 38/38 + store corpus 39/39-identical (SWI and wamjs arms), term differential 2400/0/0, **store-backed differential 500/0/0 on the 5k catalog**, cut/lowered/builtins/fact_sources suites, both conformance suites, cli_args 17/17 + 5067/0/0, CLI 86/86 (on the updated build), lmdb ungated arm (gated arm ran in Grok's env; skips cleanly here). Bonus: Grok's rebuild fixed 3 mojibake comment lines my D46 rebuild introduced in `wamjs/js/wam_runtime.js`. No runtime fixes forced. Deferred README `cli/` pointer landed. Residuals: Provides/virtual, epoch/tilde, write paths, incremental store updates, per-file layer modeling. | (this branch) |
| D49 | clojurescript (WAM lane, first use) | **The `pkg` CLI + resolver on ClojureScript (Opus) — and the Clojure WAM lane's first real program.** The pattern lane provably can't carry the resolver (7 of 28 emitted defns are TODO stubs — the search has nowhere to live in one-deterministic-function-per-predicate form), so this round is the FIRST caller of `wam_clojure_target:write_wam_clojurescript_files/3`: backtracking as a WAM choice-point stack in an immutable Clojure map (`try_me_else` conj's a snapshot, failure unwinds `:trail`, `!` prunes to `:cut-bar`). All 79 predicates compile (2536 instructions), nbb loads clean. **Six real compiler/runtime fixes**, five pinned in new `tests/core/test_clojurescript_wam_backtracking.pl`: nested `get_structure` lost the enclosing list cursor (read-cursor stack); `get_structure` had NO write mode (compound outputs failed in their own heads); `\+`'s cut pruned against a stale `:cut-bar` (per-activation barrier on call/execute — the §9 model again); lowered ITE popped the condition's CP instead of committing (the `L_ite_else_` tag, same signal wam_rust uses); structured-ITE lowering kept emitting after a `call` (now refused for such clauses); iterative deep-axis normalize + `subvec` trail truncation for scale. `run-wam-state` API added — before it the Clojure WAM lane could answer only ground queries. **Gates re-run HERE**: CLI parity **153/153** (the JS suite's 86 assertions against `pkg.cljs` + 66 byte-for-byte stdout/stderr/exit cross-checks vs `pkg.mjs`), resolver corpus **39/39** vs SWI (B1 1.20s), differential **2400/0/0** (SWI 1.82s / cljs 76.7s), 5 probes + all clojure-lane suites + ite_exec, cli_args cljs 17/17 + 25/25, wamjs corpus 39/39 sanity. **B3 scale (5k catalog)**: cljs load 0.265s (beats SWI 0.638s) but resolve **41.75s** vs SWI 0.011s — correct (identical selection), honestly ~4000× slower at scale; first-arg indexing (`switch_on_term`) unimplemented in the Clojure runtime is the named cause. Residuals: that speed gap; structured-ITE off for clauses with calls; probes pin fixed behavior, not the old wrong answers. NOTE for local reruns: SWI must run under a UTF-8 locale (`LC_ALL=C.UTF-8`) — the C-locale default mangles template UTF-8 (root cause of the D46 mojibake). | (this branch) |
| D50 | wam_rust | **The resolver on Rust (Opus): 11 silent-defect classes closed, §9 cut audit claimed — and an honest negative on speed.** `examples/pkg_resolver/rust/`: build.sh → generated Cargo project + term↔JSON shim; **corpus 39/39 vs SWI, differential 2400/0/0 — re-verified HERE** (before the fixes: 15 divergences). Fixes (probes in new `tests/test_wam_rust_builtin_probes.pl` + `test_wam_rust_cut_semantics.pl`): `put_constant []` atom vs `Value::List` at 22 builtin sites; zero-solution `findall` reported SUCCESS (stale continuation = HALT; BeginAggregate now scans for its End); `==/2` used raw PartialEq not `terms_identical`; **A3 CLASS-FIXED on both Execute AND Call** (whole builtin table + Proceed path, Go's model; `is_iso_meta_builtin` demoted); **the A2 "unreachable" frameless-Y form IS reachable** — `wam_target.pl` emits `get_level Y1` in Allocate-less ITE clauses, Rust routed it to the CALLER's frame (15 differential divergences; barrier levels now live on the ITE's own CP — **upstream-shaped: Haskell/Python/R/F# should re-audit**); aggregate-inner `!` destroyed the aggregate frame (§9 barrier raise); pending cut-barrier leaked from a fact's TryMeElse into the next Allocate; `call/1..8` didn't exist at all (meta-call walker added, scope-raising barrier); `bagof`/`setof` always failed (now inlined; ISO witness grouping = loud compile-time refusal). **Cut probes 35/35 × 4 configurations** (interpreter/pure/functions/lowered). Pre-existing suite: 44 pass / 2 fail — both reproduced identically on the base commit (not regressions); 8 suites re-sampled green here. **Benchmarks: B1 corpus 102 ms** (fastest leg yet; compile ~40 s), but **B2 differential 301.7 s vs SWI 1.8 s** and **B3 OOM on the 5k catalog** (8.5 GB): `save_regs()` deep-clones every live register per choice point, `Value` has no structural sharing — a register holding the catalog is copied per CP. `Rc`/`Arc` argument vectors recorded in `WAM_RUST_STATUS.md` as THE prerequisite for any symbolic-workload speed claim. Residuals: that memory model; `call/1` first-solution contract; witness grouping refusal; lowered-caller→lowered-callee unreachable from label entry. | (this branch) |
| D51 | wam_go (+ benchmarks) | **The resolver on Go (Grok) — the fourth target at the full bar, and the cross-target table complete.** `examples/pkg_resolver/go/`: build.sh → generated Go module + shim; **corpus 39/39, differential 2400/0/0, cut probes 35/35 (0 refused-loudly) — all re-verified HERE**, plus 5 sampled pre-existing wam_go suites and shared-lane sanity (wamjs 39/39, cli_args 17/17). Nine fixes, each probe-pinned (5 new `tests/test_wam_go_*.pl` + the ported cut suite): `switch_on_structure` emitted ConstCase fields (didn't even build); empty `*List` ≠ interned `[]`; compound `sort/2` unique-collapsed distinct `Name-Ver` pairs (shallow compare); findall results shallow-copied out of `Clone()` (freezeTerm); `call/1` unclassified → `BuiltinCall` + opaque cut floor; bagof/setof inlining opt-in + 4-operand begin_aggregate; **§9 barrier adoption** (`!/0` used the caller's `EnvFrame.CutB0`; Y-save extra-pop on retry); and a var-id aliasing bug where the shim's driver ids collided with `allocVarId` (B3's old failure — the earlier `matching_deps/4` diagnosis is retracted as wrong in the status doc, a model correction worth noting). **Benchmarks (my box): B1 0.034s — fastest corpus leg of the fleet; B2 19.5s (vs SWI 2.8s); B3 load 0.095s / resolve 11.0s with the same 10-package selection — the only NATIVE leg that finishes the 5k catalog** (Rust OOMs). `examples/pkg_resolver/BENCHMARKS.md` added: the full SWI/Go/Rust/wamjs/CLJS table with readings (SWI wins resolution outright; Go best small-scale + only native 5k finisher; Rust fastest-when-it-fits; wamjs best all-around + only store-backed leg; CLJS correct-not-fast). Cut corpus now passes on THREE targets (js/go/rust). Residuals: A2 X101≡Y1 encoding unchanged; unclassified-builtin silent fail; call/1 first-solution; witness grouping; var-id skip window 10000–10999. | (this branch) |
| D52 | wam_python + wam_haskell (+ fleet audit) | **Frameless-Y re-audit (Opus): the "unreachable" A2 form D50 proved reachable, verdicted on four targets.** The shape: `ite_use_y_level(true)` makes the shared emitter plant `get_level Y1`/`cut Y1` in Allocate-less clauses (e.g. `sat(V,gte(G)) :- \+ lt(V,G)` — resolver's `satisfies/2`); topmost-frame Y routing then reads/writes the CALLER's frame. **Python: BROKEN → FIXED, executed** — `pick_b(3, gte(1), tagX, Out)` returned `Out = -1` (a CP index) on the pristine tree; fix = the Rust model (`ChoicePoint.levels` + record/lookup in `WamRuntime.py`, BOTH interpreter loops + the legacy generator, pending level cleared on fail paths); probe `tests/test_wam_python_frameless_ite_level.pl` was RED-on-pristine (3 divergences). **Haskell: BROKEN → FIXED, static+codegen** (no GHC here; execution arm gated) — same misroute via `putRegST`→topmost frame, PLUS an independent find: the pure `step` path truncated the newest-first CP list with `take n` instead of `drop (len-n)` (correct only at n=0); `cpLevels`/`mcpLevels` added at all 11 CP construction sites incl. the lowered emitter's two. **R and F#: UNREACHABLE, probe-pinned** — neither passes the flag (corpus scan: 79 resolver + 43 argparser predicates, zero Allocate-less Y clauses), and both refuse the instruction loudly; documented as ROUTING immunity, not structural (both do write the topmost frame — a precondition recorded for ever enabling the flag). F#'s A2 cell upgraded suspected→verified (the threshold lives in `fsharp_wam_bindings.pl:546-592`, which the original audit didn't look at). Fleet-gaps matrix corrected in place: the "requires bytecode the fact compiler does not emit" dismissal replaced by the 4-line reproduction + flag-exposure list. **Follow-ups flagged: wam_go IS exposed** (flat `vm.Regs`, flag on, no Y save without Allocate — fix round dispatched), wam_lua (also no conformance arm) and wam_llvm unaudited for the shape. All four probes re-run green HERE; python/haskell suite failures verified pre-existing by pristine-tree stash-rerun. | (this branch) |
| D53 | wam_go (+ lua/llvm verdicts) | **Frameless-Y closes on Go — the LOWERED lane was the broken one (Opus).** Interpreter lane: already safe via D51's `vm.YSaves`, proved CAUSALLY (protection disabled → 14-shape differential broke exactly as predicted; restored → 14/14). Lowered lane: BROKEN on pristine — `vm.PredPick_b4(...)` returned `Out = 0` (a CP depth) and `PredPick_a4` silently failed where the same predicate at its interpreter label answered `tagX`; the lowered emitter's `call` is raw label dispatch with no `pushCallFrame` and its `allocate` pushes a frame with no `SavedYRegs`, so `YSaves` never covered it. Fix = the Rust/Python CP-levels model in `state.go.mustache` + `GetLevel`/`Cut` cases, with a `PendingLevel` park so `get_level`-before-`try_me_else` lands on the guard CP (the clause can be entered with zero CPs); off the register file → entry-path independent, closes BOTH lanes. Probe `tests/test_wam_go_frameless_ite_level.pl` red-on-pristine (2 divergences), runs every case on both lanes. Honest caveat recorded: corpus/differential don't exercise the lowered lane (the shim enters by label) — they prove the fix NEUTRAL (perf unchanged), the probe proves it correct. **Lua: EXPOSED-UNFIXED** (flag always on, emitter plants `GetLevel(201)` into the SAME slot a framed caller's first permanent uses, flat `state.regs`, no Y save on ANY lane — strictly worse than Go was; no conformance arm = no execution oracle). **LLVM: bytecode lane EXPOSED-UNFIXED** (opcode 33 → flat `%WamState` GEP; trail only helps backtrack, wrong answer is on the success path); lowered lane immune (`get_level` = explicit no-op); side-find: llvm's A2 aliasing cell resolved suspected→ABSENT. Both carry deliberate `..._is_still_exposed` probes that go red when someone fixes them. Fleet-gaps A2 cells rewritten; cpp + javascript are now the only un-re-audited flag-passers. **NEW residual defect found (separate, unfixed): a Go LOWERED method whose body calls the interpreter then tail-recurses loses its output binding** — suspected missing §9 call-frame protocol in the lowered emitter; controls isolate it from the barrier fix; recorded in `WAM_GO_STATUS.md`. Verified HERE: 3 probes, cut 35/35, rebuild byte-identical, corpus 39/39, differential 2400/0. | (this branch) |
| D54 | clojurescript (WAM lane) | **First-argument indexing lands in the Clojure runtime (Opus) — correct, probe-pinned, and honestly short of the hoped win, with the reason MEASURED.** The whole switch family the runtime used to skip as `:raw` now dispatches: `switch_on_term` (A1+A2 forms), `switch_on_structure`, constant fallthrough, `try`/`retry`/`trust` chains — parsed by their own count fields (a positional split misreads zero-structure entries), key→address maps folded once at load, first-match-wins entry order (pinned by a REAL red bar: flipping to last-wins makes `findall` answer `[3,9]` for SWI's `[1,3,9]`), unknown/unbound falls through to the full chain. Sound without the JS lane's `indexed_entry` flag because this runtime's `backtrack` pops the CP it resumes. 5 new probes (`tests/core/test_clojurescript_wam_indexing.pl`): order, fresh-atom/functor dispatch, unbound first arg, list/structure split + missing functor, dispatch across cut. **The win: 1.3–1.47× (B2/B3, contributor box; ~7% B3 on the reference box — gap recorded, not averaged), NOT the order of magnitude sought — and the step census says why**: the 5k resolve is 7.56M WAM instructions and 285k backtracks; indexing works (chain `try_me_else` essentially never fires) but 210k iterations are FOURTEEN honest scans of the 15,000-row depends LIST — `resolver.pl`'s packages live in lists inside `catalog/9`, not as clauses, so there was little clause search to index away; at ~5µs/instruction under nbb's SCI interpreter, 7.56M instructions IS the 40s. Four interpreter micro-fixes landed from the same measurement (choice-snapshot via `assoc` 5.48→2.42µs, deref fast path, `subvec` cursor, `get`/`assoc` regs); the hard floor for this shape is ~11s. **Top residual re-diagnosed: per-instruction interpretation cost** (fix path: compile instructions to closures at load, fuse straight-line runs); T4 lowering still strips switch prefixes; wamjs/Go need no enablement (verified: JS runtime already has the switch family). Gates re-run HERE: corpus 39/39, differential 2400/0, CLI 153/153, both probe suites, clojure suites; `runtime_smoke` non-termination + 2 lmdb.h failures verified pre-existing (pristine-identical). | (this branch) |
| D55 | wam_rust | **Structural sharing lands — Rust becomes the fastest transpiled leg, and the lowered-tier question gets a measured answer (Opus).** `Value::Str`/`Value::List` now carry `Args { spine: Arc<Vec<Value>>, off }` (`Deref→[Value]` kept ~320 match sites source-compatible); Arc not Rc because T7's Rayon fork pins `WamState: Send`; NO trail cooperation needed — this runtime is already immutable-terms+bindings-map, and the one in-place spine mutation (cons prepend) now copies. **Three costs collapsed, not one**: O(1) clone alone still OOM'd (13.6 GB — faster, so it allocated further); the bigger offender was `get_list` allocating the remainder VECTOR per list step (fixed by `Args::tail()` = same spine, off+1 — THE change that made the curve linear); `deref_heap` now reuses the spine when no child moved. **Numbers (contributor box, re-verified HERE)**: B1 102→36ms (31ms here), B2 306.9→**16.5s** (≈7.8× SWI), B3 **OOM→4.73s / 680 MB peak** (4.5s here); ladder linear (20.7× packages = 17.2× time, 15.6× memory). 9 SWI-oracled sharing probes, each ALSO run against the pristine deep-copy runtime (identical output = behavior-preserving). **Native-interface/lowered-tier verdict (callgrind+massif): documented negative today with a precise roadmap** — post-sharing profile is pure interpreter machinery (step 65%, backtrack 21%; NO arithmetic/constraint hotspot, so leaf builtins have nothing to capture); `emit_mode(functions)` lowers 74/79 predicates and compiles but the functions are DEAD CODE (no name-keyed dispatch hook; Execute/Call always take the interpreter). Three-item scoping recorded: (1) a `lowered_call` hook shaped like T9's, (2) UNSOUND without a resumable protocol — T4 is first-solution and the resolver genuinely backtracks (the runtime has the shape twice already), (3) the 5 refusals' exact missing shapes (solution-producing disjunction; multi-clause ITE). Sound intermediate named: dispatch the 27+ deterministic/clause_chain predicates only. **New pre-existing bug found: `arg/3` over a body-constructed compound leaves the output unbound** (pristine-identical; resolver never calls it; recorded). Disclosed off-ownership touches verified mechanical HERE (constructor renames in rust_target.pl/2 benchmark generators/fixtures/6 test files). Gates HERE: corpus 39/39, differential 2400/0, cut 35/35×4, sharing 9/9, builtin probes, 5 suites sampled; the 2 known base failures unchanged. Go's "only native B3 finisher" reading corrected in BENCHMARKS.md. | (this branch) |
| D56 | wam_javascript (stores) | **LMDB catalog backend + fleet-vocabulary caching tier (Grok).** The JS fact-source layer now speaks the graph-kernel fleet's materialisation names (per the Go/F# `lmdb_materialisation` tier set) instead of a one-off cache design: `UW_LMDB_MATERIALISATION` ∈ `eager` (scan-once, in-memory A1 index) / `lazy` (fetch-per-lookup — the D48 bytes∝query shape) / `cached` (bounded L1 LRU over BOTH backends, default like Go/F#, cap `UW_FACT_CACHE_CAP`=4096) / `auto` (residual: warns once, uses cached — no `core/cost_model.pl` hookup yet; L2 pair and compile-time `lmdb_materialisation(...)` option also residual, `wam_javascript_target.pl` frozen). LMDB now actually carries the RESOLVER: same four stores built as LMDB DBs from the same JSONL, declaration-level backend switch, corpus 39/39 + differential 500/0 on BOTH backends (lmdb arms ran gated in Grok's env; loud-error path ungated). Cache is asserted semantics-invisible (equivalence test: 39 cases × lazy/cached/eager, both backends — re-run HERE on indexed: replay_hits=270, identical results). **The honest reading Grok delivered**: Rust graph kernels see 2.25–3.17× from cached because COMPILED lookups dominate; this resolver is the opposite shape — the JS WAM interpreter dominates, so cached saves ~111× indexed reads on a 100×-repeat but only ~3% wall. Tier probes on the 5k catalog: lazy 8242B/650 reads, cached 7321B/578 (83 hits), eager 742KB/67548 (the full scan — why the scale demo forces lazy). Merged HERE: both wamjs builds rebuilt on the new runtime (term program diff = timestamp only), term+store corpus 39/39 each, store differential 500/0 at default cached, D48 bytes-read proof intact under forced lazy, 4 JS suites + conformance + cli_args 17/17. Residuals: L2/auto/cost-model, compile-time option plumbing, secondary indexes, incremental updates, write paths (must flush the cache — noted). | (this branch) |
| D57 | (uw-resolve P3 + wam_javascript) | **Debian semantics + real-index ingestion (Grok) — uw-resolve speaks its target distro's language, on real bookworm data.** First `resolver.pl` round since P0.5, additive: `deb(Epoch, UpSegs, RevSegs)` with `s(OrderCodes, DigitInt)` segments PRE-SEGMENTED at the ingestion edge (hot path = list walking; Policy §5.6.12: `~` before everything incl. empty, letters before non-letters, numeric digit runs); v/3 world byte-identical (`sort/2` kept for all-v/3 lists, predsort only when deb present); relations `>=/<=/>>/<</=` → `gte/lte/gt/lt/eq`. Provides both unversioned (satisfy `any` only, per Policy) and versioned; virtual never itself selected; real-preferred, providers in catalog order. `alternatives([dep(A,C),…])` = ordered group, first-satisfiable, downstream dead-end backtracks into later alternatives; layered short-circuits via loaded layers. `explain_blocked` extended (`providers([...])`, per-alternative reason lists). `catalog/10` (back-compat 6/9). Ingestion: `debian/parse_packages.mjs` (Pre-Depends→Depends, Breaks→Conflicts, Essential→base-candidate marker — all noted), extended JSONL/store schema (new `provide` store prefix), **committed 500-stanza REAL bookworm slice** (deb.debian.org, Last-Modified 2026-07-11; 3204 depends, 98 provides, 23 Essential) + stores. **Demo on real data re-run HERE**: `resolve(hostname)` → 4 real packages at real versions; layered vs Essential holds with libc6 pinned old → `blocked libc6 needs >=2.34 base_has 2.31-13+deb11u11`; freeze_audit marks that hold `suggest(abi_anchor)`. **Gates re-run HERE**: plunit 50 (38 old + 12 new), **dpkg cross-check RAN ON THIS BOX: 20 curated + 2000 random pairs, 0 mismatches vs real dpkg**; wamjs + wamjs_store corpus 51/51 each (rebuilds byte-stable mod timestamp); term differential **2600/0**; store differential **503/0**; 4 JS suites + conformance + cli_args 17/17. Runtime fix (maplist/2-4 + string_codes empty edge — compiler emits BuiltinCall maplist/N) arrived probe-less; **coordinator added the probe** (`maplist_string_codes_runtime` in the builtins suite, user-pred goal shape mirroring real usage) — green. **Cross-lane consequence measured: go/rust/cljs lanes now show exactly 11/51 corpus divergences each** (the P3 rows; all 40 pre-P3 rows green on their committed pre-P3 builds — verified per lane HERE). Rebuilding them against the P3 resolver WITHOUT porting is WORSE (30/51 — maplist missing breaks shared helper paths), so their committed builds stay pre-P3; **per-lane P3 port rounds queued** (each needs maplist/string_codes-class builtins + deb/provides/alternatives shim shapes). Residuals: Breaks≡Conflicts, Pre-Depends ordering, Recommends/Suggests, multiarch, epoch display `0`, write paths. | (this branch) |
| D58 | (design record) | **Resolver pruning design (Fable-class design round; design only, no code).** `docs/proposals/RESOLVER_PRUNING_DESIGN.md`: measured verdict that OVERTURNS the branch-trimming premise for layered mode — since P3, `pick_need/8`'s `->` commits to the highest candidate, so the 5k `resolve_layered` runs with ZERO search backtracks; the 7.56M instructions are 24 full list scans (14×15,001 depends + 10×7,514 packages; 94% of SWI inferences are the two walkers). The load-bearing change is **G1: a per-call pure-Prolog catalog index** (position-tagged sort → grouped → balanced tree, `icat/3` wrapper never escaping), prototyped: wamjs 5k goes from CANNOT-FINISH (2M-step cap) to **675,217 instructions, correct answer**; 1k-pkg 8.8×; honest costs stated (B2 +11–14% wall on 20-row catalogs — size-threshold option; SWI itself SLOWER with the index, 9.0→13.1ms). G2 (conflict test before expansion, free), G3 (single-pass conflicts), G4 (layered ceiling fail-early, PROVED with an explicit enumeration + the `provides_mentions` hypothesis that CE4 shows is load-bearing — a version without it is unsound). **Three families REJECTED with executable counterexamples**: most-constrained-first reordering (CE1 — changes first solutions; unfixable under the order invariant), from_base memoisation (CE2), alternatives pruning (nothing survives). **Six pre-existing hazards found and probe-pinned (CE1–CE6), PRESERVED by design**: H1 layered-mode no longer backtracks over candidate versions since P3 (probable regression — pre-P3 file answers where P3 fails; corpus can't see it); H2 one-name-two-versions via providers; H3 resolve/explain disagreement on provider-bypassed ceilings; H4 NONTERMINATION on cyclic held deps in layered; H5 named-layer provider clauses are dead code (unbound-var `==`); H6 the JS 2M-step cap silently returns fail. Plus a NEW wam_rust runtime finding: index build quadratic there (bisection: suffix-through-output-argument shape in `same_key/4`/`build_tree/4`; keyed-list build alone is linear) — for `WAM_RUST_STATUS.md` per §5 step 1c. Implementation plan sized (steps 0–2 round, 3–4 stretch), invariants incl. SWI BYTE-IDENTITY on 2,600+503 cases, review protocol with falsifiable assertions A1–A11. Implementation round dispatched; independent design review queued. | fable/resolver-pruning-design |
| D59 | (uw-resolve, all legs) | **Pruning design implemented — G1 index + G2 conflict-first land under the byte-identity gate (Opus).** Step-0 instrumentation reproduced the design EXACTLY (SWI 286,804 inferences, wamjs 11,903,199 B2 instructions, all §1.4 shares, the D54 census 210,014 — validating the design's measurements wholesale; wall times a uniform 1.8–1.9× box factor). Landed: G1a+G1b (position-tagged `sort/2` form — kept over keysort for its zero stability assumption) behind a MEASURED `index_threshold(64)` (crossover bracketed: index loses at 68 dep rows, wins 4.1× at 118; threshold turns B2's only regression, +9.9% wall forced-on, into −2.6%); G2 mirrored into the store search (premises verified). **wamjs 5k: cap→`fail` before, now 854,887 instructions and CORRECT (9.2×); 2k cap cleared; 1k 8.8×.** SWI honestly slower at 5k (+58% wall, −50% inferences) — as designed, this optimizes the transpiled legs. **Two design claims corrected**: `UW_PROFILE` does NOT lift the 2M-step cap (an env knob is a named residual), and Rust's `keysort/2` is QUADRATIC, not mild. **Step 1c outcome: Rust gated, root cause found** — `WamState::unify` deep-derefs BOTH args before dispatch (callgrind: ~60% malloc/free of term copies), so output-var-vs-list-suffix unification is O(len), Θ(N²) per element; the design's size-threshold fallback provably cannot rescue it (regression starts at 500 pkgs); lane not rebuilt, fix sketch in `WAM_RUST_STATUS.md`. Stretch G1c–G4 deliberately NOT taken (risk-budget call; CE4 + A9–A11 red bars already committed for a future G4 round). **New probe file `test_pruning_probes.pl` (24 tests)**: CE1–CE6 pristine AND post-change (hazards preserved), exhaustive index≡scan over every corpus catalog, H5 pinned, and the design's flagged A9 third-pairing RESOLVED: same-atom-as-package/held/virtual fails when the provider isn't in Acc, succeeds when it is — both pinned; `provides_mentions/2` handles it. **Merged HERE with independent re-verification: SWI byte-identity cmp-clean on 2,600 cases (fresh baseline snapshot), both differentials 0, corpus 50+51/51+51/51, probes 24/24, JS suites.** H1–H6 now in the README known-issues list. Sol design review still queued — most valuable for the unshipped G4. | (this branch) |
| D60 | (uw-resolve, review response) | **Adversarial design review lands two real falsifications; both addressed (external reviewer via user; coordinator fixes).** Review scope: baseline `dadbed63` + head `0f93b222`, heavy finite sweeps. **Falsified #1 (binding on the future G4 round, no shipped code affected): G4's preservation proof is unsound when the doomed subtree DIVERGES** — on H4's cycle placed inside an alternatives group, baseline hangs while guarded-G4 fails that alternative and returns an answer through the next; the proof wrongly treated "no solution" as "fails finitely and resumes". Recorded in the design doc §7: a future G4 must either fix H4 first and re-baseline, or take owner sign-off on divergence→success plus a timeout notion in the differential runner. The reviewer's 1,536 finite comparisons over the flagged hard cases found NO successful branch incorrectly pruned — `provides_mentions/2`'s finite-case soundness held. **Falsified #2 (merged code, FIXED here): `icat/3` was accepted as public input** — `resolve(icat(...),...)` succeeded through the delegating accessors where baseline fails (invariant 7 violated). Fix: `is_public_catalog/1` structural guard at `index_catalog/2` (catalog/6|9|10 only), pinned by new probe `a3b_icat_rejected_as_input`. **First fix attempt used `functor/3`+`memberchk/2` and silently broke ALL 51 wamjs corpus rows** — those builtins fail silently in the wamjs path (the A3-class hazard live in our newest leg; the corpus gate caught it in minutes; JS-side loud-unknown-builtin warning noted as residual). CE5 strengthened to succeed ONLY on timeout (the old form couldn't distinguish divergence from an answer). Design doc §7 addendum records all findings incl. the four missing invariants (divergence-vs-finite-failure as observables, groundness preconditions, executable wrapper-rejection, resource limits vs logical answers). Not-falsified with sweep sizes: G1 rows/order (3,600), G2 groundness (30,720 picks), G3 truth table (64/64), accessors/raw-query (108). Verified HERE: rejection reproduced pre-fix, REJECTED post-fix; probes 26/26; wamjs corpus 51/51 after rebuild; SWI byte-identity cmp-clean; term 2600/0 + store 503/0. | (this branch) |
| D61 | wam_go (P3 port) | **The Go lane catches up to the Debian resolver (Grok): corpus 51/51, differential 2600/0 — and two more real bugs, plus the honest index verdict.** Builtin inventory per the D60 lesson: `maplist/2,3,4` (meta-calls user preds via `invokeGoalOnce`) and `predsort/3` implemented+classified; `functor/3`/`arg/3`/`=../2` existed but were UNCLASSIFIED (the A3 silent-fail class — exactly what bit D60 on wamjs), now classified; **`UW_WAM_WARN_UNKNOWN=1` warn knob landed** (mirrors Rust) so the next gap announces itself. Shim round-trips the full P3 JSON (deb both directions, alternatives, provides 3/4-ary, catalog/10, `providers([...])`/`alternatives([...])` blocked shapes). **Bug 1 — switch default skipped a TryMeElse**: `SwitchOnConstantPc` resolved the default to the choice-chain head and then skipped it, so only clause 2 of `pick_need`'s classic mode ran (three provides scenarios failed); fix distinguishes label-form fall-through from Pc-form jump-to-chain-head; probe `switch_default_chain`. **Bug 2 — predsort clobbered A2**: the comparator invocation wrote A0–A2 and never restored, and stale comparator CPs could be backtracked into; corpus missed it (single-candidate P3 rows short-circuit compare/3) — the DIFFERENTIAL caught it as 40 divergences, the gate-design working exactly as intended; fix restores regs per `invokeGoalOnce` + truncates leftover CPs; probe `maplist_predsort`. All 26 wam_go suites + cut 35/35 + frameless + D51 five green. **Index verdict (honest)**: Go B3 10.62s vs 11.0s pre-index (~1.04×) against wamjs's 9.2× — Go's baseline could already finish, and the interpreted index BUILD (~45 instr/row × 22.5k rows) nearly cancels the lookup win; recorded in BENCHMARKS.md. The pattern across legs is now crisp: the G1 index rescues runtimes that couldn't finish, and the per-instruction interpretation cost still gates everything else — the lowered-tier/closure-compilation levers stay the fleet's top perf residual. Verified HERE: rebuild byte-stable, corpus 51/51, differential 2600/0, 9 probe suites + 4 sampled go suites, wamjs corpus + JS builtins sanity, B3 reproduced (14.5s, box factor). Residuals: A2 alias, A4 no string type, lowered call-then-tail-execute binding loss, bagof witness grouping. | (this branch) |
| D62 | (design-idea recon) | **M-Prolog mining notes (Sonnet recon; ideas-with-credit, never code).** `docs/proposals/MPROLOG_MINING_NOTES.md` (625 lines, attribution header: Kenichi Sasagawa, Modified BSD, cite-per-finding): 11 findings — 5 adopt, 1 adapt, 1 already-covered, 4 out-of-scope. **The headline answers our #1 open gap**: mprolog's experimental compiler compiles nondeterministic predicates into single C functions using labels-as-values, where a CHOICE POINT is a saved label + frame slot and the second solution is a JUMP, not a re-call — a working precedent for D55's missing resumable-lowered-tier protocol (trampoline/state-machine is the portable analogue). Also: mprolog's `unify()` dispatches on raw shape BEFORE deref — independently validating the exact fix `WAM_RUST_STATUS.md` sketches for the Rust quadratic; a whole-predicate determinism classifier (dyn>tail>det>nondet) whose `tail` class compiles to a native loop with NO choice-point machinery — a precedent for D55's "sound intermediate"; zero clause indexing in their interpreter (corroborates D54: indexing isn't where cost lives); their speed claim honestly rated unsubstantiated (LIPS counts, no baseline); their parallel model's single lock is an anti-pattern worth auditing our own T7 against (in-place mutation of shared clause templates). Two independent designs converged on §9's cut-as-barrier. **Triage of the 5 open questions (per the agreed Sonnet→coordinator→Opus flow)**: Q1 (trampoline prototype) + Q3 (unify shape-first mechanical feasibility) → ONE Opus round on the Rust runtime (dispatched); Q2 (do we have a tail-loop classifier?) → partially yes (the emit-class machinery), the loop-compilation half folded into the Opus brief; Q4 (shared-template mutation audit) → queued as a bounded cross-target audit; Q5 (does the precedent raise D55's priority?) → coordinator's call: YES — the lowered tier is now the fleet's clear top perf lever, and F3 removes its main design risk. | (this branch) |
| D63 | wam_rust (mprolog-informed) | **Unify shape-first fix unblocks the D59 index on Rust; lowered-dispatch hook landed neutral; trampoline deferred with obstruction write-up (Opus).** Item 1 (the load-bearing one, validated by mprolog F5): `WamState::unify` reordered into phase-1 raw-cell match (allocation-free `deref_chain`) + phase-2 owned one-level `deref_shallow` with a spine LOOP — **mostly mechanical, one borrow-checker constraint** (the read-only match must be a separate block from the mutating bind, since phase-1 borrows `&self.bindings`); NO change to `Value`, trail, or call sites. Plus iterative `deref_cons_chain` (the second Θ(N²) + the 8k-cell stack-overflow source) and the ~5% deferred `deref_heap` alloc. **Result: the D59 bisection probes go quadratic→LINEAR** (suffix-through-output-arg 8k: 10,474ms→193ms; build_tree/keysort 8k: 30,943→568; no aborts, 32k finishes in 4.1s), and **the G1 index on Rust goes from OOM-at-5k to a 2.3× win** (fixed+G1 7.5k pkgs 1,924ms vs index-off 4,402ms; index-off does not regress). Item 2a: `lowered_call` hook (T9 shape) consulted from Call/Execute, eligibility = a cp-clean greatest fixpoint (dispatch only if every transitive user callee is single-clause, because lowered fns reach callees via the interpreter and a multi-clause callee's live CP can be escaped into); runs with cp=0 + entry barrier, rolls back + returns None if a CP leaked. **Measured net-neutral** (~4% dispatch win ≈ ~3.5% snapshot cost) — honest: the hot multi-clause drivers can't be dispatched under the sound intermediate, the real lever needs 2b. Latent bug fixed in passing: lowered `get_constant`/`get_integer`/`get_nil` on an unbound arg now BIND the variable (`head_constant`), matching the interpreter. Item 2b (resumable nondeterminism) DEFERRED with a two-obstruction write-up (cross-call convention needs direct lowered-to-lowered resume entries; saved-state must shrink below the full register file or it won't beat the interpreter's own backtrack cost). Q2: self-tail-recursion→loop is NET-NEW (no tail detection in the emit classes today) and the lowest-risk next lowered-tier step. New probes: `test_wam_rust_unify_shape.pl` (9/9 SWI-oracled) + `test_wam_rust_lowered_dispatch.pl` (dispatched≡interpreted≡SWI; a member/2 enumerator keeps all solutions — proof the hook never commits a nondet pred). **Verified HERE**: both new probes + cut 35/35×4 + term_sharing 9/9 + builtin probes + 5 sampled pre-existing suites green; the 2 known base failures documented pre-existing (agent verified pristine-identical). Committed rust example left PRE-P3 (its resolver corpus/differential await a separate P3 port round — the Rust analogue of D61). Residuals: 2b + ITE-per-clause/`(A;B)` front-end; the tail-loop class; the 2 base failures. | (this branch) |
| D64 | (design-input record) | **H4/H1 solution-space exploration (external adversarial reviewer; prose, no code).** `docs/proposals/RESOLVER_H4_H1_EXPLORATION.md`: 8 approaches to the layered-mode nontermination (H4) + candidate-backtracking-regression (H1) evaluated, each with termination/soundness/CE2 verdict. **Load-bearing correction to our own framing**: CE2 is graph-cyclic but OPERATIONALLY TERMINATING (base0→a→base0, selecting `a` makes the 2nd expansion productive), so the compatibility criterion is "preserve every operationally-terminating result," not "preserve syntactically-acyclic catalogs" — which kills the naive visited-set and ancestor-chain fixes (approaches 1,2 rejected with counterexamples). **Preferred survivor = approach 3, active same-state cycle closure**: validate the incoming request FIRST, then suppress a from_base expansion only when the held Pkg-Ver is already active at the SAME branch-local selection generation (advanced after every catalog insertion incl. a second version of a name); backtracking restores marks+generation. Approach 4 (visited-set reset per selection) a serious alternative pending broader proof — the two agreed across 5,000 seeded cases under both H1 states, preserving all 4,631 in-budget baseline results. H1 repair: version-only restoration recommended (preserve held commitment, enumerate descending real versions, providers only when no real candidate) over classic-fallback. **Sequencing: one coordinated round, H4 THEN H1** (H1 can't backtrack past a nonterminating highest candidate), one baseline update; CE2's modified-catalog negative assertion becomes invalid under restored H1 (finds `[a-1,b-1,d-2,x-2]`) — split the assertion. explain_blocked needs a documented cycle-story decision (preferred-candidate blockage vs absence-of-solution). Validation = the four-way matrix (neither/H4/H1/both) × 5 runtimes; G4 reconsidered only after. This is the design-round input; Fable design brief dispatched. | (this branch) |
| D65 | wam_cpp (semantics audit) | **wam_cpp brought to the semantics-audit bar (Opus; replaces the local Gemini pilot's first packet at the user's call).** g++ 13.3.0 + swipl both present, all execution arms ran. **Frameless-Y: BROKEN → FIXED, executed proof.** wam_cpp passes `ite_use_y_level(true)`; `sat/2` clause 2 emits `GetLevel("Y1")` before `TryMeElse` in an Allocate-less clause; every Y access routes to `env_stack.back().y_regs` = the CALLER's frame for a frameless callee, so `GetLevel` overwrote the caller's permanent with a CP depth — `pick_a(3,gte(1),tagX,Out)` FAILED and `pick_b` gave `Out=0` where SWI gives `tagX`. The per-frame `y_regs` the fleet doc cited as immunity covered only the ALIASING form, not this one. Fix = the Rust/Python/Go CP-levels model (`ChoicePoint::levels` + record/lookup + `pending_level` park so a get_level-before-try_me_else lands on the guard CP; level never touches a register); covers both the interpreter lane and the lowered-caller lane. Probe `tests/test_wam_cpp_frameless_ite_level.pl` RED-on-pristine (5 divergences), green post-fix. **§9 cut corpus: 35/35 vs SWI, 0 refused** (`tests/test_wam_cpp_cut_semantics.pl`, ported from the JS/Go/Rust corpora, uncompilable shapes fail loudly). **Two more pre-existing defect classes found → fixed → probe-pinned** (RED-on-pristine, stash-verified, independent of the Item-1 fix): `!` as a meta-called goal-term (`call/1`, `call((G,!))`) was unhandled and failed (p09/p10) — fixed with a call-scope barrier (`meta_call_barrier`); `!` inside a findall/bagof/setof opened by the same clause cut below the aggregate generator base and collapsed it (p13/p29) — fixed so `!/0` never prunes below the innermost open aggregate's base (ISO goal-opacity). **Verified HERE**: frameless probe green, cut 35/35, 3 sampled pre-existing cpp suites green; fleet-doc narrative reconciled by coordinator (cpp moved from "still to check" to audited; **javascript now the ONLY un-re-audited flag-passer** — very likely immune via its Call/Proceed Y-snapshot, but that's a reason-to-expect, not an audit). Residual: the lua/llvm exposed-unfixed verdicts (D53) and the wam_javascript verdict remain the frameless-Y campaign's open items; the C register-bounds + C cut-corpus stay for a later round (the Gemini pipeline or another agent). | (this branch) |
| D66 | (design record) | **H4/H1 fix design — approach 3 made buildable, prototyped and MEASURED (Fable 5.1).** `docs/proposals/RESOLVER_H4_H1_DESIGN.md` (940 lines) turns the D64 exploration's reference recommendation into a concrete design, and went past paper: every load-bearing claim was run on a scratch prototype of `resolver.pl` (SWI + a scratch wamjs build). Decisions settled: state = one threaded term `st(Gen, Active)` as a sixth search-loop arg (`Active` = stack of `a(Pkg,Ver,Gen)` open held expansions; backtracking restores marks+generation for free since it's an ordinary undone binding, NO assert/retract — portable across all five runtimes); generation advances at exactly ONE site (every `[Pkg-Ver|Acc]` cons in the `from_catalog` arm, so the H2 second-version shape advances it for free); closure test sits AFTER `pick_need/8` (validate-then-suppress — a failing closing edge fails the branch before closure is reached, so `a-1→b-1→a=2` correctly FAILS); expansion completion = a `done(Pkg,Ver,Gen)` sentinel in the pending list; H1 = version-only restoration (held-name/held-provider arms untouched and still committed); explain_blocked reports the PREFERRED candidate's blockage (only change = report a repeated name's ceiling before suppressing its walk); `resolver_store.pl` mirror takes both edits in the same commits. **Measured on the prototype**: 50-scenario corpus green unchanged; CE2/CE5/CE6 go red exactly as designed, other 23 probes green; 2,600-case SWI oracle BYTE-IDENTICAL with H4 alone, differs on EXACTLY 11 lines with H4+H1 (all `{"fail":true}`→`{"ok":…}` on resolve_layered/layer_closure); scratch wamjs corpus 51/51 + differential 0 divergences + 11 hand-built cyclic/H1 cases at 0; SWI overhead +0.03% (H4) / +0.06% (H4+H1). Sequencing: H4 then H1, two commits, one approved re-baseline (the H1-only matrix cell turns finite failures into DIVERGENCE — the measured reason H4 must land first). Approach 4 documented as fallback, not needed on any evidence gathered. **Ready for review + implementation, but GATED on Astra's concurrent approach-3 hardening** (the five open verification points + approach-4 proof obligation) — if that surfaces a hole in approach 3, the design revises before implementation. | (this branch) |
| D68 | (design cleared) | **H4/H1 approach-3 hardening — CLEAN; design cleared for implementation (external reviewer + coordinator).** Independent adversarial hardening of the chosen approach returned no falsification: all five targeted verification cases pass; a **30,000-case seeded search found ZERO answer differences** among generation-keyed approach 3, snapshot approach 3, and approach 4 across all three H1 policies; all 28,381 in-budget baseline cases preserved under H4-alone — independent confirmation of D66's measured result. The corpus pins were shown to have teeth (three faulty-bookkeeping mutants caught: leaked mark, name-only key, cache-at-completion). Two refinements folded into the design (§7 addendum): **(1)** the sibling-scope 3-vs-4 distinction is ANSWER-INVISIBLE (both return the same selection; 3 expands twice, 4 skips) so that corpus case needs a MODEL-TRACE/instruction-count assertion, not an answer assertion; **(2)** marking at completion is falsified — approach 3's `done` sentinel must POP the entry-time mark, never CACHE a completed expansion. Design doc §7 records the verdict; implementation round dispatched (Opus, coordinated H4-then-H1, produces the re-baseline diff for human approval before merge). | (this branch) |
| D67 | wam_clojure (P3 port) | **The Clojure/nbb lane catches up to the Debian resolver (Grok) — corpus 51/51, differential 2600/0 vs LIVE SWI — and surfaces the H1 regression through BOTH pkg CLIs.** Builtins: `maplist/2,3,4` + `predsort/3` implemented and classified (meta-calls via `invoke-at-pc-isolated`, which forks fresh regs/stack/CPs/cut-bars and copies back only bindings/trail/heap/intern — the immutable-map form of the D61 predsort register-hygiene lesson; last-goal `:execute` calls `succeed-state` so `advance` can't walk into the next predicate); `functor/3`/`arg/3`/`=../2`/`memberchk` classified (A3); `UW_WAM_WARN_UNKNOWN` landed (nbb writes via `js/console.error`, `*err*` unbound). Shim speaks full P3 JSON. **Real bug found: `index_catalog` failed on every catalog past `index_threshold(64)`** (54 differential divergences) — `functor_arity_string` split on `/` and required two parts, but `N is (N-1)//2` emits `put_structure ///2` (functor `/`, arity 2), which fell to `:raw` and backtracked; fix = parse the last `/<digits>` segment in `wam_clojure_target.pl` + the lowered pred-key parse; probe `gdiv` in the new `test_clojurescript_wam_maplist_predsort.pl`. **B3 index verdict (the CLJS pole): G1 WINS here** — 5k resolve 17.6s vs D54 pre-P3 ~39–42s (~2.2×), query steps ~276× fewer; 99% of remaining instructions are the interpreted index BUILD (2,648,879 build + 27,405 query of 2,676,284) — the slow ~6.6µs/instruction baseline is exactly where G1's build cost is dwarfed by the scan savings (Go 1.04× and wamjs 9.2× are the other poles; the index's value is now fully characterized across the fleet by baseline speed). **CLI: 142/153 vs the frozen pre-P3 `expected.json`; 66/66 cljs==js.** The 11 misses are all `install-plan`/`layer` (= `resolve_layered`). VERIFIED HERE this is NOT a port defect but the **H1 regression** (D64/D66): the JS pkg CLI fails the SAME 11 (75/86), live current SWI fails them (`no layered plan for: editor`), and the cljs differential vs LIVE SWI is 2600/0 — so cljs faithfully matches the current resolver; `expected.json` is the stale pre-P3 oracle. **The pkg-CLI suites (`test_pkg_cli*.mjs`) were never in the P3-merge gate lists, so H1's CLI-visible breakage went unnoticed since D57** — now recorded. Verified HERE: corpus 51/51, differential 2600/0, 3 clojure probe suites, wamjs corpus 51/51. **Re-baseline scope for the H4/H1 round GROWS**: regenerate `cli/generated/expected.json` AND rebuild both pkg-CLI builds, and add the pkg-CLI suites to the standing gate list. Residuals: the ~6.6µs/instr floor; structured-ITE off for call-bearing clauses; known lmdb.h/Cargo/runtime_smoke env failures; `store/scale_demo.pl` still can't load P3 rich.jsonl (throwaway P3 loader used for SWI B3). | (this branch) |
| D69 | (uw-resolve) | **H4 cycle closure + H1 version-only backtracking — the layered-mode correctness fix (Opus; approved re-baseline).** Two layered-mode bugs fixed on `resolver.pl` + mirrored in `resolver_store.pl`. **H4** (cyclic held dependencies hung): active same-state cycle closure — a threaded `st(Gen,Active)` term, generation advanced on every catalog insertion, closure tested AFTER `pick_need` (validate-then-suppress), `done(Pkg,Ver,Gen)` sentinel POPS the entry-time mark (never caches — the D68 refinement). **H1** (layered committed to the highest candidate and gave up): version-only backtracking restored (held-name/held-provider arms stay committed). **Approved answer changes**: EXACTLY 11 term-oracle cases flip `fail`→`ok` (5 resolve_layered, 6 layer_closure, 2 deb-world), zero `ok`→`fail`; H4-alone oracle byte-identical to baseline — all independently reproduced HERE from a fresh baseline snapshot before merge, human-approved. The D67 pkg-CLI regression is REPAIRED: `test_pkg_cli.mjs` 86/86, `test_pkg_cli_cljs.mjs` 153/153. CE probes re-pinned (ce5 terminates to `[]`, ce2 split, ce6 classic parity) + 25 new scenarios incl. the sibling-scope TRACE probe (D68 refinement 1). Verified HERE: contract corpus + pruning probes green, store differential 0, wamjs term differential 2600/0 vs the new oracle, Go+CLJS corpus 51/51 rebuilt, both CLI suites restored. Rust stays pre-P3 (its 11 divergences are the D63 alternatives/provides gap, not H4/H1). **pkg-CLI suites now belong in the standing gate list.** G4 (the deferred pruning guard) is now UNBLOCKED. | (this branch) |
| D70 | wam_go (store backend) | **Store-backed catalog path on the Go lane — native speed + read-only-what-you-need (Opus).** The wamjs D43/D48 store design, now on wam_go: `resolver_store.pl` (shared adapter, UNMODIFIED) compiles through wam_go with its five P/2 store predicates served from dependency-free UWFI/UWIX indexed SEEK stores instead of a loaded catalog term. New `examples/pkg_resolver/go_store/`. Inventory finding: Go's graph-kernel fact sources were in-memory/eager-scan/arg1-only — useless for `indexed(Prefix)`, so the SEEK reader is NEW (`seekFactSource`: binary-search the `.idx` table via `ReadAt`, read only matching `.data` records, bytes-read counter mirroring the wamjs reader; typed key tags atom/string/int/float; unbound-arg1 full-scan for the provides walk; streams through the existing choice-point machinery). **B3 headline, reproduced HERE**: `resolve_layered` on the 5k catalog — term path ~14.6s loading the whole 1.14 MB, store path **0.55s reading 11,025 / 1,142,225 B = 0.97%** (880 seeks), identical 10-package selection — **~19× faster reading under 1% of the store** (the D43 proof, now native). **Five real runtime bugs found→fixed→probe-pinned** (owned files only; adapter untouched): `number_string/2` unimplemented; `atom_concat/3` forward-only (needed decompose modes); double-quoted `"-"` interned with its quotes; first-arg `try`/`retry`/`trust` dispatch unrecognised (collapsed multi-clause solutions); `escape_go_string` mis-escaped quotes. `lmdb(Dir)` arm registered but fails LOUD (Go can't read the npm-lmdb format; never a silent swap). **Verified HERE**: store corpus 51/51 identical to term, store differential 503/0 on the 5k catalog, B3 payoff, Go term corpus 51/51 + differential 2600/0, cut 35/35, wamjs corpus 51/51. resolver_store.pl needed NO change to run on Go. | (this branch) |
| D71 | wam_rust (P3) | **Rust WAM lane reaches P3 parity — the last term-differential gap closed (Grok partial → Opus finish).** Grok's partial P3 port (`1f525d128`: `deb/3` Debian versions, provides/virtual packages, `alternatives([...])` groups, extended blocked shapes) built and passed the curated corpus 51/51 but FAILED 53 seeded differential cases — all `p3g*`, every one returning `fail` where SWI resolves (the classic corpus-green/fuzz-red gap; non-P3 2547 cases were clean, nothing regressed). **Root cause was entirely in the hand-written Rust runtime, never the spec** (wamjs passes all 2600 from the same `resolver.pl`). The `deb/3` candidate order flows through `sort_versions_desc` → `maplist(is_v3,...)` + `predsort(cmp_ver,...)`, which meta-call user goals via `call_goal_once` + a nested `run()`; two defects bit only `deb/3` (the `v/3` path uses `sort/2` and an always-succeeding `is_v3`, so neither triggered): (1) `backtrack()` had no lower bound, so a failing meta-goal backtracked PAST the meta-call boundary into the caller's choice points and resumed the enclosing resolver search inside the nested run; (2) Yi-register trail entries from the nested run were unwound by the caller AFTER `call_goal_once` restored the stack, so `put_reg` wrote a stale nested Yi into the restored caller frame — clobbering the `predsort` output var, and `reverse/2` then saw an unbound list and failed. **Fixes at the GENERATOR source** (the crate is generated; editing `state.rs` directly is overwritten by `build.sh`): a new `backtrack_floor` in the `state.rs.mustache` template that `call_goal_once` raises to the entry choice-point depth and `backtrack()` respects, plus dropping the nested Yi trail entries in `call_goal_once` — both in `wam_rust_target.pl`. **Verified HERE** (independent rebuild from source on this branch, `LC_ALL=C.UTF-8`): `bash build.sh` clean, corpus 51/51 matched SWI, term differential **2600 / 0 divergences / 0 crashes**, new `test_wam_rust_maplist_predsort.pl` plunit port passes. Rust is now term-current across all groups — the pre-P3 caveat noted in D69 is cleared. This unblocks the Rust store-backend lane (next). | (this branch) |
| D72 | (uw-resolve) benchmarks | **Fresh single-box benchmark re-run — the stale table replaced with internally-comparable numbers (Opus).** The old `BENCHMARKS.md` mixed D51-era figures across different machines and cited the outdated 39-scenario / 2400-case gates. Re-measured EVERY reachable leg on this one 4-core coordinator box (2026-09-06), each gated first (corpus 51/51, term differential 2600/0, store differential 503/0) and only timed if green — all green, identical 10-package selection everywhere. **Headline (ratios vs SWI):** startup B1 — Rust 0.059s and Go 0.086s both BEAT SWI's 0.109s; wamjs 0.238s; cljs 1.93s (nbb boot). Throughput B2 (2600 term, target/SWI) — wamjs 6.3×, Rust 8.8×, Go 11.3×, cljs 44×. Big-catalog resolve B3 (5k `resolve_layered`, resolve/SWI=0.0197s) — **wamjs store 9.4× (reads 0.63%), Go store 30× (reads 0.97%)**, Rust term 98×, Go term 776×, cljs 1471×. The store lanes are the scale story: Go store (D70) resolves in 0.593s reading 11,025 / 1,142,225 store bytes = **0.97%**, ~26× faster than Go term's full-catalog 15.28s. Rust store row RESERVED as pending (that lane is next). Measurement caveat documented: SWI's B3 term-reference loader predates the ~8 deb/alternatives 5k-generator rows so SWI B3 used a filtered catalog with those off-path rows removed (Rust B3 differs ~5% with/without — immaterial). Pruning/G1 notes kept but marked historical/other-box. Only `examples/pkg_resolver/BENCHMARKS.md` changed; no target code touched. | (this branch) |
| D73 | wam_rust (store backend) | **Store-backed catalog path on the Rust lane — the store design's fourth target, and the fastest store leg (Opus).** The D43/D48 store design (already on wamjs and, at D70, on Go) now on wam_rust: `resolver_store.pl` (shared adapter, UNMODIFIED — confirmed byte-identical) compiles through wam_rust with its store P/2 predicates served from the dependency-free UWFI/UWIX indexed SEEK store instead of a loaded catalog term. New `examples/pkg_resolver/rust_store/` mirroring `go_store/`. **Inventory finding**: the Rust lane had a foreign-predicate mechanism (`foreign_predicates`, `execute_foreign_predicate`, streaming choice-point retry) but its fact sources were int-keyed edge lookups (CSR/lazy-LMDB), NOT P/2 atom rows; there was no D43 seek reader; `number_string/2` was unimplemented (the term lane uses `v(A,B,C)` terms directly); and the doubled shared-helper clauses from loading both `resolver.pl` + `resolver_store.pl` into `user` needed dropping. The NEW seek reader (`seek_fact_source.rs.mustache`, port of Go's `seekFactSource`): binary-searched sorted `.idx` + positioned `.data` reads via `read_at`, typed key tags, bytes-read counter, `"seek_fact"` native kind streaming `__tuple__/2` rows through the existing choice-point machinery (keyed seek on bound A1, full-scan on unbound A1 for the provides walk). Also added `number_string/2` and chunked the shared-WAM instruction `vec!` past 6000 (the ~9.9k-instruction store crate OOM-killed rustc as one function; smaller crates stay byte-identical). `lmdb(Dir)` registered but fails LOUD (no npm-lmdb reader), matching Go. **B3 headline, reproduced HERE**: `resolve_layered` on the 5k catalog resolves in **0.038 s reading 10,305 / 1,142,225 store bytes = 0.90%** (820 reads), identical 10-package selection — **~51× faster than Rust term (1.93 s)** and, at ~1.9× SWI's native resolve, the fastest store leg in the fleet (Go store 0.593 s, wamjs store 0.186 s). **Verified HERE** (independent, `LC_ALL=C.UTF-8`): store corpus 51/51 identical to term, store differential 503/0 on the 5k catalog, B3 payoff. Term lane regenerated from the updated codegen and re-gated: corpus 51/51, term differential 2600/0 (the committed term crate regenerated so committed artifacts match source). `resolver_store.pl` and `resolver.pl` UNMODIFIED. `BENCHMARKS.md` Rust-store row filled in (was pending at D72). | (this branch) |
| D74 | (wam_rust) lowered-tier design | **Rust lowered-tier throughput plan — staged, to beat SWI on B2 (Opus, design record).** New `docs/proposals/WAM_RUST_LOWERED_TIER_THROUGHPUT_PLAN.md`. The B2 raw-throughput gap (every transpiled target trails SWI; Rust ~8.8×) is the last performance lever; the recorded profile (~65% step-dispatch, ~21% backtrack, ~12% restore_regs) says the win is FEWER WAM dispatches (lower more predicates to native), not faster dispatch. Synthesized from the existing groundwork (Explore pass over MPROLOG_MINING_NOTES F3/F11, WAM_LOWERING_TAXONOMY, the existing `wam_rust_lowered_emitter.pl` sound intermediate, the Rust run-loop/choice-point/trail model, WAM_RUST_STATUS item-2b deferral). **Key finding:** the hard resumable native tier (2b, mprolog F3) was deferred for two still-unsolved obstructions — O1 lowered→lowered calls route through the interpreter `run()` (forces the cp-clean restriction), O2 the per-re-entry `save_regs` snapshot costs ~what dispatch saves. So the plan STAGES it: **Stage 0** census (classify hot predicates F11-eligible vs 2b-only vs already-lowered — decision gate); **Stage 1** F11 self-tail-recursion→native `loop{}` with zero choice-point machinery (lowest risk, dodges O1+O2, gated + interpreter fallback); **Stage 2** the 2b resumable trampoline ONLY on explicit approval after Stage-1 numbers and only with P1 (direct lowered→lowered resume path) + P2 (minimal saved-locals, not full save_regs) designed and per-solution cost PROVEN against the 21%+12% baseline before landing. Every stage keeps corpus 51/51 + term differential 2600/0 + store differential 503/0 green and is justified by measurement. Correctness invariants (trail, backtrack_floor D71, §9 cut barriers, §8 frameless-Y levels, sentinels, multiplicity/order) enumerated. Attribution: F3/F11 ideas from Sasagawa's mprolog (Modified BSD), never code. Implementation of Stage 0+1 launched; Stage 2 reserved for explicit approval. | (this branch) |
| D75 | wam_rust (lowered tier, Stage 0+1) | **F11 self-tail-recursion→native loop — implemented, measured net-negative, shipped gated DEFAULT-OFF (Opus).** Stage 0 census + Stage 1 of the D74 plan. **Census (decision gate, `docs/reports/wam_rust_f11_census_and_stage1.md`):** per-predicate dispatch counter over B2 (2600) + B3 (5k). F11-*shaped* = 46.6% of B2 dispatches, but the safe-to-commit-first-solution AND deep-enough subset is only ~12% (shallow catalog accessors: provides_list/2, base_list/2, depends_list/2, conflicts_list/2, layers_list/2, …); ~34% is F11-shaped-but-2b-in-effect (matching_deps/4→dep_to_req/3, matching_versions/4→satisfies/2, direct_on/4, no_acc_conflicts/4 — deterministic-in-practice bodies reached via `run()`); the rest are true 2b drivers (pick/7, blocked_from/4, dep_breaks/5, build_tree/4) + leaves. **Stage 1:** `wam_rust_f11_lowerable/4` (exactly one tail-recursive clause + pairwise non-unifiable heads as the determinism carrier + pure body) emits a `loop{}` (snapshot clause-entry, try recursive clause first, rebind arg regs, `continue`); 12 predicates lowered when enabled; correctly rejects the user-call/non-tail/non-exclusive ones. **Verdict: net-negative — F11 ON regresses B2 ~7% (23.1s→24.8s, ratio vs SWI ~9.0×→~9.6×), neutral on B1/B3.** Cause = obstruction O2: two per-call register-file snapshots exceed the dispatch removed for the shallow accessors that dominate the safe subset. Per the plan's 'bank it if it helps,' F11 is shipped implemented + probe-tested (`test_wam_rust_f11_tail_loop.pl` 6/6) + **gated `rust_f11_enabled/1` DEFAULT-OFF** — the committed crate is baseline (no regression), carrying only a general `deref_match_atom` empty-list aliasing fix. **Verified HERE** (F11 off, both lanes rebuilt from source): term corpus 51/51, term differential 2600/0; store corpus 51/51, store differential 503/0. resolver.pl + resolver_store.pl UNMODIFIED. **Stage-2 read (confirmed by the census AND by the user's framing — small per-call savings must be amortised over larger native regions to beat the dispatch/snapshot cost):** O2 (minimal saved-locals, not full save_regs) is the gating prerequisite even for the deterministic case; O1 (direct lowered→lowered deterministic calls) is what fuses the ~34% F11-shaped-but-2b bodies into larger native regions where lowering finally pays. Stage 2 reserved for explicit approval. | (this branch) |
| D76 | (wam_rust) lowered-tier review | **External adversarial soundness review of the Stage 2 resumable-lowering design (Kimi K2, via maintainer).** New `docs/reports/wam_rust_stage2_kimi_soundness_review.md`; hard gates folded into the design doc §6a. Verdict: the amortize-over-larger-native-regions insight is sound, but the sketch under-specifies and must clear **five load-bearing gaps**, each with a concrete wrong-answer scenario: **G-1** the resume tuple is insufficient — minimal saved-locals must come from BACKWARD liveness at every resume_arm and FORCE all argument registers live (they are live for the resumed clause even if dead on fall-through) + frame pinning; **G-2** P1 (direct native calls) removes the interpreter's choice-point-leak guard → needs a whole-program `det` lattice + a runtime post-call CP-depth guard at every tier boundary (our existing `lowered_dispatch` check is the first-solution form to generalize); **G-3** cut across tiers needs one linear CP stack, the barrier passed caller→callee explicitly, saved in the CP and restored (never recomputed) on resume; **G-4** pairwise non-unifiable heads prove clause SELECTION not single SOLUTION (a matched body can still be nondet, e.g. member/3) → dropping the CP needs a transitive at-most-one-solution proof (Stage-1 F11's non-unifiable-heads+pure-body is a conservative special case); **G-5** each resume CP needs a validated activation handle (nested self-calls) AND cut must invalidate the predicate's OWN resume CP, not just prune above it. Several gates map onto machinery the runtime already has (lowered_dispatch guard, Arc-cloned stack on ChoicePoint, §9 cut barriers, §8 ITE-levels); the net-new obligations are G-1 forced arg-liveness and G-4/G-5 det lattice + own-CP invalidation. The full build proceeds only against the review's soundness checklist with the seven failure scenarios as stress tests + 2600/503 at 0 divergences. Complements the in-flight cost spike (which measures the O2/O1 crux); together they gate the general build. | (this branch) |
| D77 | (wam_rust) lowered-tier spike | **Stage 2 cost spike — region-fusion + minimal-snapshot beats the interpreter: VERDICT GO (Opus).** Report `docs/reports/wam_rust_stage2_spike.md` + throwaway cost-model prototype `examples/pkg_resolver/rust/stage2_spike/` (standalone crate, not wired into any lane). Family measured: `matching_deps/4 ⊕ dep_to_req/3` (cleanly attributable — dep_to_req is a pure 2-clause rewrite called only from matching_deps). **O2 crux (the thing that killed F11):** minimal-locals snapshot **58 ns** vs full `save_regs` **904 ns = 15.5× cheaper** (46× under register pressure; minimal is ~constant, save_regs scales with caller live-reg count). **Per-call machinery:** interpreter ~2,990 ns/element vs fused native ~65 ns = **~46×** (conservative floor). **Not a toy slice:** over the real 2600-case corpus `save_regs` runs 1,188,875× (live avg 17.94); the matching_deps→dep_to_req region alone = **19.3% of all save_regs and 18.7% of all backtracks**. Real baseline rust B2 23.1s vs SWI 2.65s (8.7×), 2600/0. **Projected B2 delta:** ~6% removable from this one family's machinery (floor), **~15–21% from the full deterministic class (~65–70% of B2 dispatches)** plus step-dispatch → direct native calls; per-call AND per-solution cost beat the required 21%+12% baseline. **One general mechanism covers nondet too:** the minimal-locals snapshot IS the nondet resume representation — extend ChoicePoint with `lowered_state(resume_arm, minimal locals, clause_index)`, deterministic region = degenerate case; measured resumable CP cycle **125 ns vs 1,325 ns = 10.6×/solution**. **Honest caveat:** numbers are from a byte-faithful cost model (real Value types, verbatim save_regs/restore_regs, parameterised to the measured distribution, verified against bytecode), triangulated from three independently-measured real quantities — NOT a wall-clock A/B of a native region wired into the differential (deferred as too risky for a throwaway); the full build must validate the projection with a real A/B. **Recommended build scope:** fuse (1) matching_deps⊕dep_to_req, (2) matching_versions⊕satisfies⊕version_lt, (3) B3's key_dep_rows⊕dep_to_req + group_keyed⊕same_key, (4) re-enable the F11 accessor bank under P2 (minimal snapshot flips their D75 −7% to a win); nondet resume as a later round on the same representation. Gated by the D76 soundness checklist (G-1…G-5). | (this branch) |
| D78 | wam_rust (lowered tier, region 1) | **First fused native region — matching_deps⊕dep_to_req, real A/B CONFIRMS the projection, shipped default-ON (Opus).** Stage 2 build round 1, staged validation per the D77 spike + D76 soundness gates. `matching_deps/4` (dep-list walk) fused with `dep_to_req/3` (pure 2-clause rewrite, called only from it) into one native region `region_matching_deps_dispatch`: **P1** dep_to_req inlined as a direct Rust call (no `run()` round-trip), **P2** rollback snapshot = three scalars (trail/heap/var_counter), NOT `save_regs`; the recursion compiled to a native `loop` (no Allocate/Deallocate, no per-element CP), reusing runtime `terms_identical`/`deref_shallow`/`unify`. **G-gates (D76) satisfied by construction:** G-1 minimal set + all 4 arg regs forced live (never clobbered) + no frame to pin; G-2 pushes no CP / calls no interpreted code so CP-depth is invariant (asserted post-walk, generalises `lowered_dispatch`; off-shape inputs DECLINE to interpreter); G-3 no cut crosses a boundary (dep_to_req cut → Rust if/else); G-4 at-most-one-solution proven ([]/[_|_] exclusivity + committed `->`, dep_to_req cut+catch-all) — NOT relying on head non-unifiability; G-5 vacuous (loop, no resume CP). **Verified HERE** (both configs, both lanes rebuilt from source): region ON (default) and OFF (`UW_REGION1_OFF=1`) — term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0; region-on B2 output byte-identical to region-off across all 2600. 8/8 region stress tests (fusion walk, determinism decision, compound-version `==`, empty base, frame-pinning/arg-liveness, 4 decline paths). **REAL wall-clock A/B** (6-round interleaved drift-cancelling, two sha256-distinct binaries): OFF median 23,272 ms vs ON 21,648 ms = **−6.98% B2** (all six rounds negative, −4.9%…−12.2%), ratio ~9.0×→~8.3× SWI — **CONFIRMS the spike's ~6% machinery-floor projection end-to-end** (not just the cost model). Default ON. resolver.pl/resolver_store.pl UNMODIFIED. **Recommendation GO for the remaining regions** (matching_versions⊕satisfies⊕version_lt, B3 key_dep_rows/group_keyed, re-enable F11 accessor bank under P2), each behind its own flag + its own real A/B; nondet resume a separate later round. | (this branch) |
| D79 | wam_rust (lowered tier, region 2) | **Second fused native region — matching_versions⊕satisfies⊕version_lt, real A/B CONFIRMS (better than proportional), shipped default-ON (Opus).** Stage 2 build round 2, same recipe as D78 region 1. `matching_versions/4` (package-version list walk) fused with `satisfies/2` (7 clauses) and `version_lt/2` (the full Debian Policy §5.6.12 chain: segs_lt/pad_head/segs_lt_1/order_lt/order_val) into one native `region_matching_versions_dispatch`: recursion→native loop, the whole satisfies/version_lt chain inlined as Rust (P1, never `run()`), 3-scalar minimal snapshot (P2). **Shared callee handled:** satisfies/version_lt are called from many other sites — inlined a PRIVATE native copy for the region only, leaving the interpreted predicates fully intact, so every other call site is byte-identical to region-off BY CONSTRUCTION (no cross-site analysis); a stress test proves the private copies mutate no machine state. **G-4 determinism** proven via the cut/committed-`->` bodies + mutually-exclusive constraint-functor and v/3-vs-deb/3 heads (not head non-unifiability alone); version_lt matched to SWI exactly via a 10×10 truth-table gate. G-1/G-2/G-3 as region 1; G-5 vacuous (loop). **Verified HERE** (both configs, both lanes from source, region 1 stays ON): region-2 ON (default) and OFF (`UW_REGION2_OFF=1`) — term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0; region-on B2 output byte-identical to region-off (same sha256). 15/15 region-2 stress tests (full version_lt 10×10 truth table, named Debian edges ~rc/+dfsg/epoch/revision/numeric-not-lexical, v/3 path, all 7 satisfies clauses, shared-callee purity, 6 decline paths). **REAL wall-clock A/B** (6-round interleaved, sha256-distinct binaries): OFF median 21,962 ms vs ON 20,412 ms = **−7.06% B2** (all six rounds negative), ratio ~8.9×→~8.2× SWI — **CONFIRMS, better than proportional** (family is 11.1% of B2 dispatches but fusion removes the whole satisfies/version_lt/segs_lt chain, not just matching_versions'). Default ON. Cumulative Rust B2 vs SWI: ~9.0× (pre-Stage-2) → ~8.2× after regions 1+2. resolver.pl/resolver_store.pl UNMODIFIED. **Remaining: GO** — B3 key_dep_rows/group_keyed (reuse dep_to_req/same_key); F11 accessor bank re-enable under P2 needs its OWN A/B (accessors shallower); nondet resume a later round. | (this branch) |
| D80 | wam_rust (lowered tier, B3 regions) | **B3 index-builder regions — key_dep_rows⊕dep_to_req + group_keyed⊕same_key, real B3 A/B −56%, shipped default-ON (Opus).** Stage 2 build round 3, same recipe. **3a `key_dep_rows/3⊕dep_to_req/3`**: region-1 shape (last-call recursion, top-down list + monotone counter), `region_dep_to_req` REUSED verbatim → native loop. **3b `group_keyed/2⊕same_key/4`**: each iteration consumes a variable-length run via same_key then resumes at the leftover — handled as **nested native loops with an explicit accumulator** (outer index i, inner scan j, outer resumes at i=j); solution order preserved, **no resume-state CP** (stays deterministic, does NOT enter the nondet round); one region serves both call sites (dep rows and pkg rows) via a generic destructurer. **G-gates:** G-1 minimal 3-scalar snapshot + arg regs forced live; G-2 no CP/no interpreted call, CP-depth asserted, off-shape declines; G-3 no cut crosses a boundary; G-4 at-most-one-solution on committed bodies + `==` never binds; G-5 vacuous (loops). **Verified HERE** (both configs, both lanes, regions 1+2 stay ON): term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0 — green ON and OFF; B3 term output byte-identical ON vs OFF, store scale selection identical (10). 18/18 new stress tests (full lib 175/175). **REAL wall-clock B3 A/B** (5000-pkg resolve_layered, 6-round interleaved, sha256-distinct binaries): OFF median ~1996 ms → ON ~872 ms = **−56%** (all six negative) — **CONFIRMED**, large by design (these are ~68% of B3 dispatches). B2 spot-check: no regression (slight gain). Store lane B3 neutral — resolve_layered_store never invokes the index builders (region never entered; confirmed). Both default ON. resolver.pl/resolver_store.pl UNMODIFIED. **Remaining:** F11 accessor bank re-enable under P2 (own A/B needed — accessors shallower); build_tree/4 (21.9% of B3, non-tail tree builder) — assess resume-CP need first (may be nondet round or an explicit-stack region like 3b); genuine backtracking drivers pick/7, blocked_from/4, dep_breaks/5 are the nondet round's real target. | (this branch) |
| D81 | wam_rust (lowered tier, region 4 + F11 verdict) | **build_tree/4 lowered as deterministic region 4 (B3 −41%), F11 accessor bank verdict OFF — the deterministic tier closes (Opus).** Final deterministic cleanup round (survived a container restart + a session rate-limit; re-run pushed per-increment). **Region 4 (build_tree/4):** shape verdict = single clause with a hard `->` commit on `N=:=0` → **deterministic, at-most-one-solution, no resume-state CP** → belongs to the deterministic tier, NOT the nondet round. Non-tail (two self-calls then build node) handled as **bounded native recursion over the materialised input** (balanced split, O(log N) depth ≈ explicit stack), P1 direct-native + P2 3-scalar minimal snapshot, difference-list Rest = captured input suffix. G-1..G-5 satisfied; G-5 vacuous (no resume CP). **Real B3 A/B** (5k resolve_layered, regions 1/2/3 ON both legs, sha-distinct binaries): OFF ≈911 ms → ON ≈539 ms = **−41%** (all 6 negative), byte-identical output, selection 10. Store resolve path never calls build_tree → inert there. Default ON. **Task B — F11 accessor bank under P2: bounded attempt did NOT flip it → stays OFF.** Structural finding: F11 clauses REBIND argument registers, so the loop needs a per-iteration register snapshot — the regions' 3-scalar minimal snapshot cannot apply. The cheap fix (drop the 200-register clear) was correct (2600/0/0 with F11 ON) but F11-ON measured **median +4.2% B2 slower** (the remaining cost is the per-instruction `vm.step` String-dispatch — a codegen rewrite, out of bounded scope, not worth it for shallow accessors). Experiment reverted; committed surface is region 4 only; `rust_f11_enabled` OFF. Amortization theory CONFIRMED: shallow predicates don't pay even with the cheap snapshot — the fused regions do because they bundle real work. **Verified HERE** (both configs, both lanes; regions 1/2/3 ON): term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0 — green with region 4 ON and OFF; B3 output byte-identical. cargo lib 186/186 (region4_build_tree_tests 11/11). resolver.pl/resolver_store.pl UNMODIFIED. **Deterministic tier COMPLETE:** regions 1,2,3a,3b,4 lowered (matching_deps, matching_versions⊕satisfies⊕version_lt, key_dep_rows⊕dep_to_req, group_keyed⊕same_key, build_tree). Cumulative B3 resolve_layered ≈2000 ms → ≈530 ms (**≈−73%**) vs pristine interpreter; B2 down ~9.0×→~8.2× SWI (regions 1+2). **Remaining ONLY for the nondet round:** the genuine backtracking drivers pick/7, blocked_from/4, dep_breaks/5. | (this branch) |
| D82 | (uw-resolve) benchmarks | **Benchmark refresh — Stage-2 Rust lowered-tier gains captured (Opus, single-box 2026-09-07).** `examples/pkg_resolver/BENCHMARKS.md` re-measured on this 4-core box + new `docs/reports/uw_resolve_bench_2026_09_07.md`. All legs gated then timed (corpus 51/51, term 2600/0, store 503/0). **Rust term B2 ~23s→20.7s = ~8.2× SWI** (was 8.8× at D72; regions 1+2). **Rust term B3 `resolve_layered` regions ON vs OFF = −74%** (OFF median 1937.7ms → ON 504.8ms, 6-round interleaved drift-cancelling A/B, sha-distinct verified-relink binaries: OFF 0 region arms, ON 5), byte-identical output, selection 10 — reproduces the D81 cumulative ≈2000→≈530ms. Store legs unchanged from D72 (index builders inert on the store resolve path; bytes_read/n_reads byte-identical). B3 ratios vs SWI now: Rust store 2.4×, wamjs store 8.1×, Rust term 26× (was 98× at D72), Go store 29×. **Merge diligence:** the benchmark subagent tripped a security classifier on a measurement command (sha256/process-control in the A/B harness); coordinator independently verified the commit is docs-only + in-scope (no code/spec/template/target touched), scanned both files clean, and REPRODUCED the headline (6 warm 5k runs hit the report's ~505ms at the low-variance end; box is noisy, interleaved A/B is what cancels it). SWI B3 carried over (frozen loader can't build the current 5k generator's version/alternatives rows). Only BENCHMARKS.md + the report changed. | (this branch) |
| D83 | (wam_rust) lowered-tier classification | **Committed-choice recursion = deterministic — classification refinement before the 2b nondet round (project owner; design record).** Plan §6b added. Principle: a recursion whose body makes a nondeterministic call is deterministic overall if backtracking is forbidden from re-entering that call past the iteration boundary — the per-step nondet call is committed (once/1, a cut before the tail call, or `->`) so exactly one solution is produced and no earlier iteration's nondet call is re-entered on backtracking. Such a recursion lowers with the CHEAP deterministic mechanism (native loop/explicit-stack + P2 minimal snapshot, NO resume-state CP, like regions 1–4) and the hard gates are vacuous for it (G-4 multiplicity/order, G-5 activation identity do not arise with no CP left). So the classifier must decide, per apparent backtracking driver (pick/7, blocked_from/4, dep_breaks/5): (a) committed-per-iteration → deterministic path (preferred — safer + faster), or (b) genuinely exposes alternatives → resume-state trampoline. **In-project reference: PLAWK** (`examples/plawk/core/plawk_core.pl`, `codegen/plawk_native_codegen.pl`, `probes/`, TUTORIAL) compiles exactly this committed-choice recursion to a deterministic native loop despite a nondet/meta call — the deterministic-path eligibility test mirrors PLAWK's. Relayed to the in-flight nondet spike so it classifies the drivers before building any trampoline. | (this branch) |
| D84 | (wam_rust) lowered-tier classification | **Second deterministic pattern: aggregate-then-iterate (project owner; design record).** Plan §6b extended. In addition to committed-choice recursion (D83), PLAWK uses: turn the nondeterministic part into an AGGREGATION (findall/bagof/setof/aggregate_all — itself deterministic, fully explores and returns one list) and ITERATE DETERMINISTICALLY over the result. The backtracking is contained at the aggregate boundary; the hot outer fold has NO resume-state CP → cheap deterministic lowering, G-4/G-5 vacuous. The aggregate call stays an interpreted builtin (or lowered separately later); the iteration over its result lowers. Driver classification is now three-way: (a) committed-per-iteration recursion, (b) aggregate-then-iterate, (c) genuinely alternative-exposing → only (c) needs the trampoline; (a)/(b) preferred. (b) applies only where a predicate ALREADY has the shape — never by editing the frozen spec. Reference: PLAWK `codegen/plawk_native_codegen.pl` is full of `findall(X, member(X,L), Xs)` aggregate-then-iterate idioms. Relayed to the in-flight nondet spike (classify all three drivers three-way before building any trampoline). | (this branch) |
| D85 | (wam_rust) nondet driver classification | **Backtracking drivers classified — the nondet round mostly closes; one cheap deterministic win surfaced (Opus, design record).** `docs/reports/wam_rust_nondet_driver_classification.md`; resolver.pl NOT modified, no trampoline built. Three-way classification (per §6b) of the drivers, evidence from frozen resolver.pl: **dep_breaks/5 = class (a) committed-per-iteration → ALREADY deterministic** — single clause `(HN==N,HV==V,dep_breaks_need(...) -> COut=CBroken ; dep_breaks(Rest,...))`; the nondet `dep_breaks_need` (member/2) sits in the `->` condition, committed before the sole tail self-call, and is consumed first-solution by first_broken/4. At-most-one-solution, no CP. **No rewrite needed — only a recognizer widening** (generalize the region-4 committing-`->` detector to accept a committing condition that contains a nondet sub-goal). Cheap deterministic lowering, no spec change. **pick/7 = class (c) essential nondeterminism AND dead code** — `pick(classic,...)` is bare uncommitted version enumeration (candidate_versions + member exposes each version upward = the resolver's version-backtracking search), but has NO callers (only pick_need/8 is live), never enters the corpus. Not locally rewritable; moot. Leave interpreted. **blocked_from/4 = class (c) genuine generator — CANNOT be made deterministic without changing results** — uncommitted req(Name,C) clause chain (not mutually exclusive) × walk_pkg_for_blocked × member × uncommitted recursion; its **full multiplicity/order is observed** by `findall(B, explain_blocked(...), Bs)` in test_pruning_probes.pl + test_resolver.pl, so first-solution commit would break the test contract. Only a high-risk subsystem-level aggregate-then-iterate refactor (all-solutions list in exact generator DFS order; deterministic consumer blocked_acc partly exists) could do it. **Keep interpreted; record as its own future resolver round.** **Net:** no free deterministic rewrite for the two class-(c) drivers, and **no target should build a resume-state trampoline for these this round**; resolver.pl stays frozen (no rewrite round needed for pick/7 dead + blocked_from/4 too risky). The single actionable low-risk win is lowering dep_breaks/5 as a new deterministic region via recognizer widening (no spec change). Awaiting human decision. | (this branch) |
| D86 | (wam_rust) lowered-tier classification | **The deterministic-recursion family is broader than tail recursion (project owner; design record).** Plan §6c added. The lowering recognizer must target a FAMILY of deterministic recursion shapes, not assume a tail loop — proven by the landed regions: 3b (group_keyed) and 4 (build_tree) are NON-tail recursions lowered via explicit accumulator/stack, not loops. Enumerated shapes: tail recursion → native loop (F11; regions 1/2/3a); non-tail via explicit accumulator/stack (regions 3b/4); committed-choice recursion (§6b; dep_breaks/5, region 5 in flight); aggregate-then-iterate (§6b). **Project owner named the concrete additional patterns to cover: linear recursion, transitive closures, tree recursion, mutual recursion.** Design consequence: the eligibility recognizer is a CLASSIFIER over a family of deterministic recursion shapes (each with its native emission — loop / explicit stack / committed-`->` / fold), interpreter-decline outside it; a survey of the project's deterministic recursion patterns (PLAWK + resolver) should drive a general, target-agnostic recognizer so every backend applies it. | (this branch) |
| D87 | (uw-resolve) deterministic-recursion taxonomy | **Survey of the project's deterministic recursion patterns → general-recognizer design (Sonnet 5, read-only; escalations resolved by coordinator/Opus).** `docs/proposals/DETERMINISTIC_RECURSION_TAXONOMY.md` (961 lines); no code/spec/target touched. Covers 8 patterns with concrete PLAWK/resolver.pl file:line examples, marker × emission × status each: tail recursion (regions 1/2/3a ON; F11 bank OFF; siblings filter_satisfies/key_pkg_rows/tree_lookup = gap); linear non-tail post-order (gap, topo_one); non-tail via explicit accumulator (region 3b ON); tree recursion (region 4 ON); mutual recursion (SCC/Tarjan + whole-SCC at-most-one-solution — segs_lt/segs_lt_1 already ON as a side-effect of region 2; lookup_held/item_ver and topo_all/topo_one = gaps); committed-choice (dep_breaks/5 widening recommended; close_moving fixpoint = new gap; PLAWK process_all/4 = meta-call gap); aggregate-then-iterate (no target yet); transitive closure (worklist/post-order + finite-domain Seen — inst_walk, topo_all/one = gaps). **Two soundness escalations resolved by coordinator and written in:** meta-calls (call/N) join the committed-choice class only under three preconditions (closed-world target set, closure/partial-application resolution, MODE-sensitive per-target determinism); transitive-closure sort-dedup is moot given faithful reproduction — the emission must reproduce the source's exact aggregate/reduction operator, never substitute. **Ranked gaps:** (1) widen committed-choice recognizer (covers dep_breaks/5 + close_moving + eventually process_all/4); (2) process_all/4 (highest PLAWK value, still interpreted); (3) the sibling-gap family (the case for a general recognizer over one-off regions); (4) mutual recursion as an explicit SCC recognizer (mechanism proven by region 2); (5) aggregate-then-iterate emitter (order/multiplicity risk); (6) transitive closure (cold in this corpus, matters for other projects). **Recommended: a compositional `deterministic_recursion_class/2` pipeline** — SCC decomposition → meta-call resolution → per-clause self-call arity/position → commit/aggregate-boundary detection → memoized whole-SCC at-most-one-solution proof → termination classification (structural-decrease vs finite-domain-Seen) → mechanical emission selection, with the regions' runtime decline-guard as the safety net (doc §11). Mutual-recursion emission (call-cycle without stack blowup) flagged for a possible deeper look. | (this branch) |
| D88 | wam_rust (lowered tier, region 5) | **dep_breaks/5 lowered — committed-choice recursion, real B2 A/B −3.15%, default-ON (Opus).** The first committed-choice region (§6b): a recognizer/emitter widening, NO resolver.pl change. Generalizes the region-4 committing-`->` family — where region 4 special-cases an arithmetic semidet test (N=:=0), region 5 accepts a committing condition whose per-iteration goal CONTAINS a nondet sub-goal (dep_breaks_need → member/2), because the `->` commits the first solution before the sole tail self-call → the whole predicate is single-solution. Detection: `rust_region5_dep_breaks_ok` matches `( Cond -> Then ; Self(Rest,...) )` via `=@=` + the two committed sub-predicates (dep_breaks_need/selected_ver, reusing region 2's satisfies/version_lt shape checks); wired into `lowered_call` after region 4. Emission: native loop over the depends list, per element commit-or-continue, P2 3-scalar snapshot, no CP; exhaustion → genuine failure. **G-gates:** G-4 at-most-one-solution proven FROM THE COMMIT (not head-exclusivity), doubly confirmed by first_broken/4's first-solution consumption; G-1 minimal snapshot + arg regs preserved; G-2 no CP/no interpreted call, off-shape declines; G-3 cuts are Rust ifs (no boundary crossing); G-5 vacuous. **Verified HERE** (both configs, both lanes, regions 1–4 ON): term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0 — identical OFF and ON; output byte-identical ON vs OFF across all 2600 (0 diff); full lib 199/199, 13 region-5 stress tests. **REAL B2 A/B:** 16-round drift-cancelling interleaved, sha-distinct verified-relink binaries (OFF 0 arms, ON 1 arm), **median −3.15% / mean −2.73% / 13-16 rounds negative / t≈−4.6 significant** — CONFIRMED, consistent with dep_breaks/5's ~4.9% dispatch share. Default ON. resolver.pl/resolver_store.pl UNMODIFIED. This is the committed-choice recognizer's first instance; per the D87 taxonomy the same widening path also reaches close_moving's fixpoint and (with meta-call handling) PLAWK process_all/4. | (this branch) |
| D89 | (core + wam_rust) general deterministic-recursion recognizer | **The general `deterministic_recursion_class/2` pipeline — one structural classifier lowers the sibling-gap family, target-agnostic core (Opus, taxonomy §11).** New shared core `src/unifyweaver/core/deterministic_recursion.pl` (stages 1–6, TARGET-AGNOSTIC): call-graph + SCC (Tarjan-equiv) → meta-call preconditions → per-clause self/SCC-call arity+position → commit/aggregate boundary → structural at-most-one-solution proof (variable-sharing, not `=@=`) → termination class. Only stage-7 emission is per-target (`rust_genrec_arms/3` + `rust_genrec_shape_method/2` in wam_rust_target.pl). Reusable by every WAM backend — the classifier is not Rust-specific. **LOWERS 3 sibling-gap predicates, NONE covered by any hand-written region:** `filter_satisfies/3` (list_filter), `key_pkg_rows/3` (list_map_index), `tree_lookup/3` (bst_descent) — three distinct shapes via one classifier. **DECLINES (detected, emission deferred):** close_moving + dep_breaks (committed; region 5 covers dep_breaks), mutual SCCs segs_lt/segs_lt_1 (tail, already region-2-fused), lookup_held/item_ver + topo_all/topo_one (non-tail cross-call — need explicit stack, deliberately not half-built), process_all/4-style meta-calls (decline(meta_call_unresolved)). Regions 1–5 untouched and win on overlap; the decline-guard is the safety net. **Verified HERE** (both configs, both lanes, regions 1–5 ON): term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0 — genrec ON and OFF; output byte-identical ON vs OFF over all 2600 term cases AND scale-5000 (where all three fire). cargo lib 220 + classifier plunit 12 + genrec 21 stress tests (all shapes + every decline path). **Real B3 A/B** (20-round interleaved, sha-distinct binaries): scale-1000 −18.09%, scale-5000 −23.84% (2 preds) / −21.38% (all 3), non-overlapping ranges → default ON. (B2/store don't reach these predicates: correct-but-neutral there.) resolver.pl/resolver_store.pl UNMODIFIED. **Follow-ups:** mutual-recursion stack-safe cyclic emission; close_moving committed emitter; meta-call/process_all 3-precondition layer; aggregate-then-iterate emitter; and eventually SUBSUMING regions 1–5 through genrec (the classifier already recognizes key_dep_rows identically and classifies matching_versions/matching_deps as committed). | (this branch) |
| D90 | (uw-resolve) benchmarks | **Benchmark refresh — full Rust lowered tier (regions 1–5 + general recognizer), single-box 2026-09-07 (Opus).** `examples/pkg_resolver/BENCHMARKS.md` + `docs/reports/wam_rust_bench_refresh_full_tier.md`; docs-only. All legs gated then timed (corpus 51/51, term 2600/0, store 503/0). **Headline: Rust term B3 `resolve_layered` full-tier ON-vs-OFF = −80.7%** (pristine ~1927 ms → ~372 ms, 8-round interleaved drift-cancelling, sha-distinct binaries: OFF 0 dispatch arms, ON 9 = regions 1–5 + genrec's filter_satisfies/key_pkg_rows/tree_lookup, byte-identical selection) — deeper than D82's −74% (region 5 + genrec add ~7 pts). **Honest Rust-vs-SWI, per axis:** startup B1 Rust 0.048s BEATS SWI 0.113s; throughput B2 SWI still ~7.2× faster (Rust 19.1s vs 2.64s — down from 8.8×→8.2×→7.2× as regions landed); term resolve B3 SWI ~19.5× faster (gap collapsed 98×→26×[D82]→~19.5×); store resolve B3 SWI ~2.0× (Rust store 0.040s vs 0.0197s — within a factor of two). Rust is faster than SWI ONLY on startup; the Rust-vs-Rust improvement %s are gap-closing, not overtaking. Store legs unchanged from D82 (bytes-read/read-count byte-identical; index builders inert on the store path). SWI B3 carried over (frozen loader can't build the current 5k generator's rows — verified). | (this branch) |
| D91 | (wam_rust) hotspot profile | **Full-tier profile — dispatch is GONE, next lever is the runtime Value/functor representation, NOT more lowering (Opus).** `docs/reports/wam_rust_hotspot_profile_full_tier.md`; docs-only. Same UW_PROF dispatch census + callgrind Ir as the old 65/21/12 baseline (cache-sim off, so allocator's real wall share is higher than its Ir share). **Dispatch collapsed** (step/backtrack/restore_regs self-Ir → 3.3% B2 / 0.3% B3; restore_regs 12%→3.2%/0.3%) — the lowered tier maxed out its lever. **New hotspots, and the two workloads diverged:** B2 (differential) is **backtrack+allocate bound** — ~61% memory mgmt (malloc/free/drop), ~16% deref_heap (142M calls), ~11% functor-string 'f/N' handling; 479,531 backtracks over 2,600 nondeterministic queries (backtrack inclusive 21%→72%). B3 (5k resolve) is **sort+deref bound** — ~49% memory mgmt, ~24% deref, ~22% functor-string+compare; one msort/2 that eager-derefs every element then sort_by(term_compare) on 'f/N' strings (term_compare 63%, sort_by 46%), only 1,296 backtracks (backtrack 21%→1.8%). **Declined-predicate interpreter fallback is only ~3% B2 / ~0% B3 of TIME** (dispatch is cheap now) — so more genrec/lowering has a ceiling of ≤3% B2 / ~0% B3; the declined preds are expensive via term alloc/deref where they run, not dispatch. **Ranked next lever:** (1) INTERN FUNCTORS (u32, kill 'f/N' strings) — ~11% B2 + ~20% B3, helps both, cross-cutting; (2) cut Value-alloc churn (2a deref_heap memoization both; 2b B2 backtrack saved-state; 2c B3 msort decorate-sort key-caching, attacks the 46%/63% sort); (3) store path as default for large catalogs — ~10× at B3 scale (40ms vs 385ms), the LMDB-at-scale lever; (4) more genrec — ceiling ≤3%/~0%, do NOT lead with it. Strategic pivot: from 'lower more predicates' to 'optimize the runtime term representation' (functor interning + alloc churn), which helps every predicate and is a runtime change other targets can mirror. | (this branch) |
| D92 | (wam_rust) backlog | **Consolidated deferred/backlog (design record) — captures the user's 'look at LMDB in the future' + the profile-ranked runtime levers.** Plan §10 added: single ordered view of what's left. (1) Runtime term-representation levers (D91's actual next-biggest): intern functors (u32, ~11% B2/~20% B3) + cut Value-alloc churn (deref memoization, backtrack saved-state, msort key-caching). (2) **Genuine LMDB catalog tier on Rust (user-requested)** — the lmdb(Dir) tier fails loud on native Rust today; wire a real Rust LMDB reader (heed/lmdb-rkv) then MEASURE vs the working D43 indexed seek store before committing (indexed store already reads <1%, so LMDB's win is unproven; measure-first). (3) blocked_from/4 aggregate-then-iterate resolver round (high-risk, D85). (4) mutual-recursion stack-safe cyclic emission (detection done D89). (5) remaining genrec emitters (close_moving, meta-call/process_all, aggregate-then-iterate; ceiling ≤3%/~0% per D91). (6) subsume regions 1–5 into genrec (cleanup). (7) cross-target reuse of the shared-core classifier + store-as-default to C++/Go/wamjs. (8) the 2b resumable trampoline (shelved; no driver yet worth it). Store-as-default-at-scale is in flight separately. | (this branch) |
| D93 | (uw-resolve) store-as-default at scale | **Size-gated auto-switch (term→indexed-store ladder) — the store path is now the automatic default at scale (Opus).** New `examples/pkg_resolver/resolve_auto.mjs` + `run_auto_switch_test.sh`; frozen spec + crates UNCHANGED (driver-level routing only). **Assessment:** two separate binaries with no chooser today (rust/ term = whole catalog as WAM terms; rust_store/ = D43 indexed seek store keyed CatId|Name); the store shim accepts the SAME term-format case line, so the switch only picks which binary reads each case — no payload transform. Store is a PRE-BUILT artifact (store-dir baked into the crate at compile time; data re-indexes sub-second, one dir holds many catalogs by CatId → build binary once, resolve many); build-on-demand-per-resolve never wins (67s cargo ≫ any resolve), so the driver assumes a cached store and gates on availability. **Implemented:** an ordered size→backend LADDER (`{minPackages→backend}`, backends an enum/extension point) — default threshold 500 pkgs (below→term, ≥→indexed), overridable (`--threshold`/`UW_RESOLVE_STORE_THRESHOLD`), forceable (`--backend`/`UW_RESOLVE_BACKEND`, forced-but-unavailable exits 2), demotes to term when no store (never a wrong/failed resolve); store-routed cases stamped with the store's catalog_id. **A future LMDB rung slots in as a third tier** with a registry entry (marked in source) — the ladder is built for it. **Crossover:** term load+resolve linear (50ms@371 → 103ms@1511 → 403ms@7522); store resolve FLAT ~40ms (reads 0.9–4%); break-even ~200–300 pkgs; ~10.7× at 7522 (binary-level). **Verified HERE:** all four gates pass (term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0); `run_auto_switch_test.sh` — auto→store, force-store, force-term, small→term, mixed-stream, store-unavailable fallback ALL byte-identical to the term oracle. resolver.pl/resolver_store.pl unmodified. **Follow-up:** same seam for Go/wamjs stores (per-target crossover re-measure); LMDB third rung (deferred, backlog §10.2). | (this branch) |
| D94 | (wam_rust) hot-path deep profile | **Per-site deep profile — every D91 hot category attributed to concrete sites, ranked worklist (Opus; DHAT + callgrind cache-sim).** `docs/reports/wam_rust_hotpath_deep_profile.md`; docs-only. **63% of B2 allocations / 76% of B3 allocations are tiny atom/functor/var-name STRINGS** (deref_heap + deref_var). deref re-walks+clones the whole term tree per top-level deref, even ground terms; functors are stored as `"name/arity"` Strings in `Value::Str`. **Cache-sim overturns D91's caveat:** LL miss rate ~0.0% both workloads → allocator cost is INSTRUCTION-bound, not latency-bound → Ir is a faithful proxy and cutting allocation COUNT is exactly the lever. Existing sharing confirmed carrying its weight (Arc<Vec<StackEntry>> stack 0.7%, Args structural sharing, save_regs — none are top sites); the leftover churn is the deref subsystem's spine rebuilds + name-String clones + the string-tagged trail. **Ranked per-site worklist:** #1 **decorate-sort the msort comparator** (`state.rs:13354`/`13071`, B3, LOW-RISK one-fn change — `term_compare` re-derefs+allocs a functor String on EVERY comparison; the comparator path is 61.6% of ALL B3 Ir; a `term_compare_derefed` trusting the O(n) pre-deref → **~30–40% B3**); #2 **intern functors+atoms+var-names → u32** (~11% B2 / ~15–25% B3; high blast radius — ~425 Atom + ~230 Str sites, MAJORITY in generated lib.rs ⇒ a codegen change in wam_rust_target.pl + value.rs/state.rs + shim, not a hand-edit); #3 **deref_heap ground-bit/memo** (`:4665`, both — top non-allocator fn 9.5%/13.8% self-Ir + 48 MB spine rebuilds); #4 **enum-tag the trail** (`bind_var` does `format!("__binding__{}")`+`to_string()` = 2 String mallocs/bind → `TrailEntry.key: String`→enum, B2). #5 store-default at scale, #6 more genrec (≤3% B2/~0% B3, do not lead). | (this branch) |
| D95 | wam_rust (hot-path opt #1) | **Decorate-sort the sort comparator — B3 −60% Ir / −63% wall (2.70×), byte-identical, default-ON (Opus).** The D94 #1 lever. `term_compare` re-deref'd BOTH operands (`deref_heap(deref_var)`) and allocated a fresh functor String on EVERY comparison (the comparator path was 61.6% of all B3 Ir). Fix: new `term_compare_derefed` that TRUSTS pre-deref'd operands (no deref, no per-node alloc) + decorate-sort wiring — `sort/2`, `msort/2`, `sort/4`, `keysort/2`, `setof` now sort/dedup their pre-deref'd keys via a `sort_cmp` dispatch (`compare/3` and the `@<` family keep raw `term_compare`, operands not pre-deref'd). Functor-String alloc avoided WITHOUT interning by comparing via borrowed `functor_of` (`&str`) instead of owned `display_functor_name` (byte-identical normalisation, deref_heap already normalised) + a borrowing `value_atom_name_ref`. Lives in the generator `wam_rust_target.pl` (emitted into state.rs), NOT the mustache. Gated Cargo feature `decorate_sort` (default ON). **Verified HERE** (both configs, both lanes): term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0 — ON and OFF; output byte-identical ON≡OFF (sha match on differential + 5k selection). **Real A/B:** callgrind Ir OFF 4.03B → ON 1.61B = **−60.0%**; wall (6-round interleaved) 350.5ms → 129.8ms = **−63.0%, 2.70×** (reproduced HERE: B3 resolve 129.8ms); projection (~30–40%) exceeded — the re-deref was nearly the entire comparator cost. B2 +1.0% (noise, no regression; B2 barely sorts). 5 comparator-equivalence stress tests. resolver.pl/resolver_store.pl UNMODIFIED. **Round #2 note:** interning's B3 win is now measured against this smaller 1.61B baseline (deref ~80% smaller absolute) while its B2 case is unchanged/stronger; `functor_of` is now a load-bearing comparator equality-normaliser — interning to u32 must preserve `functor_of`/`display_functor_name` equivalence (or switch to integer-id compare) to keep sort byte-identical. | (this branch) |
| D96 | wam_rust (hot-path opt #2) | **Intern functor/atom/var names to u32 — B3 −29% Ir/−29% wall, B2 −41% Ir/−32% wall, byte-identical, default-ON (Opus).** The D94 #2 lever. Names in `Value::Atom`/`Value::Str`/`Value::Unbound` were the tiny-String bucket (63% B2 / 76% B3 alloc blocks). New `Sym` name type chosen at compile time by a Cargo feature `intern`: OFF = `type Sym = String` (the verbatim pre-intern path, a perfect A/B baseline needing zero edits); ON = `Sym(u32)` Copy index into a global, append-only, CANONICAL interner (lock-free/wait-free chunked `resolve`, RwLock-read intern fast path, `Box::leak`'d `&'static str` so de-intern has zero lifetime friction). Result: cloning a name is a u32 copy (not a malloc), `deref_var` is allocation-free, and the `"f/N"` functor parse in `deref_heap` interns the normalised name instead of `to_string()`-cloning it. **SORT-ORDER TRAP handled via approach (a):** the comparators de-intern to `&str` for the LESS/GREATER decision (`value_atom_name`/`value_atom_name_ref`/`functor_of`), so standard order stays NAME-correct; the ON `Sym` deliberately impls NO `Ord`, so any accidental order-by-id is a COMPILE error not a wrong answer, and its `PartialEq` (id compare) is sound because interning is canonical (id-eq iff name-eq). Codegen change: `value.rs` template gets the `Sym` infra + interner; ~650 construction sites across the generator, mustache templates and both shims route names through `.into()`/helper ctors (identity under OFF); comparators, `deref_heap`, copy_term/variant var-maps, env and region kernels de-intern at the String boundaries. **Verified HERE** (both configs, both lanes): term corpus 51/51, term differential 2600/0/0, store corpus 51/51, store differential 503/0 — ON and OFF; output byte-identical ON≡OFF (`cmp` clean on term+store differential, corpus, AND the 5000-package resolve_layered selection, selection_size=10). **Real A/B** (sha-distinct verified-relink binaries, decorate-sort + regions 1–5 + genrec all ON): **B3** callgrind Ir 1,617,957,821 → 1,149,491,479 = **−29.0%**, wall (resolve_ms, 6-round interleaved median) 133.7 → 94.6 ms = **−29.2%** (OFF matches the ~1.61B/~130ms post-decorate-sort baseline); **B2** wall (6-round interleaved) 18,740 → 12,775 ms = **−31.8%**, Ir (600-case subset) 33.13B → 19.62B = **−40.8%**. Both **confirm and EXCEED** D94's projection (~15–25% B3 / ~11% B2) — interning makes EVERY name clone a Copy (deref_var, deref_heap functor, put_reg/backtrack), collapsing the malloc/free/String::clone/drop family (~62% B2 / ~50% B3 self-Ir), not just the parse+clone D94 counted for B2. Gated Cargo feature `intern`, now in `default = ["decorate_sort", "intern"]`; pre-intern path kept via `--no-default-features --features decorate_sort`. 5 interning stress tests (sort-trap proof with reverse-id names, de-intern round-trip, canonical equality, comparator total-order) + the D95 comparator-equivalence suite, all pass both configs. `docs/reports/wam_rust_interning_ab.md`. resolver.pl/resolver_store.pl UNMODIFIED. **Remaining D94 levers:** #3 deref_heap ground-bit/memo (both), #4 enum-tag the trail (B2 bind path). | (this branch) |
| D97 | wam_rust (CI fix for D96) | **Fix two D96-interning regressions that reddened the `test` CI job (Opus).** Adding the `[features]` table (D95/D96) switched ON Cargo's `unexpected_cfgs` lint, which then flagged the pre-existing T7 `#[cfg(feature="parallel")]` sites in `state.rs` (the `parallel` value was never declared) as `warning: unexpected cfg condition value: parallel`; and the `intern` feature's `Value::Atom`/`Value::Unbound` now hold `Sym` not `String`, so the hand-written test fixture `tests/foreign_tuple_aliases.rs` (emitted by `test_wam_rust_foreign_tuple_aliases.pl`) failed `error[E0308]: mismatched types ... expected Sym, found String` at its `Value::Atom(name.to_string())` / `Value::Unbound("...".to_string())` construction sites — and that test does a zero-warning **transactional** `cargo test` build, so both surfaced as `rust_alias_test_failed(exit(101))`. **Fix (metadata + test-only, no runtime semantics):** (1) the generator (`wam_rust_target.pl`) now emits `parallel = []` in the `[features]` table it appends to `Cargo.toml` (declares the cfg name so the lint knows it; stays off/empty — enabling it needs rayon, a hard dep only under the generator's `parallel(true)` option); both committed crates' `Cargo.toml` synced to match. (2) the fixture's three construction sites append `.into()` (String→Sym via the `From<String>`/`From<&str> for Sym` impls the D96 `value.rs` template already provides; identity under the OFF `type Sym = String` build). **Verified HERE:** `test_wam_rust_foreign_tuple_aliases.pl` passes (clean transactional `cargo test`, exit 0); regenerated term crate builds with the `parallel` cfg warnings gone (4 pre-existing dead-code warnings remain, unrelated, ungated); term corpus 51/51 vs SWI; store lane regenerated + store corpus 51/51 vs SWI. No emitted runtime code changed — only the Cargo feature declaration and the test fixture. resolver.pl/resolver_store.pl UNMODIFIED. | (this branch) |
| D98 | resolver pruning design (external review) | **Record + verify Kimi K3's 2nd adversarial review of `RESOLVER_PRUNING_DESIGN.md`; doc-only (Opus, coordinator).** Routed the review against the current head (post-D97). Verified its two runnable claims against the committed `resolver.pl` before recording: (1) **invariant-7 residue** — the §7.2/D60 `is_public_catalog/1` fix only guards the resolve edge (`resolve/3`, `resolve_layered/3`, `layer_closure/3` route through `index_catalog/2`); the other 9 catalog-taking exports reach the delegating `icat/3` accessors directly and accept the wrapper (confirmed HERE with a forced index: `resolve_layered(icat)` fails, but `explain_blocked`/`dependents`/`freeze_audit`(icat) all succeed with correct answers). Low severity (answers correct) but invariant 7 as written is false → §4 restated. (2) **§6.1/§3.2 baseline stale after the H4+H1 round** — re-ran the §6.1 probes verbatim: CE2 and CE6 **no longer reproduce**, CE5 **terminates** (`S=[]`, H4 fixed); CE1/CE3/CE4 still reproduce; the committed `test_pruning_probes.pl` already re-pins CE2/CE5/CE6 (cites `RESOLVER_H4_H1_DESIGN.md`) and passes. (3) **G4 unsound on non-ground provides rows** (design-level; G4 unshipped) — `provides_mentions/2` uses `==` while the search's `already_provided/…` rescue uses unification, so a variable virtual defeats the guard yet satisfies the search (wrong-answer, sharper than §7.4(ii)); binding on any future G4 round as an *executable* ground precondition. G1/G2/G3 + ground-case G4 not falsified. Doc changes: status banner, §4 invariant 7 restated, §3.2 + §6.1 staleness notes, new **§7.5**. No code touched; `resolver.pl`/`resolver_store.pl` UNMODIFIED. Follow-up (owner's call, needs full-fleet re-verify if taken): guard the 9 non-resolve exports, or accept the restated invariant. | (this branch) |

---

## 2. In progress 🔄

| ID | Target | Item | Owner | Branch |
|---|---|---|---|---|
| A1 ✅ | (SWI spec) | **peerhailer argparser in Prolog — DONE** (`examples/cli_args/`): `cli_args.pl` mirrors the oracle exactly — schemas as `Name-schema(Options,Positionals)\|group(Actions)` data, strict + verbatim lenient + leading globals, `ok(Positional,Flags)`/`err(Message)` tagged results (no throw; the JS edge maps `err→CliError`), flags as insertion-ordered KV pairs. Even the JS prototype-chain lookup quirks (`--toString` as a truthy global, `--__proto__` no-op assignment) are modelled. **17/17 plunit** + **5067-line seeded differential vs node oracle: 0 divergences, 0 message mismatches**. Pure/det/tail-recursive, no cuts/throws/pcre — transpilation-ready. A2/A3 re-point `run_differential.sh`'s Prolog leg at the transpiled build (same seed, same bar). | opus | merged |

### Phase 2 — the argparser maturity demonstration (A-series)

Goal: transpile a REAL program — peerhailer's CLI arg parser — through the JS-family
targets, as the maturity proof. Oracle = `examples/cli_args/oracle/cliArgs.js` +
its 17-test corpus `oracle/cliArgs.test.mjs` (copied from peerhailer @ `08ad35e`,
verified 17/17 green under `node --test`). A usage error is a thrown `CliError` at
the JS boundary; the Prolog spec returns `error(Message)` and the transpile maps it
to `throw new CliError(Message)` at the edge, so the same corpus drives both.

| ID | Step | Depends | Owner |
|---|---|---|---|
| A1 | Prolog reference implementation + plunit corpus port + differential harness vs node oracle | — | opus (running) |
| A2 ✅ | **DONE (Grok)** — `examples/cli_args/wamjs/`: `cli_args.pl` compiles through the JS WAM interpreter (reproducible `build.sh`), **corpus 17/17 with one import line changed; differential 5067 lines, 0 divergences, 0 message mismatches** (wamjs ~8.9s vs oracle ~0.18s — interpreter tier, expected). Shim is term-conversion only (`/3` registries supported at the edge). Forced 3 real runtime fixes, each probe-pinned: `sub_string/5` builtin was missing; **Y-register clobber across `Call`** (huge no-`Allocate` ground fact overwrote caller Ys — Call snapshots, Proceed restores; a JS-runtime workaround for frozen `wam_target.pl` fact compilation); `Execute` of a builtin halted the machine instead of returning to the CP. | A1 | grok |
| A3 ✅ | **DONE — the honest measurement**: ≈5% of the parser transpilable by predicate count, **0% by mechanism** (all four engines blocked by G-A3-9/10/12/16). Found + FIXED 7 S-gaps incl. two pre-existing flagship bugs: the batch path emitted **unparseable TS for any two-goal clause** (G-A3-2, never caught because tests assert substrings, not `node --check`) and annotated_js misparse (G-A3-7). Full catalogue G-A3-1…17 in `docs/proposals/A3_PATTERN_TRANSPILE_REPORT.md`; 30-test `test_typescript_cli_args_shapes.pl` (fix regressions + executable gap probes). Open: G-A3-6 guard hoisting (M), **G-A3-8 fact-fallback EXECUTES the predicate — compiler hazard, hangs on ordinary input (M, next)**, G-A3-9 multi-output loops (L), G-A3-10 ITE in recursive body (M), G-A3-11 scaffolding (S), G-A3-12 compound→string (M), G-A3-13 bool→string (S), G-A3-14 `_G` leak (S), G-A3-15 reversible-builtin direction (S), G-A3-16 list/pair head pattern (M). **Follow-up run CLOSED G-A3-8/-11(.1/.2/.4)/-13/-14/-15** (hazard gone: ≤12 ms refusals with actionable errors; JS booleans; named ITE bindings; output-slot-aware reversible builtins; semidet arity-1; real CLI entry; module refusal/warning — shapes suite now 55 tests, `node --check` + SWI-oracle gates in). **Runs 3–5 closed G-A3-10, G-A3-9, G-A3-6, G-A3-12 (+G-A3-16 fell out free)** — and hit the MILESTONE: **the lenient parse mechanism (14 predicates) compiles whole via `compile_module/3` and matches SWI 10/10 under node, no shim**. Cross-predicate calls lowered by callee output-count (semidet→boolean condition, outputs→const/destructure; failure = only the 0-output form, as clause fall-through; det-with-outputs throws); compounds = `{$: tag, args: […]}` (distinguishable from atom/bool/list at runtime, no throwing tests); in-order guard placement fixes the TDZ hoisting bug; mutual recursion works via JS declaration hoisting (verified). Census: **30/43 cli_args predicates compile clean (was 2); 1 of 4 mechanisms end-to-end**; byte-harness 26 shapes × 3 targets = 0 diffs; shapes suite 105 tests. **Sole remaining blocker for strict/globals/schemaFor: a semidet callee WITH outputs in condition position** (needs a failure sentinel + determinacy analysis); plus ground-fact constant tables (`js_object_prototype_keys/1`) and pinned minor probes (var-var `==` identity, `0 is N mod 2` clause-body bug, annotated_js `as`-cast comment mangling — one-line fix noted). | A1 | opus |
| A4 ✅ BOTH LANES | **THE DEMONSTRATION IS COMPLETE for the pattern lane**: `examples/cli_args/patternjs/` — `parse_args/2`, ALL 43 predicates, compiles whole via `compile_module/3` (40 functions + 3 inlined fact tables, 0 refusals), reproducible `build.sh`, thin conversion-only shim. **Corpus 17/17 with one import line changed; differential 5067 lines, 0 divergences, 0 message mismatches (error messages match too)**. Verified through typescript, vanilla_js AND annotated_js. Closed en route: G-A3-18 (semidet-with-outputs via module-private `Symbol` sentinel + can-fail least-fixpoint over the call graph; exactly 9 predicates get it, everything else byte-identical), G-A3-19 (ground-fact tables inlined as match-not-call), G-A3-20 (deferred head-output binding + continuation duplication into ITE branches), arity mangling (`parse_args_2`/`_3`), 2 pre-existing correctness bugs (silent rebinding dropped an `=` test; vanilla strip rule ate `args: [`). Byte harness 78 outputs: 72 identical, 6 changed-with-cause. Shapes suite 116. **wamjs arm now ALSO green: 17/17 + 5067/0/0 (A2)** — the demonstration is complete on both lanes. | A2/A3 | main |

### Phase 2 — further gap items (user-named)

| ID | Target(s) | Gap | Status | Notes |
|---|---|---|---|---|
| GP-PROF | wam_javascript | Runtime profiling/instrumentation | ✅ D38 | landed (Grok) — baseline for GP-PAR |
| GP-PAR | wam_javascript (+pattern) | Parallelism — WAM-level (haskell/elixir/rust precedent) and/or pattern-level | ⬜ | deferred earlier as G-W6; revisit after profiling gives a baseline |
| GP-LMDB | wam_javascript | LMDB-backed fact sources | ⬜ | needs a native/npm dep (`lmdb`), conflicting with the no-deps ethos peer targets follow (Lua skipped it) — needs a decision: allow an optional dep tier, or an mmap-free file index instead |

**Remaining tail** (all low-priority polish / out-of-scope — no active work):
- **Pattern:** bagof/setof-with-witness-grouping at pattern level (G-P3 follow-up — the minimal slice is marginal over aggregate_all(bag/set)); (JSON `schema` mode now landed for the Node consumer, D36; CSV subset projection via `project_columns`, D33); bare positive `match` batch-guard (**not** a clean 2-liner after all — adding `match` to the shared `clause_body_analysis:is_guard_goal/2` regresses two targets: `python_guard_condition/3` has no `match` clause and no catch-all so the guard `maplist` fails (python renders `match` via the output-goal path, not as a guard), and wam's `is_builtin_pred/2` derives builtin-ness from `is_guard_goal` so `match` would emit `builtin_call match/2` that no WAM runtime implements. Only clojure+ts have `match` guard renderers. Proper fix = add `match` guard renderers to python + gate wam's builtin path, i.e. a cross-target change, not polish. D28's deferral stands.); test depth (G-P13).
- **WAM (lane exhausted for term model/builtins):** AVL assoc (list-based today); LMDB/CSR fact sources (out of scope, matches Lua peer); G-W6 parallelism (deferred); fact-source JSON/TSV cells still intern as atoms (D37 residual). Cosmetic only: `\n`/`\t` escapes in `writeq` and brace-term `{a}` special-casing. `op/3`/postfix (D32), real string tag (D34), `string_length/2`+`writeq`/`~q` quoting (D35), and compiled string literals → string tag (D37, the shared-file `wam_target.pl` item) now all landed.

---

## 3. Open gaps — PATTERN targets ⬜ — CONFIRMED by analysis A1

Two extension mechanisms apply: **bindings** (`declare_binding/6`) and the **component
pattern** (`component_registry` + per-target `custom_*.pl`). A1 corrected two of my
suspicions (see §3b N/A): bindings are **not** a gap (JS leads), and TS/AJS register
**all 7** recursion hooks (more than python's 4) — the gap is hook *quality*, not count.

| ID | Target(s) | Gap | Status | Copy-from | Size | Owner |
|---|---|---|---|---|---|---|
| G-P1 | typescript (→ AJS/VJS) | **"Supported" recursion hooks are canned Fibonacci** — `compile_tree_pattern`/`compile_multicall_pattern`/`compile_direct_multicall_pattern(typescript,…)` all emit `const result = pred(n-1)+pred(n-2)` regardless of the real clause (`typescript_target.pl:~1402,1435,1468`; tail arity-2 stubs `return items.length` `:1228`). | ⬜ | extend TS's own partial `native_ts_clause_body` (`:736`); generality from python/go native lowering | L | opus |
| G-P2 | all JS pattern targets | **No structural recursion** (member/append/reverse) — none compile it (`docs/JS_PATTERN_CONFORMANCE.md`); python/rust/go do via native clause lowering. | ⬜ | python_target / go_target native lowering | L | opus |
| G-P3 | all JS pattern targets | **No aggregate compilation** — 0 `aggregate_all/findall/bagof/setof` goal refs in TS/CLJS vs go 36, python 10, rust 10. Core datalog surface. | ⬜ | `go_target.pl` (richest) | L | opus |
| G-P4 | typescript (→ AJS/VJS) | **Components collected but never emitted** — `compile_module/3` (`:414-436`) never calls `component_registry:compile_component` (python `:198`/rust `:378`/go `:14951` do). Also `custom_chart.pl` is orphaned (self-registers at `:219` but no module `use_module`s it → dead). | ⬜ | python_target.pl:198 emit loop | M | opus |
| G-P5 | clojure + clojurescript | **No component support at all** — `clojure_target.pl` has zero `component_registry` refs; `custom_clojure.pl:79` self-registers but is never loaded; no CLJS component. | ⬜ | mirror python_target.pl:96-97,198 | M | opus |
| G-P6 | clojurescript | **DONE (D19).** CLJS `compile_module/3` added. (Structural recursion still tracked under G-P2.) | ✅ | — | — | done |
| G-P7 | typescript (→AJS/VJS) + clojure/CLJS | **Minimal slice DONE (this branch).** Added negation (`\+`/`not`) + type-check (integer/atom/number/is_list/compound/var/nonvar/ground/float, + inert `atomic`) + `member` guard clauses to `ts_guard_condition/3` and `clojure_guard_condition/3` (AJS/VJS inherit TS; CLJS inherits clojure). Negation-of-non-guard-goal handles `member` specially and otherwise fails cleanly (no wrong code). node + nbb verified vs SWI; all 5 target suites green (TS 57, AJS 17, VJS 22, CLJS 26, clj-native 12). See `integration_patches/GP7_INTEGRATION_PATCH.md`. **Still open (separate follow-ups):** regex `match/2,3` (python `translate_match/6` model); `constraint_analyzer.pl` unique/unordered dedup (rust/go call `get_constraints/2`; TS/CLJS don't). | 🔄 (slice done) | python_target guard/negation; clause_body_analysis | S–M | opus |
| G-P8 | typescript (→ AJS/VJS) | **No streaming/pipeline/generator mode** — 0 refs vs python 432/go 390/rust 325. CLJS inherits clojure's `generator_mode`/`pipeline_mode` best-effort. | ⬜ | clojure_target (closest paradigm) or python | M | opus |
| G-P9 | typescript (→AJS/VJS) | **Data-source consumer (RE-SCOPED by recon).** python/rust/go do NOT consume csv/json sources (the old `semantic_source.pl` dispatch claim was wrong). Only PowerShell-pure consumes them (via `_suffix` templates + its own path; `powershell_compiler.pl:225-239`), and C# via a query-plan/metadata model. **Minimal slice = Option A: mirror PowerShell-pure** — add `_typescript` templates to `csv_source.pl`/`json_source.pl`, a thin wrapper module, + one routing clause in `compile_predicate_to_typescript/3` (before the fallback) checking `dynamic_source_compiler:is_dynamic_source/1`. Independent path; no native-clause/guard changes. Option B (C#-style relation model) = separate L task, not "catching up" since python/rust lack it too. **DONE (this branch, Option A):** added `json_file_source_typescript`/`json_stdin_source_typescript` + `csv_source_unary_typescript`/`csv_source_binary_plus_typescript` templates (additive only), new wrapper `targets/typescript_source_compiler.pl` (`compile_to_typescript_source/3`), and one routing clause in `compile_predicate_to_typescript/3` before the fallback. Emits self-contained Node (`fs` + `JSON.parse`, no npm/jq). JSON + CSV both land; node-verified vs jq/awk; new suite `tests/core/test_typescript_source.pl` green; TS/AJS/VJS suites unaffected. CSV non-comma delimiters + `columns`/`schema` projection noted as follow-ups. See `integration_patches/GP9_INTEGRATION_PATCH.md`. | ✅ (this branch) | powershell-pure (`powershell_compiler.pl`) | S–M | opus |
| G-P10 | vanilla_js | **Inheritance asymmetry** — registers **0** `compile_*_pattern` clauses (annotated_js registered all 7); a caller driving the advanced compiler with `target(vanilla_js)` gets no clause and fails. **Verified.** | ⬜ | copy annotated_js:886-911 (delegate to TS then `vanilla_js_type_strip/2`) | S | opus |
| G-P11 | clojurescript | **FIXED.** Wired the `clojurescript` binding key via `resolve_binding([clojurescript, clojure], …)` — CLJS bindings override, clojure is fallback. Loaded both catalogues (`ensure_cljs_bindings/0`), added a call-head rewrite ordered after the interop rewrite (disjoint tokens, no double-transform); bb runtime skips it. Proven with `parse_double/2 → js/parseFloat`, nbb-verified, JVM Clojure regression green. Bindings 67→68. | ✅ D17 | — | — | done |
| G-P12 | annotated_js, vanilla_js | **PAR-1 arm activation** — arms exist but skip until the harness loads the targets; needs a real tsc/node run (fine in an agent env). | ⬜ | — | S | opus |
| G-P13 | JS pattern targets | **Thin test depth** — 8/3/3/5 referencing files (TS/AJS/VJS/CLJS) vs python 204/rust 155/go 78; no aggregate/negation/service/data-source tests. Grow as G-P1..G-P9 land. | ⬜ | those suites | M (ongoing) | opus |
| G-P14 | annotated_js, vanilla_js | **FIXED (D22).** Hardened both rewriters: vanilla strips union types incl. `\| null`/`\| undefined`; annotated leaves `import * as`/`{a as b}` intact (scanning `as`-cast detector) and strips inline arrow-param annotations; union return → `@returns {A \| B}`. node + `tsc --checkJs` verified. (Left, pre-existing/out-of-scope: `const x = (fact as any).foo;` misdetected as a signature by the pre-existing detector.) | ✅ D22 | — | — | done |

### 3b. Pattern-side N/A / not-a-gap (per A1)
- **Bindings** — JS family LEADS: TS **179** (most in the repo), CLJS 67, vs python 106, go 161, rust 29. Not a gap. (Earlier G-P8 "bindings depth" dropped.)
- **Recursion-hook count** — TS/AJS register all 7 multifile hooks (python registers 4). Coverage is fine; quality is the issue (G-P1/G-P2).
- **Unix-socket / TCP service generation** — mostly N/A for a browser/Node JS target; TS's Express HTTP server/router/client (`typescript_target.pl:517-669`) is the right analogue and is present.
- **AJS/VJS lacking own bindings/components** — by design they inherit TS; not a gap except the concrete bugs G-P4 (emission) and G-P10 (hooks).

---

### Fleet denominators (census — how common each capability is, out of 18 WAM target modules)
- **Lowered emitter:** 14/18 have one; the only 4 without are `wam_c`, `wam_ilasm`, `wam_jvm`, **`wam_javascript`** → G-W1 is the fleet norm, not exotic.
- **External fact sources (LMDB/CSR/TSV):** 14/18; the 4 without are `wam_ilasm`, `wam_jvm`, `wam_kotlin`, **`wam_javascript`** (JS has an empty `fact_sources:{}` scaffold only) → G-W4.
- **Runtime-parser capability:** 7/18 registered (`wam_r/cpp/python/fsharp/haskell/rust/go`); JS not among them → G-W2.
- **Dedicated kernel templates:** only 2/18 (Haskell, F#, 8 each); 8 more drive kernels via the shared `recursive_kernel_detection.pl`; **JS does neither** → kernels are the deferred tail (see §5b), the reference peer `wam_lua` also has neither.
- **WAM bindings file:** 7/18 have none at all; among those that use `declare_binding`, counts run 19–49. JS has a 39-entry catalogue in a different scheme → not a gap.
- **Conformance arm:** JS is registered (16 arms total); `wam_lua` and `wam_clojure` are NOT.

## 4. Open gaps — WAM (`wam_javascript`) ⬜ — CONFIRMED by analyses A2 + census

Mature WAM targets have up to three tiers (interpreter → lowered emitter → FFI kernels);
`wam_javascript` is interpreter-tier only. Reference peer is `wam_lua` (interpreter-only,
dynamically typed); `wam_rust`/`wam_haskell` are the full-stack exemplars. Evidence
cited is from A2 (file:line in the analysis).

| ID | Gap | Status | Copy-from | Size | Collision | Owner |
|---|---|---|---|---|---|---|
| G-W1 | **Tier-2 lowered emitter + `functions`/`mixed` emit modes** — every peer incl. `wam_lua` has `wam_*_lowered_emitter.pl`; JS is interpreter-only (`wam_javascript_target.pl:48-51` throws on other modes). Subsumes the old "emit modes" row. | ⬜ | `wam_lua_lowered_emitter.pl` (closest) | L | ⚠️ emitter + runtime | grok — **after P1 indexing lands** |
| G-W2 | **Runtime term parser** — CLI parses **ints + atoms only** (`runtime.js.mustache:1862-1866`) — no floats/lists/compounds; JS absent from `wam_runtime_parser_capability.pl`. Blocks structured-arg queries + `read_term`. | ⬜ | register `runtime_parser(compiled)` like go/haskell/rust; or extend `parse_cli_atom_or_int` | S–M | low (distinct runtime region) — **parallel-safe** | grok or opus |
| G-W3 | **ISO/library builtin breadth** — missing `term_variables/2`, `numbervars/3`, `=@=/2`; sort family (`sort/2,4`, `msort`, `keysort`, `predsort`); structural list lib (`append`, `reverse`, `nth0/1`, `last`, `sum_list/max_list/min_list`, `select`); atom/string ops (`atom_concat`, `sub_atom`, `atom_length`, `atom_chars/codes`, `char_code`, `split_string`); `format/2,3`, `tab/1`; assoc lib. (NB: JS is *ahead* of Lua on aggregates/findall — not a peer gap there.) | ⬜ | `rust_wam_bindings.pl` for breadth | M | bindings file disjoint; runtime `builtin_*` shares file (low-mod) | grok |
| G-W4 | **External fact-source / data tier** — no LMDB/CSR/TSV/JSON consumption; JS has no fact-source options. | ⬜ | `wam_lua_target.pl:475,545` (lightweight `lua_fact_sources`); rust/haskell for full LMDB+CSR | L | new templates disjoint; some `_target.pl` options | grok — **premature until G-W1** |
| G-W5 | **Switch-indexing conformance row** — runtime wires `SwitchOn*` (`runtime.js.mustache:70-73,1146-1149`) but JS is absent from `WAM_SWITCH_INDEXING_CROSS_TARGET.md:86-99` and its harness. | ⬜ | that harness (rust/haskell/lua rows) | S | disjoint (docs + test) — **follow-on to P1** | main/opus |
| G-W6 | **Parallelism + cost model** — a real fleet capability, present in **≥3 backends** each with its own mechanism: `wam_rust` (rayon / `parallel_gate.pl`, 86 hits), `wam_haskell` (`parMap` + `cost_function.hs.mustache`, 76 hits), `wam_elixir` (actor/Task-based, 23 hits + its own lowered emitter). `wam_javascript` has none. Node analogue = Worker threads. **DEFERRED — low priority: comes after G-W1 (lowered emitter), P1 (indexing), and other single-thread perf.** | ⬜ (deferred) | wam_haskell cost model; wam_elixir for actor style | L | ⚠️ runtime + emitter | — (later) |

---

## 5. Sequencing / collision rules

- **Grok owns `runtime.js.mustache` + `wam_javascript_target.pl`** while indexing (P1) is live. All ⚠️ WAM items serialize behind it — do NOT hand them to a parallel agent.
- **Opus takes disjoint pattern-target gaps** (recursion patterns, components, aggregates, PAR-1 activation) which don't touch the WAM files.
- **INT-0 (main)** applies shared-file wiring centrally (registry, BINDING_MATRIX, harness arms) from each agent's integration patch.
- Worktree agents branch from `main`; for extend-not-rebuild tasks, tell them the target files already exist on `main` and to return net deltas.

---

## 5b. Explicitly N/A or premature for `wam_javascript` (do NOT file as near-term gaps)

Per A2 — recorded so they aren't re-raised:
- **Tier-3 FFI graph kernels** — the reference peer `wam_lua` has none either; not a peer-parity gap. Only after Tier-2 (G-W1).
- **Parallelism gate / cost model** (`parallel_gate.pl`, cost templates) — used by **only** `wam_rust` (haskell uses parMap); not a peer gap.
- **Perf cross-target row** (`WAM_PERF_CROSS_TARGET.md`) — meaningless until a lowered/kernel path exists; add after G-W1.
- **Distinct string type tag / String-vs-Atom standard order** — low-value ISO corner (`WAM_JAVASCRIPT_STATUS.md:93`).

## 6. Pending analysis (populate on completion)

- **A1 (pattern parity + component axis)** → ✅ landed; folded into §3 (rewritten as 13 confirmed gaps G-P1..G-P13) and §3b (N/A). Headline: canned-fib recursion hooks, no structural recursion, no aggregates, component collect-but-never-emit, clojure/CLJS component void, vanilla_js hook asymmetry. Corrected: bindings are not a gap (JS leads), hook *count* is fine.
- **A2 (WAM parity)** → ✅ landed; folded into §4 and §5b (WAM rows collapsed 10→5 confirmed gaps; Tier-3/parallelism/perf reclassified N/A; builtin-surface reframed as ISO breadth since JS is ahead of Lua on aggregates).
- **Census (Sonnet)** → ✅ landed; denominators added above §4. Key: lowered emitter 14/18 (JS one of 4 without), external fact sources 14/18, runtime-parser 7/18, dedicated kernels 2/18. Also confirmed annotated_js/vanilla_js have NO own bindings/runtime/template dirs — by design (inherit TS); not a gap. And 10 WAM modules exist on disk but aren't registry-registered (invoked directly by tests) — an architecture note, not a gap.

All three analyses (A1, A2, census) are now folded in. Next: schedule the wave (see §7).

## 7. Proposed next wave (post-analysis)

Grok stays in the WAM runtime/emitter lane; Opus takes disjoint pattern work; both avoid the files the other is editing.

**Highest impact first:**
1. **G-P1 + G-P2 (real recursion)** — replace canned-fib hooks + add structural recursion. Opus. Large. The most important pattern fix (current output is partly fake).
2. **G-P10 (vanilla_js hooks)** — quick correctness fix. Opus/main. Small.
3. **G-P4 (component emission)** — wire `compile_component` into TS `compile_module`; load orphaned `custom_chart`. Opus. Medium.
4. **G-W5 (switch-indexing conformance row)** — after Grok's P1 indexing lands. Small.
5. **G-W1 (lowered emitter)** — Grok, after indexing. Large; fleet norm (14/18).
6. Then: G-P3 aggregates, G-W2 parser, G-W3 builtin breadth, G-P5/G-P6/G-P7/G-P8/G-P9, G-W4 fact sources.
**Deferred (low priority):** G-W6 parallelism/cost, dedicated kernels, perf row.

# ML environment for the μ-cosine prototype.
# NOTE: the pure-stdlib pieces (train_cosine_mu.py proof, mu_encoder.py forward scaffold,
# gen_mu_pairs.py generator) need NONE of these — they run on stdlib alone. These are only for the
# separate project: training the real encoder with MiniLM-initialised embeddings.
torch>=2.0
numpy>=1.24
sentence-transformers>=2.2    # pulls `transformers`; provides all-MiniLM-L6-v2 (needs HF egress)
einops>=0.7                   # only for the nomic-ai/nomic-embed-text-v1.5 preset in dense_mu_direct.py

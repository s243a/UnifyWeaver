mu_pairs.tsv
# unscored candidate pairs are regenerable (gen_mu_pairs.py / gen_more_sym_pairs.py); the EXPENSIVE
# Haiku-scored sets (mu_pairs_scored*.tsv) are committed — bought once.
mu_pairs_more.tsv
mu_pairs_multidomain.tsv
mu_pairs_4roots.tsv
mu_pairs_matheng.tsv
mu_pairs_corephys.tsv
# generated dense μ maps (regenerate with emit_dense_mu.py / dense_mu_direct.py); the committed
# dense_mu_e5.sample.tsv is the illustrative sample. *.sample.tsv is kept (negated below).
dense_mu*.tsv
!dense_mu*.sample.tsv
# generated decision-band worklist / fused map / pairs (regenerate with boundary_band.py +
# fuse_and_validate.py from the prior); only the *.sample.tsv illustrative slices are committed.
# The EXPENSIVE Haiku rescores live in tests/fixtures/wikipedia_physics_boundary_haiku.tsv (committed).
boundary_band_e5.tsv
boundary_pairs_e5.tsv
!boundary_*.sample.tsv
*.pt
mu_pairs_eng.tsv
mu_pairs_enwiki.tsv
mu_pairs_mathfields.tsv
mu_pairs_scored_cumulative.tsv
mu_pairs_scored_prior.tsv
mu_pairs_alggeom.tsv
mu_pairs_boundary.tsv
mu_pairs_stats.tsv
mu_pairs_sysinfo.tsv

// Generated by UnifyWeaver WAM-JVM Target
// WamState - Warren Abstract Machine state for JVM (Jamaica format)
// Date: {{date}}

import java.util.HashMap;
import java.util.ArrayList;

public class WamState {
    %default_constructor public

    public int pc;
    public int cp;
    public HashMap regs;
    public ArrayList heap;
    public ArrayList trail;
    public ArrayList choicePoints;
    public ArrayList code;
    public HashMap labels;

    public static WamState create() {
        WamState vm = new WamState();
        vm.pc = 1;
        vm.cp = -1;
        vm.regs = new HashMap();
        vm.heap = new ArrayList();
        vm.trail = new ArrayList();
        vm.choicePoints = new ArrayList();
        vm.code = new ArrayList();
        vm.labels = new HashMap();
        ireturn vm;
    }

    // --- Step method (generated) ---
{{step_code}}

    // --- Helper methods (generated) ---
{{helpers_code}}
}

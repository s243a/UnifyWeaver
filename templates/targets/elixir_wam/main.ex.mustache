# Generated by UnifyWeaver WAM-Elixir Target
# Date: {{date}}

defmodule {{module_name}}.Main do
  @moduledoc "Entry point for WAM program"

  def main(args \\ []) do
    state = %WamRuntime.WamState{}
{{predicate_setup}}
    case WamRuntime.run(state) do
      {:ok, final_state} ->
        IO.puts("Query succeeded.")
        {:ok, final_state}
      :fail ->
        IO.puts("Query failed.")
        :fail
    end
  end
end

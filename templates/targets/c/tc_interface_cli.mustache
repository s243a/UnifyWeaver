int main(int argc, char** argv) {
    char line[MAX_LINE];

    {{> tc_input_stdin}}

    if (argc < 2) {
        fprintf(stderr, "Usage: %s <start> [target]\n", argv[0]);
        return 1;
    }

    if (argc == 2) {
        find_all(argv[1]);
    } else {
        if (check_path(argv[1], argv[2])) {
            printf("%s:%s\n", argv[1], argv[2]);
        } else {
            return 1;
        }
    }
    return 0;
}
